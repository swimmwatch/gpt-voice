# Local Whisper Technical Specification

Status: Approved
Date: 2026-08-24
Spec slug: `local-whisper`
Decision evidence: [decisions.yaml](decisions.yaml)
Research baseline: [Local Whisper Runtime and GPU Compatibility Research](../../researches/local-whisper/main.md)
Prior approval: **APPROVAL-001** Explicit `approve` for revision 2 recorded in the persistent `spec:local-whisper` interview on 2026-08-01.
Revision 3 review: changes requested pending the separate Task 08–16 feasibility audit.
Revision 4 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-01 after the resulting worker, IPC, packaging, diagnostics, and failure-contract repair.
Revision 5 review: reopened after commit-pinned upstream and platform-source audits found invalid Windows fixed-handle semantics, incomplete Linux per-message credential authentication, unsafe native loader/fallback behavior, and non-authoritative in-process unload/cancellation assumptions. Revision 4 remained the approved baseline during review.
Revision 5 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-01 after the commit-pinned runtime and platform contract repair.
Revision 6 review: reopened after the second commit-pinned and platform audit to make numeric-token validation implementable, make Linux fd `3` transfer collision-safe, order the Windows suspended bootstrap correctly, distinguish HIP's upstream build floor from product compatibility, and close remaining ambient backend-build defaults. Revision 5 remained the approved baseline during that review.
Revision 6 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-02 after the bounded source and platform clarifications.
Revision 7 review: reopened on 2026-08-02 after the user removed Faster-Whisper/CTranslate2 from the product and requested a single-engine plan. Revision 6 remains the approved baseline until this draft is explicitly approved.
Revision 7 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-02 after the single-engine contract and active-artifact removal criteria were normalized.
Revision 8 review: reopened on 2026-08-03 after Task 19 reached the intentionally deferred production archive-format gate. The user durably selected the restricted deterministic `tar.gz` contract; revision 7 remains the approved baseline until this draft is explicitly approved.
Revision 8 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-03 after the restricted production archive, trust, compatibility, and acceptance contract was normalized.
Revision 9 review: reopened on 2026-08-03 after the blocker audit found a stale revision-7 plan baseline, an uncomposed production artifact pipeline, unresolved qualification-versus-production trust ownership, no concrete model origin/corpus/variant matrix, unfrozen candidate/profile evidence, and a stale claim that the preceding Linux package was unavailable. Revision 8 remains the approved baseline until this draft is explicitly approved.
Revision 9 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-03 after the blocker, public-origin, qualification-trust, candidate, corpus, model-variant, measurement, and production-gate contract was normalized.
Revision 10 review: reopened on 2026-08-03 after Task 19 proved that revision 9 required `candidateDigest` to hash `profileDigests` while every `profileDigest` hashed `candidateDigest`, and also required Linux-only Task 19 to freeze exact Windows package/runtime/profile inputs before Windows Task 20 could produce them. Revision 9 remains the approved baseline until this draft is explicitly approved.
Revision 10 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-03 after the acyclic shared-candidate/platform-branch/aggregate identity graph and cross-platform freeze ownership were normalized.
Revision 11 review: reopened on 2026-08-04 after the user separated implementation completion from representative hardware qualification. Revision 10 remains the approved baseline until this draft is explicitly approved.
Revision 11 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-04 after complete Windows/Linux implementation was separated from later representative platform testing.
Revision 12 review: reopened on 2026-08-04 after the completed Task 19 gate proved only static implementation readiness while the ordinary Linux application remained disabled by its deferred production catalog. The initial draft required one real model through the normal application on CPU and NVIDIA CUDA; revision 11 remained the approved baseline.
Revision 13 review: reopened on 2026-08-04 after the user rejected revision 12's one-model availability boundary. Task 19 must install and expose all six curated models through the normal application for CPU and NVIDIA CUDA, with a bounded representative runtime smoke rather than the later hour-long qualification matrix; revision 11 remains the approved baseline until this draft is explicitly approved.
Revision 14 review: reopened on 2026-08-04 after the user rejected any Task 19 handoff that relied on preinstalled model files. The ordinary application must provide and exercise direct anonymous Hugging Face download for all six exact pinned models; revision 11 remains the approved baseline until this draft is explicitly approved.
Revision 14 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-04 after authenticated non-packaged activation, real all-six Hugging Face download/installation, all-model CPU/CUDA readiness, and the bounded ordinary-app execution proof were normalized.
Revision 15 review: reopened on 2026-08-05 after the user requested a provider-specific main-screen `Load model`/`Free model` control following the Ollama Prettify precedent. Revision 14 remains the approved baseline until this draft's narrow main-window residency-command, state, failure, concurrency, and acceptance amendments are explicitly approved.
Revision 15 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-05 after the active-provider state matrix, closed stale-checked main lifecycle command, accessible sanitized failure behavior, provider-switch blocking, and objective acceptance coverage were normalized.
Revision 16 review: reopened on 2026-08-06 after the user requested hardware-matched RTX 30-, 40-, and 50-series CUDA runtime delivery on Linux and Windows. Revision 15 remains the approved baseline until this Draft's compute-target, pre-install discovery, option-filtering, migration, packaging, and qualification amendments are explicitly approved.
Revision 16 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-06 after the per-target runtime, hardware applicability, settings migration, privacy, and six-cell Linux/Windows qualification contract was normalized.
Revision 17 review: reopened on 2026-08-06 after the user clarified that their available NVIDIA test hardware is limited to RTX 50-series devices. Revision 16 remains the approved baseline until this Draft's evidence-availability and external-manual-gate clarification is explicitly approved.
Revision 17 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-06 after RTX 50-only user test availability and external Pending ownership for RTX 30/40 physical qualification were normalized.
Revision 18 review: reopened on 2026-08-07 after the user narrowed the current NVIDIA CUDA release scope to RTX 50-series hardware and postponed RTX 30/40 implementation and qualification. Revision 17 remains the approved baseline until this Draft's RTX 50-only delivery, fail-closed compatibility, and reduced Linux/Windows qualification contract is explicitly approved.
Revision 18 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-07 after RTX 50-only Linux/Windows delivery and qualification, fail-closed RTX 30/40 behavior, and the non-executable future-work boundary were normalized.
Revision 19 review: reopened on 2026-08-07 after the user requested a complete CI/CD contract for application installers and Local Whisper native runtime packs and required every user-installable binary to originate from immutable assets in the approved GitHub Release. Revision 18 remains the approved baseline until this Draft's protected signing, exact signed-candidate promotion, same-tag asset, final-origin verification, failure-recovery, and CI authority boundaries are explicitly approved.
Revision 19 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-07 after protected production-equivalent CI/CD, final signed-candidate qualification and exact promotion, same-tag immutable GitHub Release assets, final-origin verification, non-clobbering recovery, and the pinned Hugging Face model exception were normalized.
Revision 20 review: reopened on 2026-08-07 after the user required a dedicated release branch and release pull request to own changelog/version preparation, review of registered manual scripts, exact signed-byte Linux/Windows qualification before merge, committed release metadata, and post-merge creation of the immutable tag on the unchanged qualified pull-request head. Revision 19 remains the approved baseline until this Draft's release-branch identity, merge, tag, and pre-merge gate contract is explicitly approved.
Revision 20 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-07 after committed release-branch preparation, pre-merge exact signed-byte manual qualification, preserving release-PR merge, post-merge immutable tag creation, and separate publication authority were normalized.
Revision 21 review: reopened on 2026-08-21 after the user reduced the remaining lifecycle to Build and Deploy, selected a public signed cross-platform `v2.4.0-alpha.1` prerelease before `v2.4.0`, and required a fresh fully requalified Linux/Windows final candidate. Revision 20 remains the approved baseline until this Draft is explicitly approved.
Revision 21 approval: explicit `approve` recorded in the persistent `spec:local-whisper` interview on 2026-08-21 after the Build/Deploy-only lifecycle, public cross-platform alpha boundary, fresh final candidate, and complete Linux/Windows final qualification were normalized.
Revision 22 review and approval: revised and automatically approved on 2026-08-22 after the user replaced the single-alpha sequence with an iterative immutable `v2.4.0-alpha.N` Build/Deploy loop before the fresh fully qualified `v2.4.0` final release.
Revision 23 review: reopened on 2026-08-22 after the user required each alpha to be built and publicly released before separate Linux and Windows testing, and removed a separate platform-test phase from the final production release. Revision 22 remains the approved baseline until this Draft resolves post-alpha failure behavior, alpha test depth, and final evidence lineage.
Revision 23 approval: automatically approved on 2026-08-22 after bounded post-publication Linux and Windows alpha smoke, fail-closed next-alpha recovery, and reproducible latest-alpha lineage for a fresh final release without physical final testing were normalized and passed the six-perspective gap analysis.
Revision 24 approval: automatically approved on 2026-08-22 after the user required every application, runtime, catalog, signature, and verification component needed for complete Linux and Windows alpha testing to be published together. This revision clarifies that six output classes are a logical matrix rather than six physical files and that static policy validation cannot substitute for production artifact construction.
Revision 25 approval: automatically approved on 2026-08-22 after the user corrected the Task 32 boundary: private candidate construction must not execute publication, but it must preserve and regression-test the guarded tag-and-publication capability that Task 33 uses to publish the real alpha before Linux and Windows testing.
Revision 26 approval: automatically approved on 2026-08-24 by the user's
explicit `local-whisper-alpha-release` implementation plan. One six-hour
version-scoped Watch invocation now owns the complete Task 32-to-Task 33
prepublication repair and immutable alpha.1 publication loop. Promotion reuses
the exact versioned candidate from its prior run without rebuilding; success
stops after the green publication workflow and public prerelease identity,
before Tasks 34/35.

## 1. Objective

Local Whisper SHALL be one selectable local Voice provider that runs buffered batch Whisper speech-to-text inference on the user's device and returns final transcription text through the existing GPT-Voice completion flow.

The feature SHALL provide:

- one fixed `whisperCpp` engine identity plus explicit GPU/CPU target, backend, device, model, revision, and variant selection;
- validated basic and advanced inference settings;
- explicit installation, update, rollback, and removal of immutable native `ggml` model revisions;
- explicit installation and removal of app-version-pinned runtime packs;
- hardware-matched runtime applicability so only host- and device-compatible installation actions are offered;
- lazy model loading, settings-screen `Load now`/`Unload`, and a main-screen `Load model`/`Free model` control;
- an exact, model-specific answer to whether the selected configuration can run on the current device;
- safe failure without automatic target, backend, device, model, revision, variant, or CPU fallback.

**OUT-001** Success means a user can select Local Whisper, deliberately install the required runtime and model, validate and load the exact configuration, transcribe locally, unload its RAM/VRAM allocation, and understand every unsupported, missing, invalid, or failed state without changing existing remote-provider behavior.

## 2. Normative Language

`SHALL` and `MUST` are mandatory. `SHALL NOT` and `MUST NOT` are prohibited. `MAY` is optional only when the surrounding requirements remain satisfied.

Support tier, artifact setup, capability validation, residency, and active transcription are separate dimensions throughout this specification. A path may be supported but not installed, installed but not validated, validated but unloaded, or loaded but busy.

## 3. Scope

### 3.1 Included

**SCOPE-001** The first release SHALL expose one provider named `Local Whisper`. The phrase “local dispart provider” is treated as a typo, not as a separate provider. The provider SHALL use only the fixed `whisperCpp` engine identity, based initially on pinned `whisper.cpp` v1.9.1 build inputs. The engine discriminator SHALL remain in validated settings, IPC, capability, cache, audit, and artifact identities as the non-editable literal `whisperCpp`; it SHALL NOT be a user-selectable control.

An immutable runtime manifest SHALL own the complete engine and dependency revisions used in a shipped pack. Changing those baselines requires an explicit specification/catalog revision and qualification; upstream `latest` is never resolved at runtime.

**MODEL-005** The curated multilingual logical model catalog SHALL contain only:

- `tiny`;
- `base`;
- `small`;
- `medium`;
- `large-v3`;
- `large-v3-turbo`.

**MODEL-001** Each logical model SHALL be selectable and manageable from the provider settings. **MODEL-002** Each installable revision SHALL be immutable and independently downloadable and removable. **VRAM-001** The selected model SHALL support explicit load and unload operations in addition to lazy loading.

**SCOPE-002** The provider result contract SHALL contain final transcription text only and SHALL enter the existing successful transcription completion flow exactly once.

**DEV-001, SEC-015** Before production publication exists, an explicitly opted-in, non-packaged Linux development launch MAY activate the ordinary Local Whisper application flow with a locally generated, authenticated `qualification`-purpose catalog and its exact runtime/model inputs. This is a development activation channel, not a fifth catalog purpose or a publishable package mode. Packaged builds, production-purpose collection, and ordinary development launches without the explicit activation SHALL remain fail-closed on the deferred-publication catalog.

### 3.2 Excluded

The first release SHALL NOT include:

- Distil-Whisper, English-only model families, arbitrary third-party models, or user model import;
- Faster-Whisper, CTranslate2 model conversions, embedded Python/NumPy inference runtimes, and alternate Local Whisper inference engines (**NONGOAL-004**);
- translation to English, VAD, timestamps, segments, diarization, or partial/interim result contracts (**NONGOAL-003**);
- automatic fallback between GPU and CPU, backends, devices, models, revisions, or variants;
- automatic model/runtime downloads, updates, selection changes, or storage cleanup;
- custom model/runtime storage paths;
- renderer WebGPU inference or any dependency on Chromium GPU acceleration;
- DirectML or Windows ML (**NONGOAL-002**);
- a user-managed Python, Conda, compiler, CUDA toolkit, or arbitrary executable integration;
- RTX 30 / `sm_86` or RTX 40 / `sm_89` CUDA runtime delivery, installation actions, qualification, or support claims; these families remain future deferred work and require a later approved specification and plan revision before implementation;
- fixture, unsigned, user-authored, or renderer-selected catalogs, origins, keys, artifact paths, or trust purposes;
- production-ready macOS or Apple Silicon inference (**NONGOAL-001**).

## 4. Observed Baseline and Invariants

**BASE-001** Local Whisper has an unshipped, non-production cross-platform implementation foundation: the repository contains shared/settings/catalog contracts; both authenticated transfer profiles; managed artifact/inventory/filesystem boundaries; Linux and Windows filesystem/process-owner adapters; framed protocol; native launcher/guard; CPU/CUDA workers and deterministic runtime-pack tooling; process-owned production environment; runtime/device discovery; exact launch leasing and model authority; worker residency/lifecycle; safe artifact removal; and qualification schemas/tooling. Revision-11 static implementation readiness passes, but the ordinary application still receives the immutable deferred-publication sentinel with an empty production keyring/origin set and exits before hardware validation. No public alpha, final-origin alpha deployment, post-release Linux/Windows smoke result, or platform release-support claim exists. Existing work remains subject to this specification and the rebuilt plan.

**ARCH-001** Local Whisper SHALL integrate with the existing main-owned buffered batch Voice-provider flow. It SHALL NOT add a streaming recorder or a new renderer audio path.

**ARCH-002, ARCH-008, SEC-006** The provider metadata and readiness types SHALL gain a real `localRuntime`/no-auth contract. Local Whisper SHALL NOT create or persist a dummy API key, access token, cookie, or browser session.

**ARCH-003, ARCH-006, LIFE-001** Provider factories may continue creating side-effect-free provider instances, but the main-process composition root SHALL own the long-lived Local Whisper coordinator, catalogs, repositories, downloader, capability service, worker supervisor, and residency state. Module-level constructed mutable runtime instances are prohibited.

**IPC-001, SEC-001** Filesystem access, executable selection, native process execution, downloads, integrity checks, GPU/CPU probing, model inventory, and residency SHALL remain in Electron main. The renderer SHALL never receive authority to choose a URL, filesystem path, executable, environment variable, or command argument.

**ARCH-004, IPC-002** Renderer access SHALL use additive, typed `window.electronAPI` operations and trusted-sender-validated IPC. Main SHALL authoritatively validate every command even when the UI has already disabled or validated it.

**SET-001** Local Whisper settings SHALL use a versioned, namespaced, private provider-settings repository rather than overloading authentication settings. **UI-002** Its form SHALL work within the current scrollable 560×680 provider-settings window and its 440×520 minimum size.

**COMP-001, COMP-002** Existing provider IDs, defaults, remote inference, batch recording, retry, and provider-switch behavior SHALL remain compatible. Installing or enabling Local Whisper is optional.

**COMP-003** On successful local transcription, existing cache, clipboard, history, timing, notification, and audit completion behavior SHALL be reused. A failed, cancelled, conflicting, or partial local operation SHALL not mutate clipboard, successful-result cache, or transcription history.

**SEC-002, DIAG-001** Existing privacy rules remain authoritative: audio, transcripts, initial prompts, raw worker output, arbitrary paths, and native exception text SHALL NOT appear in routine logs, audit records, or diagnostics.

**PKG-001** Existing base Windows and Linux installers SHALL not embed GiB-scale model weights or unrequested accelerator packs.

**DOC-001** Documentation that currently says no local Whisper model, CUDA setup, or GPU is required SHALL be revised to make clear that this remains true for the base app and remote providers, while Local Whisper is an optional explicit download with platform-specific prerequisites.

## 5. Resolved Product Contract

The following choices are fixed for this Draft:

1. One Local Whisper provider exposes the fixed, non-editable `whisperCpp` engine discriminator and no engine selector (**ARCH-007, UI-003**).
2. One pinned `whisperCpp` engine ships behind the shared domain contract with backend-specific workers, runtime packs, `ggml` model artifacts, and qualification profiles (**RUNTIME-001**).
3. GPU and CPU are explicit targets with GPU as the initial default. There is no `auto` target and no GPU-to-CPU fallback (**COMP-005, SET-002, CAP-005**).
4. `whisperCpp` is eligible for NVIDIA, AMD Preview, and CPU paths (**COMP-006**).
5. Runtime packs and models are downloaded only after explicit user action. Runtime packs are app-version-pinned and executable artifacts are signed (**PKG-002, SEC-003, OPS-001**).
6. Models and runtimes use fixed application-managed storage. Custom locations and import are unavailable (**MODEL-006, SEC-004**).
7. Basic controls are always visible; reviewed expert controls are under `Advanced` (**UI-004, SET-003**).
8. Loading is lazy on the first eligible cache-miss transcription that requires inference, with equivalent `Load now` and explicit `Unload` operations. Runtime, target, backend, device, model, or variant changes unload the current worker. Provider change and application exit unload it as well (**VRAM-002, LIFE-002**).
9. `Ready` is earned only by backend initialization, allocation, full model load, and bounded warm-up for the exact capability fingerprint (**CAP-006, CAP-007**).
10. Updates are explicit and installed alongside old immutable revisions. Nothing changes the selected revision automatically (**MODEL-007, RUNTIME-003, COMP-007**).
11. Deleting the selected or loaded model confirms, rejects active transcription, unloads first, removes exact managed files, preserves the selection as `Model missing`, and performs no fallback (**MODEL-008, VRAM-003, FAIL-001**).
12. Conflicting lifecycle operations fail immediately and require explicit retry; unrelated downloads may continue (**LIFE-003, FAIL-002**).
13. Local Whisper remains selectable when unsupported or Not ready. The UI SHALL show the known tier/state/reason, and load/transcription SHALL still fail safely if invoked (**UI-005, CAP-008**).
14. Runtime packs SHALL be downloaded from immutable GPT-Voice GitHub Release assets; model files SHALL be downloaded directly from the reviewed public `ggerganov/whisper.cpp` Hugging Face repository at an immutable commit. GPT-Voice SHALL NOT republish upstream model bytes merely to fit its runtime archive format (**DIST-001, DIST-002**).
15. Runtime packs use `restricted-tar-gzip-v1`; one-file upstream models use `pinned-raw-model-v1`. Neither transport is remotely selectable (**PKG-005, SEC-008**).
16. Release 1 enables exactly one model variant per family: `full` for `tiny`, `base`, `small`, and `medium`; `q5_0` for `large-v3` and `large-v3-turbo`. Other catalog variants remain `estimateOnly`. Physical release smoke covers `base/full`; it does not create per-model performance or resource claims for the other variants (**MODEL-011, QUAL-003**).
17. Fixture, qualification, and production trust remain isolated. Public-alpha platform smoke SHALL consume only the exact production installers, runtime packs, catalogs, signatures, and manifests already published under that alpha's immutable GitHub Release tag; no qualification-purpose transport may substitute for final-origin bytes (**PKG-011, SEC-014, OPS-003**).
18. Release smoke uses one pinned, non-private deterministic audio fixture and forbids private user recordings, prompts, transcripts, paths, or device identifiers in public evidence. The existing public FLEURS corpus and direct-engine tooling MAY remain for nonblocking diagnostics but is not a `v2.4.0` release gate (**QUAL-002, PRIV-005**).
19. Release evidence uses one acyclic generation/deployment/platform-smoke/lineage graph. Every alpha first seals and publicly deploys one signed six-output generation; Linux and Windows then independently seal bounded smoke branches against those exact public bytes; one alpha aggregate records whether both passed. Final readiness binds the latest passing alpha aggregate, an allowlisted final-only source delta, and fresh final reproducibility/signing evidence without creating final physical-test branches (**REL-001, QUAL-001, COMP-012**).
20. Cross-platform implementation readiness and public-alpha hardware smoke are separate gates. The complete Windows and Linux CPU/CUDA product paths—including platform adapters, packaging contracts, process ownership, model authority, lifecycle, and deterministic automated checks—SHALL be implemented. The already-required ordinary Linux development proof installs all six exact release-1 models and runs bounded `base/full` once on CPU and CUDA. For the release loop, each public alpha receives a separate bounded Linux smoke and bounded Windows smoke using its final-origin assets; the former all-six-model accuracy/performance/resource/repetition profile is nonblocking diagnostic work and is not required for alpha or final release (**DL-005, MODEL-001, MODEL-002, MODEL-005, IMPL-001, IMPL-002, QUAL-004**).
21. The Task 19 normal-app proof SHALL use an explicit non-packaged development activation that consumes purpose-isolated authenticated qualification inputs. Packaged builds SHALL continue using only production-purpose trust and SHALL remain fail-closed until protected production publication. No fixture key, private key, unsigned catalog, verification bypass, renderer path/URL authority, or persisted trust-mode toggle is permitted (**DEV-001, SEC-015**).
22. While Local Whisper is the committed active provider, the main toolbar SHALL expose one stateful `Load model`/`Free model` control backed by a narrow main-window residency command. It SHALL permit the same eligible on-demand full validation/load/warm-up as lazy first use, free only an idle loaded worker, remain non-cancelling while busy, and block provider switching while its load or unload command is pending. It SHALL NOT expose settings, artifact, download, path, URL, device-authority, or provider-selection authority (**ARCH-011, IPC-004, UI-009, LIFE-007, FAIL-004, SEC-016**).
23. Current NVIDIA CUDA delivery SHALL use one independently authenticated per-platform `sm_120a-real` runtime pack for supported GeForce RTX 50-series desktop and laptop devices. Main SHALL derive applicability from the selected physical device, host platform/architecture, driver, and stable resource prerequisites before offering an explicit download action. RTX 30 / `sm_86`, RTX 40 / `sm_89`, and every other unsupported compute target SHALL receive no CUDA runtime installation action. Main SHALL perform no background download, PTX-JIT fallback, runtime substitution, backend/device/model fallback, or silent migration of an existing stored revision (**CAP-018, COMP-013, DIST-003, RUNTIME-005, UI-010, VAL-004**).
24. CI/CD SHALL validate and produce compatible Linux and Windows application installers plus separate per-platform CPU and RTX 50 `sm_120a-real` runtime-pack assets. Pull-request and main-branch workflows validate only; final signing and publication occur only through a protected approved release workflow. Every user-installable application or Local Whisper native binary SHALL originate from immutable assets under the approved application GitHub Release tag; CI artifacts, mutable GitHub URLs, mirrors, and arbitrary origins are never installation sources. Models remain the exact pinned public Hugging Face exception (**CI-001, CI-003, CI-004, DIST-004, REL-002**).
25. Each version uses one combined release operation that performs Build and Deploy in order without a physical platform-test gate between them. The required loop is `Release v2.4.0-alpha.N → Linux smoke + Windows smoke → review feedback → either Release v2.4.0-alpha.(N+1) or Release v2.4.0`. `N` starts at 1 and increments without gaps. Linux and Windows smoke are independent post-publication verification activities, not permission to mutate the deployed alpha (**REL-003, QUAL-007**).
26. Every `v2.4.0-alpha.N` SHALL be a public immutable GitHub prerelease with all six newly built and signed Linux/Windows outputs before its two platform smokes begin. It SHALL remain visibly prerelease and SHALL NOT promote final Production support. Either smoke failure blocks final and requires a fresh sequential alpha containing every accepted fix; the failed alpha remains public and unchanged. Only after both latest-alpha smokes pass and feedback requires no further alpha may `v2.4.0` freeze a distinct final head limited to final identity/changelog/tag-derived preparation, rebuild and sign all six outputs, pass deterministic automated and reproducibility gates, and publish without separate physical Linux or Windows testing (**REL-004, QUAL-007**).
27. A public alpha is testable only when its same-tag GitHub Release contains the complete physical asset inventory needed by both platform smokes. The six logical output classes SHALL expand to every configured Linux installer format, the Windows installer, four per-platform CPU/RTX 50 runtime archives, and all detached signatures, production catalogs/keyring material, checksums, signed release manifest, SBOMs, notices, provenance, and compatibility records required to authenticate and exercise them. The exact pinned Hugging Face models remain the only externally hosted test inputs. Job names, validators, metadata-only fixtures, qualification packs, or disabled Local Whisper packaging do not satisfy this delivery contract (**CI-001, CI-002, CI-007, DIST-004, REL-002**).
28. A task's prohibition on executing a later irreversible action SHALL constrain invocation and authority, not delete the guarded capability owned by the later task. The production release workflow SHALL retain one explicit publication gate that is disabled by default for Task 32, consumes only the exact verified complete candidate, and is enabled by Task 33 only with an explicit matching release tag and publication authorization. Regression policy SHALL fail if the publication path disappears, becomes enabled by default, bypasses candidate verification, gains an alternate trigger, or grants write authority to candidate jobs (**CI-003, CI-004, CI-007, CI-008, REL-002**).

## 6. Support Matrix

Support tier is a reviewed product claim, not a result of probing. `Production target after gate` means the path SHALL NOT be labeled Production in a release until the exact hardware gate in Section 19 has passed for that OS and engine.

| OS / architecture                       | Engine       | Target / backend         | Device                | Release tier                                                      |
| --------------------------------------- | ------------ | ------------------------ | --------------------- | ----------------------------------------------------------------- |
| Windows x64                             | `whisperCpp` | GPU / CUDA `sm_120a`     | RTX 50 desktop/laptop | Production target after the Windows `sm_120a` gate                |
| Linux x64                               | `whisperCpp` | GPU / CUDA `sm_120a`     | RTX 50 desktop/laptop | Production target after the Linux `sm_120a` gate                  |
| Windows/Linux x64                       | `whisperCpp` | GPU / CUDA `sm_86`       | RTX 30 desktop/laptop | Planned/unavailable; future specification revision required       |
| Windows/Linux x64                       | `whisperCpp` | GPU / CUDA `sm_89`       | RTX 40 desktop/laptop | Planned/unavailable; future specification revision required       |
| Windows x64                             | `whisperCpp` | GPU / Vulkan             | AMD                   | Preview; no hardware evidence in this specification task          |
| Linux x64                               | `whisperCpp` | GPU / HIP                | AMD                   | Preview; exact allowlisted OS/ROCm/device/`gfx` intersection only |
| Linux x64                               | `whisperCpp` | GPU / Vulkan             | AMD                   | Preview; explicit alternative, never a HIP fallback               |
| Windows x64                             | `whisperCpp` | CPU / CPU                | x64 CPU               | Production target after the Windows CPU gate                      |
| Linux x64                               | `whisperCpp` | CPU / CPU                | x64 CPU               | Production target after the Linux CPU gate                        |
| macOS arm64                             | `whisperCpp` | GPU / Metal              | Apple Silicon         | Planned/unavailable skeleton only                                 |
| macOS arm64                             | `whisperCpp` | CPU or any other backend | Any                   | Unavailable in release 1                                          |
| Other OS/architectures or unlisted GPUs | `whisperCpp` | Any                      | Any                   | Unsupported                                                       |

**COMP-004** NVIDIA and CPU Production claims remain conditional release gates rather than current facts. **NVIDIA-001, AC-MAN-001** The available Linux x64 laptop—NVIDIA GeForce RTX 5070 Ti Laptop GPU, driver 595.84, compute capability 12.0, and 12,227 MiB VRAM—can supply Linux NVIDIA and hybrid-device evidence only. The user also reports an available Windows RTX 5090 host with current drivers. These RTX 50 / compute-capability-12.0 systems are the only post-alpha NVIDIA smoke hosts in the current scope; they do not prove AMD or Apple behavior.

**COMP-013, RUNTIME-005, QUAL-005** RTX family labels are convenience groupings, not runtime authority. Main SHALL map only the approved compute-capability-12.0 RTX 50 intersection to `sm_120a-real`. Compute capability 8.6, compute capability 8.9, an unlisted compute capability, a non-NVIDIA device, an incompatible driver, an unsupported OS/CPU architecture, or an unmet stable capacity prerequisite SHALL receive no CUDA runtime download action. Production eligibility is independent for Linux `sm_120a` and Windows `sm_120a` and requires the latest public alpha's passing post-release execution defined by **AC-MAN-017** and **AC-MAN-018**; one operating system never substitutes for the other.

**CPU-001, COMP-008, AC-MAN-002** `whisperCpp` targets Production CPU support on Windows and Linux x64 only after that operating system's latest-alpha bounded CPU smoke passes. This CPU contract never extends to macOS and makes no per-model performance claim.

**AMD-001, AMD-002** AMD is technically feasible but untested here and SHALL remain Preview. Documentation and mocked probes do not constitute hardware validation.

**AMD-003** Windows AMD SHALL use `whisperCpp` Vulkan only. Windows HIP, DirectML, and Windows ML are excluded.

**AMD-004** Linux AMD SHALL prefer HIP only for an exact supported intersection and SHALL expose Vulkan as a separate explicit Preview choice. A HIP failure SHALL NOT select Vulkan.

**AMD-006, PKG-003, CAP-009** Pinned `whisper.cpp` has an upstream HIP compilation floor of `>= 6.1`; that range is not a product allowlist. Each Linux HIP pack SHALL pin exactly one reviewed ROCm/HIP release and one exact supported matrix intersection. An immutable app manifest SHALL enumerate Linux distribution/version and x64 architecture, kernel/amdgpu ABI, exact ROCm/HIP package versions, bundled/external SONAMEs and binary build identities, AMD PCI device IDs, explicitly compiled `AMDGPU_TARGETS`/`gfx` values, applicable CPU PCIe atomics capability, `/dev/kfd` and render-node requirements, and the complete shared dependency closure. Arbitrary system/bundled ROCm mixing, a merely newer version, and every unlisted combination fail closed.

**CAP-002, CAP-004** Vendor enumeration alone never proves compatibility. Every path must satisfy its backend features, allocation/compute probe, full selected-model load, and warm-up.

**MAC-001, MAC-002, MAC-003** macOS SHALL have only shared protocol types, the `metal` backend identifier, an unavailable adapter skeleton, and UI/type tests. It SHALL publish no Local Whisper runtime or model download catalog, allow no runtime/model download or execution action, make no CPU exception, and never report `Ready`.

## 7. Architecture and Ownership

```mermaid
flowchart LR
    UI[Renderer provider settings]
    PRELOAD[Typed preload API]
    IPC[Trusted main IPC]
    COORD[Local Whisper coordinator]
    CAP[Capability service]
    CATALOG[Immutable signed catalogs]
    RUNTIME[Runtime repository]
    MODEL[Model repository]
    DOWNLOAD[Artifact downloader]
    SUPERVISOR[Worker supervisor]
    WORKER[Whisper.cpp sidecar]
    COMPLETE[Existing transcription completion]

    UI --> PRELOAD --> IPC --> COORD
    COORD --> CAP
    COORD --> CATALOG
    COORD --> RUNTIME
    COORD --> MODEL
    COORD --> DOWNLOAD
    COORD --> SUPERVISOR --> WORKER
    WORKER --> COORD --> COMPLETE
```

### 7.1 Coordinator

The process-owned coordinator SHALL own:

- current normalized settings and a monotonically increasing configuration epoch;
- immutable catalog and sanitized inventory snapshots;
- support, setup, capability, residency, and activity states;
- the one active worker supervisor;
- operation conflict arbitration and per-artifact locks;
- sanitized progress and error snapshots.

Provider instances SHALL delegate transcription and shutdown to this coordinator. Reading provider metadata or a settings snapshot SHALL NOT start a worker, probe a device, access the network, download an artifact, or allocate RAM/VRAM.

**ARCH-010, ARCH-011, IPC-003, IPC-004, UI-008** The coordinator SHALL expose four non-overlapping typed surfaces:

- the existing provider-dispatch port for pre-cache eligibility, transcription, cancellation, provider switching, and shutdown;
- a settings-window command/query port for validated settings, artifact, capability, load/unload, reset, and open-managed-folder operations;
- a narrowly authorized main-window residency-command port for `load` and `unload` only;
- a read-only main-window status port for the current sanitized readiness snapshot and subscription.

The settings and main windows SHALL be authenticated independently by exact live `WebContents` ownership and expected frame URL. Main-window status or residency-command access SHALL NOT authorize settings, artifacts, downloads, compatibility checks, managed-folder access, full settings snapshots, or provider selection. Subscription registration SHALL atomically return or replay the current snapshot; later events SHALL carry a strictly increasing `snapshotRevision`, and unsubscribe, reload, replacement, and close SHALL remove the old subscriber without cancelling process-owned downloads or lifecycle operations.

**IPC-004, LIFE-007, SEC-016** The closed main-window command DTO SHALL contain exactly `{ kind: 'load' | 'unload', expectedSnapshotRevision }`, where the revision is a positive safe integer. The closed result SHALL contain only the requested kind, success state, the current `LocalWhisperMainStatusSnapshot`, and a sanitized failure or `null`. Main SHALL reject an unknown or extra field, wrong sender, inactive Local Whisper provider, stale revision, ineligible state, duplicate/conflicting command, or forged value before invoking the coordinator. An inactive provider or lifecycle conflict SHALL return the applicable sanitized operation-conflict failure; a revision mismatch SHALL return the sanitized stale-state failure. No rejected command may load, unload, cancel, switch providers, start a download, mutate settings, or produce a privileged side effect.

After validating sender, DTO, committed active provider, current snapshot revision, and action eligibility, main SHALL delegate exactly once to the existing coordinator `loadNow` or `unload` operation and SHALL record the same sanitized lifecycle audit category as the equivalent settings action exactly once. Renderer presentation and button hiding are never authorization. A command response or local pending flag SHALL NOT overwrite an already received higher `snapshotRevision`; subscription snapshots remain the sole authoritative residency state after renderer reload, replacement, or result/event reordering.

The renderer snapshot SHALL contain only stable IDs, sanitized labels, configuration/inventory epochs, `snapshotRevision`, option availability/tier/reasons, selected-but-unavailable entries, recommended/saved/default markers, remembered dependent selections, field validation issues, approximate family ranges, exact selected estimates when valid, sanitized storage labels/counts, artifact/action/progress state, and support/setup/capability/residency/activity. It SHALL expose only `hasInitialPrompt`, never prompt text, paths, URLs, executable data, native bindings, device authority, or raw errors. The smaller main-window status and residency-command result SHALL remain within their stricter allowlists and SHALL NOT expose the full settings-window snapshot.

Provider selection SHALL distinguish a pending renderer choice from the authoritative committed provider. Main SHALL return a typed result containing the committed provider ID and sanitized readiness revision. The renderer SHALL commit the new selection only after main succeeds; `OPERATION_CONFLICT` or another failure leaves the prior provider selected and visible.

**DEV-001, SEC-015** Development artifact activation SHALL be owned by the main-process composition root and require both a non-packaged application identity and an explicit launch-time opt-in. The opt-in SHALL identify only a main-readable activation descriptor; renderer state, saved provider settings, downloaded catalog metadata, and ambient discovery SHALL never select a trust purpose, public key, origin, catalog, runtime, model, or path. The descriptor SHALL bind one authenticated `qualification` catalog document, its qualification public keyring, exact allowed origins, and expected app/worker compatibility. It SHALL be revalidated on every launch, SHALL NOT persist as application configuration, and SHALL construct the same production coordinator, artifact, capability, worker, provider-dispatch, IPC, and settings UI graph used by the ordinary application.

Packaged application identity SHALL reject development activation before reading developer-supplied trust inputs. A missing, malformed, purpose-mismatched, incompatible, or unauthenticated descriptor SHALL fail closed with `CATALOG_UNAVAILABLE`; it SHALL NOT be reported as an unsupported platform, select the fixture or production purpose, or fall back to another catalog. An active development channel SHALL be visibly labeled `Development qualification artifacts` in the settings status surface and SHALL never imply a Production support tier.

### 7.2 Whisper.cpp adapter

**MODEL-004** `whisperCpp` SHALL consume separately pinned `ggml` artifacts only. Other model formats are invalid Local Whisper artifacts.

The `whisperCpp` adapter SHALL map the validated domain contract to only reviewed worker options. Unsupported upstream flags SHALL not be accepted from persistence or IPC.

### 7.3 Worker boundary

**ARCH-005, RUN-001** `whisperCpp` SHALL run as a supervised sidecar, never as renderer code or a native Electron/Node addon. Worker process exit is the hard resource-release boundary after a graceful free attempt.

**RUN-002, SEC-005, PRIV-001** The supervisor SHALL:

- spawn an absolute, manifest-owned executable with `shell: false`, a fixed app-owned working directory, an allowlisted argument vector, and a sanitized environment;
- give the worker no TCP, UDP, Unix-domain, or named-pipe endpoint and expose no persistent or user-addressable local service; the one-use native filesystem-guard-to-launcher control channel in **SEC-011** is the only local-control exception, carries no inference payload, and closes before worker handshake;
- use versioned, request-ID-based, length-prefixed stdin/stdout frames with strict schemas;
- require a handshake containing protocol version, engine identity, runtime revision, backend capabilities, and maximum frame sizes before any model load;
- carry audio, prompt, managed model identity, and other private or user-specific inference values in bounded protocol frames, never argv or process titles; a model path is prohibited by **SEC-010**;
- reserve stdout for protocol frames and keep a sanitized, capped stderr ring buffer that is never copied verbatim into routine logs;
- reject oversized, malformed, duplicate, out-of-order, or unknown mandatory frames;
- bound spawn, handshake, probe, load, warm-up, transcription, unload, and termination stages;
- request graceful model release, then terminate and finally hard-kill the complete child tree if bounds expire;
- place the complete worker tree in a Windows Job Object with kill-on-job-close on Windows;
- start a dedicated Linux process group through a minimal reviewed launcher that sets parent-death signaling, rechecks the parent after setup, and terminates descendants on control-stream/parent death;
- bind every worker/lock to a random app-instance ownership nonce plus PID, OS process start identity, verified executable identity, and configuration epoch;
- never kill or adopt a process from PID alone; restart recovery must prove the full OS identity/nonce and otherwise treat a stale lock as non-authoritative;
- confirm child termination before reporting an uncertain allocation as released.

**RUN-005, FAIL-007** Initial non-user-editable upper bounds SHALL be 10 seconds for handshake, 30 seconds for the bounded backend probe, 5 minutes for full model load, 2 minutes for warm-up, 15 seconds for graceful unload, and two 5-second terminate/kill-confirmation stages. Local Whisper SHALL add its own inference deadline because the current shared batch flow has none: `max(120 seconds, 10 × validated audio duration)`, capped at 30 minutes. An uncached end-to-end transcription is bounded by the applicable spawn/probe/load/warm-up stages plus that inference deadline and cleanup bounds. Expiry cancels the request, terminates an uncertain child, returns `OPERATION_TIMEOUT` with the exact stage, and produces no partial success. Changing these constants requires qualification evidence and a specification revision.

Protocol JSON/control frames SHALL be at most 1 MiB, audio SHALL be chunked into bounded binary frames, and captured stderr SHALL be capped at 64 KiB per worker. Breaching a bound is a protocol failure and terminates the child.

**CAP-014, SEC-005, PRIV-001** A GPU ordinal by itself is not device authority or proof. Main SHALL create one process-local, non-persisted device authority bound to a random authority ID, configuration epoch, engine, exact runtime build, backend, topology generation, selected opaque device ID, ordered backend-native registry fingerprint, and bounded runtime-local GPU/IGPU ordinal. The authority and its native proof values SHALL remain private to main and the worker; they SHALL NOT enter renderer/preload/IPC contracts, settings, cache identity, routine logs, audits, diagnostics, errors, or process arguments.

Before and after every probe/load, main SHALL re-enumerate through the exact engine/runtime/backend registry algorithm and require the authority to resolve to the same selected physical device, fingerprint, and ordinal. A successful worker result SHALL be derived after backend activation and SHALL confirm the authority ID, registry fingerprint, actual activated ordinal, and an ephemeral authority-salted backend-native device proof. GPU load additionally SHALL prove a positive byte count of model-weight allocation owned by that exact activated device. Copying request values, matching only an ordinal, or merely retaining a CPU backend is not proof. Any changed authority, topology, order, device proof, activation, or GPU model-allocation result fails closed and terminates the worker without fallback. CPU authority SHALL contain no GPU ordinal/proof and SHALL prove that no GPU backend initialized.

**CAP-018, PRIV-006** Before any CUDA worker is installed, main SHALL obtain one bounded NVIDIA host inventory through a reviewed, shell-free, absolute-path adapter. The inventory SHALL bind each physical adapter's private PCI/native identity to compute capability, driver version, total VRAM, availability, and a per-install opaque renderer ID. Main MAY use only reviewed platform locations for the system-owned NVIDIA management interface; it SHALL NOT search `PATH`, invoke a shell, trust a device name or series label as capability evidence, or expose raw PCI IDs, UUIDs, driver output, command paths, or full hardware records through renderer/preload IPC, diagnostics, errors, logs, settings, or process arguments.

The pre-install inventory authorizes catalog filtering only. After installation, the existing worker registry, runtime-build binding, topology fingerprint, backend activation, allocation, and device-proof contract remain authoritative. Main SHALL reconcile both views to the same physical device and compute target before probe/load. Missing, malformed, stale, duplicated, reordered, unsupported, or contradictory inventory SHALL remove the CUDA setup action or produce a sanitized Not-ready reason; it SHALL NOT trigger a worker/runtime download, reuse another adapter, or select CPU. Refresh after driver, topology, suspend/resume, or external-GPU change SHALL preserve an existing stored selection as unavailable until the user explicitly saves another applicable choice.

**SEC-010, SEC-007, RUN-004** A model path string SHALL NOT authorize load. A full-load worker SHALL receive exactly one explicitly inherited, read-only model-root descriptor/handle duplicated from the still-active authenticated model lease, bound to private logical model-authority slot ID `3`, with every unrelated descriptor/handle closed. Linux SHALL realize logical slot `3` as actual file descriptor `3`. Windows SHALL bind logical slot `3` to one arbitrary inherited `HANDLE`; neither launcher nor worker may require, manufacture, or imply numeric Windows handle value `3`. The model root is the exact regular `ggml` model file. The worker SHALL NOT resolve an absolute path or an ambient working-directory path.

The worker SHALL load through its custom model-loader interface from that authority, verify the expected immutable artifact identity, installed byte count, and SHA-256 supplied by main, and report the same identity only after successful engine load. The model path SHALL be absent from private control frames and argv; it MAY remain sanitized main-owned display metadata only. The coordinator SHALL retain only the authenticated opaque lease token, while the native filesystem guard retains and revalidates the original OS descriptor/handle through load and residency. The worker closes its duplicate at the defined loader/exit boundary. Missing, writable, substituted, replayed, bound to the wrong logical slot, identity-mismatched, or unexpectedly duplicated authority fails before model parsing.

**SEC-011, RUN-007** Because the active OS authority is held inside the native filesystem guard rather than Electron, full-load creation SHALL use a one-use native handoff bound to the model lease, app ownership nonce, configuration epoch, launcher PID and OS start identity, expected worker slot, and expected model identity/digest. The existing filesystem guard and the operation-scoped platform launcher are the only helper roles in this handoff; “authority broker” describes their bounded handoff protocol, not a third resident process or service.

On Linux, the guard and launcher SHALL use one unnamed, preconnected `AF_UNIX` `SOCK_SEQPACKET | SOCK_CLOEXEC` socket pair with no filesystem or abstract address and no `bind`, `listen`, or `accept`. Both receivers SHALL enable `SO_PASSCRED` and use `recvmsg(..., MSG_CMSG_CLOEXEC)`. First, the launcher SHALL send one descriptor-free authenticated request carrying kernel-validated `SCM_CREDENTIALS`; the guard SHALL validate the expected launcher PID, UID/GID, OS process-start identity, ownership nonce, lease, configuration epoch, and artifact identity/digest before releasing authority. The guard SHALL then return exactly one transfer message whose same record contains its kernel-validated `SCM_CREDENTIALS` and exactly one `SCM_RIGHTS` descriptor; the launcher SHALL validate the expected guard PID/UID/GID/start identity and the same operation binding. `SO_PEERCRED` alone is not sufficient authentication for this pre-created socket pair. Each side SHALL reject truncation flags and missing, duplicate, extra, stale, replayed, or mismatched control records, credentials, or descriptors. The launcher SHALL reserve fd `3` before transfer or explicitly handle an authenticated received descriptor already numbered `3`; only after both directions validate may it collision-safely map the descriptor to actual worker fd `3` (or clear `FD_CLOEXEC` on that already-at-`3` descriptor), close every unrelated descriptor and both operation-scoped channel ends, and execute the worker. It SHALL never call `dup3(3, 3, ...)`.

On Windows, the guard SHALL own one owner-private, one-use native named-pipe control endpoint, authenticate the expected launcher PID/start identity and nonce, and duplicate exactly one read-only handle into that launcher with `DuplicateHandle`. The launcher SHALL validate the duplicate, create only the restricted inheritable copy needed by the worker, and stage a launcher-authenticated bootstrap record that binds its arbitrary numeric value to logical slot ID `3` and contains the operation nonce, lease, configuration epoch, and expected artifact identity/digest. That record and the worker acknowledgement SHALL remain private to native launch/control and SHALL never enter argv, renderer/preload IPC, settings, logs, audit, or diagnostics. The launcher SHALL use `STARTUPINFOEX` and `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` with `bInheritHandles=TRUE` so only protocol handles and that model handle are inherited. It SHALL create the worker suspended, assign it to a preconfigured kill-on-close Job Object, and fail closed on assignment or nested-job failure without breakaway flags. Only after successful assignment may it resume the worker, complete the private logical-slot bootstrap and receive the worker acknowledgement, close the one-use endpoint, and begin the ordinary worker handshake or any model parsing. Bootstrap failure SHALL terminate the Job-owned process tree. The endpoint ACL, name, handshake, and lifetime SHALL be bounded to that operation and unusable before ordinary handshake. Neither platform channel may carry inference data, expose an address to the renderer, accept a second peer, or remain as a worker/application listener.

`Check compatibility` SHALL create a probe-only worker with no model authority and terminate it after bounded probe evidence. `Load now` and eligible lazy load SHALL first acquire the exact model lease, then create a fresh full-load worker with slot `3`, perform handshake, repeat backend activation and authority proof in that same process, load and warm up, and retain that worker only after total success. An already-running probe worker SHALL NOT receive a model authority later. Linux transfer behavior SHALL have executable integration proof before native engine work; Windows source/contract tests may precede the first public-alpha Windows smoke gate.

**RUN-011, SEC-013** The pinned `whisperCpp` patch series SHALL harden the sequential custom loader with exact-read/optional-exact-read semantics, checked integer arithmetic, finite model-format limits, same-open-object size/hash enforcement, and typed load failures before allocation or copy. Zero bytes are valid only at a defined record boundary; every partial scalar, tensor header/name, or tensor body is invalid. The loader SHALL maintain a checked offset, reject reads beyond the held object, make close idempotent and non-throwing, and never accept or reconstruct a path. Native workers SHALL not search the current working directory or honor environment variables for backend libraries. Release workers SHALL use backend-specific linked dependencies or a manifest-owned trusted-directory loader that verifies every module before activation.

**CAP-017** GPU success SHALL prove the backend-native physical identity resolved inside the fresh full-load process, the actual activated backend/device, positive model-weight bytes owned by that device, and the primary execution/state backend. `whisperCpp` SHALL bind both model-buffer selection and state-backend creation to the same selected `ggml_backend_dev_t`; the selected device must own the model weights, be the primary state backend, and be the only GPU owning model buffers. A backend value such as `cuda`, an ordinal, context construction, or a retained CPU backend is insufficient. Raw UUID, PCI, LUID, registry, and allocation values remain private under CAP-014/PRIV-004. A backend instance that cannot expose a canonical durable native physical identity SHALL be shown as unavailable with `DEVICE_FEATURE_MISSING`; it SHALL NOT be persisted, loaded, or rebound from a description/index/memory tuple.

**RUN-012** The `whisperCpp` patch series SHALL install the reviewed abort callback on every execution backend that supports it, check cancellation before mel preparation, before and after each scheduled graph stage, and map cancellation separately from compute failure. Cooperative acknowledgement MAY preserve a demonstrably healthy resident `whisperCpp` worker; the supervisor SHALL still terminate an unresponsive worker because GPU kernels may not be preemptible. The worker may not emit a late success after cancellation becomes terminal.

**RUN-009** Every TypeScript and C++ control decoder SHALL enforce the same unreleased protocol-v1 resource grammar before schema mapping: at most 1,048,576 body bytes, depth 16, 4,096 parser values/events, 128 members per object, 256 elements per array, 128 UTF-8 bytes per key, and 262,144 UTF-8 bytes per decoded string, with stricter field caps taking precedence. Duplicate keys, invalid Unicode, negative zero, decimals/exponents, and non-canonical integer tokens are invalid; protocol numbers use canonical base-10 integer tokens and must fit JavaScript's safe-integer range before field validation. Because SAX value callbacks do not preserve every original numeric lexeme, each peer SHALL run a bounded project-owned lexical token validator before or alongside SAX/schema mapping; integer callbacks alone do not satisfy canonical-token validation.

Each peer SHALL have one framed-I/O owner that serializes writes and one inference owner for engine state. Control may request cooperative cancellation through the engine's reviewed callback, but SHALL NOT call unsafe engine APIs concurrently. Exactly one response may be consumed for a request; duplicate or post-terminal responses, frames received while asynchronous post-response revalidation is pending, or a changed worker/authority generation are protocol failures. Cleanup SHALL join inference ownership, release engine/model/backend state, close child model authorities, emit no further success, and then exit; the supervisor's process-tree termination remains the hard cancellation bound.

**AUDIO-001, RUN-006** One transcription SHALL carry one complete canonical WAV object through ordered audio frames. Its declared total SHALL be a safe integer from 46 through 57,600,044 bytes, inclusive, corresponding to one through 28,800,000 mono PCM16 samples at 16 kHz and at most 30 minutes. Main SHALL derive duration from the validated sample count rather than trust a caller-supplied duration, enforce the existing inference deadline from that value, and reject an invalid or over-limit declaration before reserving the full buffer. The worker SHALL independently enforce the same envelope, exact accumulated length, frame sequence, single terminal frame, and cancellation cleanup before inference.

**AUDIO-002** A maximum request may require 57,600,044 canonical WAV bytes and 115,200,000 float32 sample bytes. Project-owned complete-input/conversion storage SHALL therefore be checked before allocation, remain at or below 172,800,044 bytes, create no third complete audio copy or temporary file, and release both representations on every terminal path. Audio received after cancellation or a terminal frame SHALL be drained only within the transport bound and discarded without conversion or inference.

**RUN-003, FAIL-005** A crash, hang, protocol mismatch, load failure, or warm-up failure SHALL fail the current operation, discard partial output, invalidate operational readiness, clean up the child, and require a fresh explicit load or later lazy-load attempt. There is no transparent transcription replay, restart loop, or fallback.

### 7.4 Runtime prerequisites

**PKG-004, COMP-009** Runtime packs SHALL include only reviewed redistributable user-space dependencies and SHALL declare every system-owned prerequisite. The app SHALL NOT install GPU drivers, a full CUDA toolkit, system ROCm, device permissions, or elevated services.

**RUNTIME-005, DIST-003** Each Linux x64 and Windows x64 NVIDIA catalog SHALL publish exactly one immutable CUDA 12.8.1 runtime identity: the platform's `sm_120a-real` pack. Each pack SHALL contain native code for only `sm_120a` plus its reviewed platform dependency closure; it SHALL contain no PTX fallback or other real target. The Linux and Windows packs SHALL have independent build/pack revisions, archive names, manifests, signatures, provenance, SBOMs, notices, expected files, driver prerequisites, and support state. The Linux pack SHALL require NVIDIA driver `>= 570.26` and the Windows pack `>= 570.65`, matching the reviewed CUDA 12.8.1 product profiles. The active catalogs SHALL contain no `sm_86-real` or `sm_89-real` runtime identity. A user's installed CUDA toolkit version is not an input; the system NVIDIA driver is. A newer driver that supports CUDA 13 MAY run the selected bundled CUDA 12.8.1 runtime when it satisfies that pack's declared driver prerequisite.

- NVIDIA packs MAY include only license-approved CUDA/cuBLAS/cuDNN redistributables and required notices; the NVIDIA driver remains system-owned.
- HIP packs SHALL declare whether each reviewed user-space ROCm component is bundled and which exact system driver/ROCm/permission prerequisites remain external; they SHALL reject an unlisted mixture of bundled and ambient ROCm libraries.
- Vulkan packs SHALL use the installed vendor driver/ICD and SHALL reject software implementations. They SHALL NOT bundle a vendor GPU driver, Vulkan SDK, headers, or `glslc`; build-only shader tooling stays outside runtime packs.
- CPU packs SHALL declare required ISA features and fail validation rather than risk an illegal-instruction crash.
- Every worker SHALL load only manifest-owned runtime libraries. Current-working-directory discovery, user `PATH`, and backend-specific dynamic-loader environment overrides SHALL be ignored or removed before worker creation.

### 7.5 Non-normative implementation references

The following user-supplied references were inspected at immutable commits on
2026-08-01. They are evidence and implementation examples, not dependencies,
security proof, qualification evidence, or permission to weaken Sections
7.1–7.4:

- [OpenWhispr application at `bf8b7e0`](https://github.com/OpenWhispr/openwhispr/tree/bf8b7e0b4e1de0c9779c63f4752bd80bdd39ee2c): its state-owning persistent manager and backend-specific runtime selection illustrate retaining one loaded model; its loopback HTTP ports, model/private values in argv and multipart requests, temporary audio files, inherited environment, raw diagnostic logging, automatic GPU-to-CPU retry, mutable GitHub release lookup, PID files, and `taskkill` cleanup are explicitly not adopted.
- [OpenWhispr whisper.cpp fork at `dd18d11`](https://github.com/OpenWhispr/whisper.cpp/tree/dd18d1107cf20feb58f11b2719d66a5bfeaff0dc): `examples/server/server.cpp` illustrates serialized `whisper_context` initialization, inference, abort callback, replacement, and final `whisper_free`; `.github/workflows/build-binaries.yml` illustrates separate CPU/CUDA/Vulkan artifacts and companion-library packaging. The HTTP server, listener, upload surface, and published binaries are not reused as the worker boundary or accepted as release evidence.

For traceability, implementation packets pin the exact reviewed commits and
paths they use as background. A later reference revision is not treated as the
same evidence; its ideas or source require a fresh diff, provenance, license,
build, security, and compatibility review before they can affect a runtime
pack.

## 8. Provider Settings Screen

**UI-001, VAL-001** Local Whisper SHALL have a dedicated provider-settings form with the fields, actions, conditional visibility, and renderer/main validation below. A syntactically valid but unsupported or incomplete configuration may be saved and selected; it SHALL remain visibly Not ready and fail safely if load or transcription is invoked.

The form SHALL be organized into these scrollable sections:

1. status and current-device assessment;
2. runtime setup;
3. model and revision management;
4. basic inference settings;
5. collapsed `Advanced` inference settings;
6. managed storage and licenses.

Every disabled action SHALL expose a perceivable reason. Errors SHALL be associated with the relevant field, announced to assistive technology, and summarized near the action area. Progress SHALL include text/status semantics rather than color alone.

### 8.1 Settings fields

| Field             | Type and allowed values                                                                                        | Default                                                                                                                  | Visibility                                                                                                            | Normative behavior                                                                                                                                                                                                     |
| ----------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Engine            | Fixed literal `whisperCpp`                                                                                     | `whisperCpp`                                                                                                             | Read-only compatibility field; no selector                                                                            | Required and persisted. Any other value is invalid. The discriminator is retained for forward-compatible contracts but cannot be changed in release 1.                                                                 |
| Execution target  | `gpu` or `cpu`                                                                                                 | `gpu`                                                                                                                    | Always                                                                                                                | Required; there is no `auto`. No target fallback is allowed. A change unloads and invalidates capability.                                                                                                              |
| Backend           | `cuda`, `hip`, `vulkan`, `metal`, or `cpu`                                                                     | See Section 8.2                                                                                                          | Selector for supported GPU paths; read-only `CPU` for CPU; read-only disabled `Metal (Planned)` in the macOS skeleton | `cpu` is valid only with target CPU. `metal` is a typed macOS-only unavailable skeleton value. Other GPU backends are invalid with target CPU. A failure never changes the persisted backend.                          |
| Device            | Main-issued opaque stable ID plus sanitized display name                                                       | See Section 8.2                                                                                                          | GPU selector; read-only host CPU summary for CPU                                                                      | A GPU device is required for load/transcription. Arbitrary IDs are rejected. A disappeared device remains selected and unavailable; no other device is selected automatically.                                         |
| Runtime revision  | Immutable hardware-applicable catalog revision                                                                 | The sole app-pinned applicable `recommendedRevision` for a new `(engine, target, backend, compute target)` selection key | Runtime setup; one matching revision only                                                                             | Existing selections never advance automatically. A missing/incompatible selection remains `Runtime missing`/`Runtime incompatible`. A change unloads and invalidates capability.                                       |
| Model family      | `tiny`, `base`, `small`, `medium`, `large-v3`, or `large-v3-turbo`                                             | `base`                                                                                                                   | Always                                                                                                                | Required. Every option exposes the approximate family RAM/VRAM guidance from Section 8.1.1 before selection. A change unloads and invalidates capability.                                                              |
| Model revision    | Immutable catalog revision for the selected engine/family                                                      | Catalog `recommendedRevision` for a new `(engine, model family)` selection key                                           | Always                                                                                                                | Existing selections never advance automatically. A selected absent revision remains selected as `Model missing`.                                                                                                       |
| Model variant     | Reviewed variant from the selected artifact entry                                                              | `full` when available; otherwise the sole variant                                                                        | Only when more than one reviewed variant exists                                                                       | A reviewed `q5_0` variant MAY appear only when its exact manifest and qualification exist; only `large-v3` and `large-v3-turbo` are candidates in release 1. A change unloads.                                         |
| Language          | `auto` or a canonical ID from the app-shipped Whisper.cpp language catalog                                     | `auto`                                                                                                                   | Always                                                                                                                | Required. No free text or localized label is persisted. Only IDs with a reviewed `whisperCpp` mapping are allowed; unknown aliases are invalid.                                                                        |
| Initial prompt    | Unicode text                                                                                                   | Empty                                                                                                                    | Always                                                                                                                | Optional; maximum 1,000 Unicode code points. NUL, invalid Unicode scalar sequences, and longer values are rejected. No silent truncation, trimming, or normalization is permitted. A live counter SHALL be shown.      |
| Temperature       | UI decimal `0.00..1.00`; canonical persisted/IPC integer `temperatureHundredths` in `0..100`, divisible by `5` | `0` (`0.00` in UI)                                                                                                       | Always                                                                                                                | Non-safe-integers, out-of-range, and off-grid values are invalid. Locale parsing remains renderer-only; main receives the canonical integer. No temperature fallback list is exposed.                                  |
| Decoding strategy | `greedy`, `beamSearch`, or `bestOfSampling`                                                                    | `greedy`                                                                                                                 | Advanced                                                                                                              | Only the mappings in Section 8.4 are valid.                                                                                                                                                                            |
| Beam size         | Safe integer `1..10`                                                                                           | `5`                                                                                                                      | Advanced, only for `beamSearch`                                                                                       | Required when visible. Fractional and out-of-range numeric values are invalid. It is absent from other worker requests.                                                                                                |
| Best of           | Safe integer `1..10`                                                                                           | `5`                                                                                                                      | Advanced, only for `bestOfSampling`                                                                                   | Required when visible. Fractional and out-of-range numeric values are invalid. It is absent from other worker requests.                                                                                                |
| CPU threads       | `auto` or integer `1..detectedLogicalProcessors`                                                               | `auto`                                                                                                                   | Advanced, CPU target only                                                                                             | The current main-process detected upper bound is authoritative. `auto` resolves at load time. The value is never applied to a GPU worker. A change invalidates the capability proof and requires reload/warm-up.       |
| Model storage     | Sanitized platform label/app-relative location and aggregate/per-artifact disk usage                           | Fixed per-user data root                                                                                                 | Model management                                                                                                      | The renderer never receives the absolute path or username. An optional `Open storage folder` action is performed entirely by trusted main. No arbitrary path, custom directory, import, or symlink target is accepted. |

**SET-005, VAL-002** These defaults are applied only when no Local Whisper settings have ever been stored for the relevant profile. Missing or corrupt fields in an existing selection SHALL produce validation/repair state; they SHALL not silently switch a previously selected engine, target, backend, device, model revision, or runtime revision.

### 8.1.1 Approximate model memory guidance

**MODEL-010, UI-007** Before a user selects, downloads, validates, or loads a model, the model-family control SHALL expose the following release-1 capacity guidance for all six curated multilingual families. The values are deliberately approximate, rounded ranges for advance planning rather than exact allocation limits.

| Model family     | Approximate GPU VRAM capacity | Approximate total system RAM | Guidance note                                                                     |
| ---------------- | ----------------------------- | ---------------------------- | --------------------------------------------------------------------------------- |
| `tiny`           | approximately 1–2 GiB         | approximately 2–4 GiB        | Lowest resource use; appropriate for resource-constrained supported devices.      |
| `base`           | approximately 1–2 GiB         | approximately 2–4 GiB        | Default model; similar capacity class to `tiny` with higher model cost.           |
| `small`          | approximately 2–3 GiB         | approximately 4–6 GiB        | Mid-range option with a material increase over `base`.                            |
| `medium`         | approximately 3–6 GiB         | approximately 6–10 GiB       | High-memory option; exact backend and model variant materially affect the result. |
| `large-v3`       | approximately 6–8 GiB         | approximately 10–16 GiB      | Highest release-1 memory class; qualification may publish a narrower figure.      |
| `large-v3-turbo` | approximately 3–6 GiB         | approximately 6–10 GiB       | Reduced large-model class; not assumed equal to `large-v3`.                       |

VRAM guidance applies only to a GPU target; CPU execution does not allocate model VRAM. RAM guidance is total installed system-capacity guidance, not a claim that the model itself owns the entire amount. The ranges conservatively combine pinned `whisper.cpp` model and representative memory evidence, runtime and host overhead, model-variant variation, and operating-system/application headroom. They SHALL be labeled `Approximate requirements` and SHALL NOT be presented as a guarantee, a qualified peak, or a substitute for current free-memory validation.

After engine, target, backend, model revision, and variant are known, the screen SHALL replace or supplement the family range with the narrower matching catalog estimate and identify it as `Estimated for selected configuration`. When a matching qualified measurement exists, it SHALL also show `Qualified peak` separately; neither value may silently rewrite the family guidance or persisted selection.

The family table itself SHALL NOT block selection, download, or installation. Current-device preflight and `Load now` use the exact selected-configuration rules in Sections 9.2 and 11. A device below the family guidance MAY still be tested when the exact catalog threshold and resource probe permit it; a device inside or above the range may still fail a real allocation or load.

### 8.2 Initial backend and device selection

**SET-004, CAP-010, SET-007, VAL-003, VAL-004** Dependent selections SHALL be persisted by these stable keys:

- runtime revision by `(engine, target, backend, compute target)`, where CPU uses its declared ISA target and current CUDA support uses only the selected device's main-derived `sm_120a` target;
- selected backend by `(engine, target)`;
- selected device by `(engine, backend)`;
- model family by engine;
- model revision and variant by `(engine, model family)`;
- CPU threads by engine;
- shared request controls independently of engine.

Switching a parent field SHALL restore the last explicitly saved compatible child values for the resulting key, including a currently missing or unavailable device/artifact. If the key has never existed, only these deterministic initialization rules apply:

- the fixed engine starts with model family `base` and its catalog `recommendedRevision`/default variant;
- a new target starts with its target-specific thread default where applicable;
- a new engine/target/backend/compute-target key starts with its sole hardware-applicable app-pinned runtime `recommendedRevision`;
- a new model family starts with its engine/family `recommendedRevision` and `full` variant when available;
- no catalog update re-runs initialization for an existing key.

Backend and device remain explicit persisted values. For a new, never-configured GPU selection key:

- if no catalog-eligible physical GPU/backend combination is detected, backend/device remain unset and the configuration is Not ready;
- if exactly one catalog-eligible physical GPU/backend combination is detected, it SHALL be selected initially;
- if more than one eligible combination exists, backend/device remain unset until the user chooses;
- CUDA is the eligible NVIDIA route;
- Windows AMD exposes Vulkan;
- exact-allowlisted Linux AMD exposes HIP first in the list and Vulkan as a separate explicit alternative;
- software Vulkan devices, Intel GPUs, unknown vendors, and unsupported adapters are not eligible defaults.

This one-time initialization is not fallback authority. A driver change, device disappearance, backend failure, or later discovery SHALL preserve the persisted value and make it Not ready until the user explicitly changes it.

A versioned migration SHALL preserve every valid existing CPU or `sm_120a` runtime selection under its expanded key. If the saved revision cannot be proven applicable to the selected current device, migration SHALL retain it as selected-but-unavailable and require an explicit user save; it SHALL NOT rewrite it to `sm_120a`, CPU, another backend, or another device. Any unexpected `sm_86`, `sm_89`, or other out-of-scope saved target SHALL remain unavailable and SHALL NOT authorize a catalog action, download, launch, or fallback. Fresh profiles and genuinely new compute-target keys MAY initialize only the sole current hardware-applicable recommended revision.

Deliberately changing a parent can therefore initialize previously unseen children, but a runtime failure never does. Returning to an earlier key restores its prior choices rather than selecting current catalog recommendations.

### 8.3 Whisper.cpp language catalog

**SET-008, COMP-011** The app release SHALL pin a `languageCatalogRevision` containing `auto` plus the reviewed canonical Whisper language IDs supported by the `whisperCpp` adapter. Each entry SHALL have one stable persisted ID, localized display metadata, and an explicit worker mapping. Locale-specific variants or aliases are unavailable unless the catalog defines an unambiguous mapping. Automated tests SHALL enumerate every entry through the adapter; an incomplete mapping removes the ID from the catalog rather than deferring behavior to the worker.

### 8.4 Decoding cross-field rules

| Strategy         | Temperature                                                       | Active control                | Worker contract                                                                |
| ---------------- | ----------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------ |
| `greedy`         | `temperatureHundredths = 0` (`0.00`)                              | Neither beam size nor best-of | One deterministic candidate; inactive controls are omitted.                    |
| `beamSearch`     | `temperatureHundredths = 0` (`0.00`)                              | Beam size `1..10`             | Beam search with the submitted size; best-of is omitted.                       |
| `bestOfSampling` | `temperatureHundredths = 5..100`, divisible by `5` (`0.05..1.00`) | Best of `1..10`               | Temperature sampling with the submitted candidate count; beam size is omitted. |

The UI SHALL show a cross-field error instead of silently changing temperature, strategy, beam size, or best-of. Hidden UI values MAY be retained for convenience, but they SHALL be absent from the active normalized settings and worker request.

### 8.5 Validation and change semantics

The renderer SHALL provide immediate feedback, but main SHALL be the only validation authority.

1. Unknown enum/catalog values, malformed unions, forged device IDs, non-finite numbers, invalid integers, invalid Unicode, and cross-field-invalid settings SHALL be rejected without partial persistence.
2. Engine, runtime revision, target, backend, device, model identity, variant, and CPU threads are load-affecting. Changing one SHALL first unload/terminate the resident worker, increment the configuration epoch, and invalidate current operational readiness (**LIFE-004**).
3. Language, prompt, temperature, strategy, beam size, and best-of are request-affecting. They apply to the next transcription without unloading, but every request captures one immutable validated settings epoch.
4. The `whisperCpp` adapter MAY declare an otherwise request-affecting field load-affecting only through an app-shipped capability manifest and tests; the UI/state snapshot SHALL then expose that behavior.
5. While transcription or a conflicting lifecycle operation is active, a load-affecting save SHALL fail with `OPERATION_CONFLICT`. It SHALL not be queued or applied partially.
6. Syntactically valid but unsupported combinations MAY be saved. They remain visibly Not ready and cannot pass load/transcription validation.
7. A missing runtime/model MAY remain selected so the setup action can target the exact required artifact.
8. Download completion alone never produces readiness.
9. Settings snapshots SHALL not perform probe/load/download side effects.

### 8.6 Initial-prompt storage

**SET-006, PRIV-002** The initial prompt SHALL persist in the versioned Local Whisper settings file because it is a provider setting, but it SHALL be treated as private local text:

- never log, audit, export, or include its value in crash diagnostics;
- never place it in argv, process title, URL, download metadata, or filesystem name;
- send it to the selected worker only through the private framed protocol for a transcription;
- clear it when the user resets Local Whisper settings;
- do not claim encryption unless the repository adds and verifies such a contract.

**SET-009** A renderer save request SHALL contain all public setting fields plus expected configuration/inventory epochs and exactly one prompt mutation: `unchanged`, `clear`, or `replace` with a newly validated prompt. Main SHALL merge `unchanged` with the private persisted value, validate the complete candidate once, perform any required unload once, and atomically commit or reject the save. Main SHALL return prompt presence only. A stale epoch, invalid replacement, or unload conflict leaves both settings and prompt unchanged.

### 8.7 Settings actions and status surface

The screen SHALL expose:

- `Check compatibility`: performs a non-resident platform/device/prerequisite/resource preflight and returns at most `EstimateOnly`; it never downloads or leaves a model allocation;
- `Download runtime`, `Resume`, `Cancel`, and `Retry` for the exact selected revision, plus per-installed-revision `Remove runtime` actions;
- per-model-revision `Download`, `Resume`, `Cancel`, `Retry`, and `Delete` actions;
- `Load now`: runs the same full load/warm-up contract used by lazy loading;
- `Unload`: releases the current worker allocation;
- the complete approximate six-family RAM/VRAM guidance before model selection, plus selected-configuration estimated and qualified RAM/VRAM;
- installed/download/expanded sizes, license/provenance link, support tier, setup state, capability state, residency state, last validation time, and safe failure/recovery guidance.

**UI-010, COMP-013** Main SHALL project only hardware-applicable runtime and model setup actions into the renderer snapshot. For a selected NVIDIA adapter, the runtime selector SHALL contain at most one downloadable CUDA revision for that adapter's exact compute target; other platform and compute-target packs SHALL not cross preload IPC and SHALL not appear as disabled installation choices. CPU MAY remain a separate user-selected target only when its platform, ISA, and stable capacity requirements apply. Model download/install actions SHALL be offered only when catalog compatibility and stable total RAM/VRAM/disk capacity permit the selected target; an inapplicable model MAY appear only as bounded explanatory guidance without an action.

Free RAM, free VRAM, and immediately available disk are transient. A transient shortage SHALL preserve the otherwise applicable option, disable the affected action with a sanitized requirement/recovery status, and be rechecked before download, compatibility validation, and load. It SHALL never cause automatic reselection, disappearance of a saved selection, fallback, or download. When no CUDA pack is applicable, the screen SHALL show one safe reason such as unsupported GPU, incompatible NVIDIA driver, unavailable hardware inventory, or insufficient stable capacity rather than exposing unrelated packs.

Known Planned or permanently Unsupported actions SHALL be disabled with an explanation, while main still rejects forged IPC. Recoverable Not-ready states SHALL surface the relevant setup/retry action. Selecting Local Whisper as the active provider remains permitted; an attempted transcription then returns the typed state-specific failure rather than switching providers.

When platform/architecture support exists but no trusted catalog is active, the screen SHALL show `Catalog unavailable` with publication or development-activation guidance instead of the words `Unsupported platform`. An authenticated development activation SHALL show its non-production label while retaining the normal model, runtime, compatibility, load, unload, and transcription controls.

`Load now` is the definitive user-facing “can this exact configuration run?” action: it performs full load and warm-up and intentionally leaves the model resident. Lazy first transcription runs the same proof. The status surface SHALL distinguish `Estimate only`, `Validated · Unloaded`, and currently operational `Ready`.

### 8.8 Main-screen Load/Free model control

**UI-009, LIFE-007, FAIL-004** The main toolbar SHALL reserve one stable, provider-specific control while Local Whisper is the committed active provider and SHALL omit it for every other active provider. Its visible state SHALL be derived only from the latest authoritative main-window status:

| Authoritative Local Whisper state                                                                                           | Control presentation and behavior                                                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status not yet received                                                                                                     | Disabled pending indicator with a perceivable `Loading Local Whisper status` reason.                                                                                                                                            |
| `Unloaded`, runtime and model both `Installed`, `canAttempt = true`, no blocking code, and the selected option is available | Enabled `Load model`. This includes eligible `Unchecked`, `EstimateOnly`/available-on-demand, `ValidatedUnloaded`, and retryable prior-failure states; invoking it runs the full coordinator preflight, load, and warm-up path. |
| Any other `Unloaded` state                                                                                                  | Disabled `Load model` with the current sanitized setup, selection, support, or readiness reason and the relevant settings recovery path; it SHALL NOT start compatibility checking or an artifact download implicitly.          |
| `Loading`                                                                                                                   | Disabled pending indicator announced as `Loading model`; it is not a cancellation control.                                                                                                                                      |
| `Loaded` plus `Idle`                                                                                                        | Enabled `Free model`; invoking it performs the contract action `unload`.                                                                                                                                                        |
| `Loaded` plus `Transcribing`                                                                                                | Disabled `Free model` with a perceivable model-in-use reason; transcription cancellation remains a separate existing operation.                                                                                                 |
| `Unloading`                                                                                                                 | Disabled pending indicator announced as `Freeing model`.                                                                                                                                                                        |
| Transient `Failed`                                                                                                          | Disabled control with the authoritative sanitized failure; after the following authoritative `Unloaded` snapshot, presentation is derived again from the Unloaded predicates above.                                             |

The UI SHALL use the user-facing labels `Load model` and `Free model`; the typed privileged actions remain `load` and `unload`. The icon-only visual precedent does not remove text semantics: the control SHALL provide localized accessible name, title/tooltip, keyboard activation, focus-visible behavior, non-color pending/failure status, reduced-motion-compatible progress, and a focusable/perceivable disabled reason. Ongoing state SHALL use status semantics; a new command failure SHALL be announced once as an alert. Only sanitized failure labels/recovery guidance may appear. A preload transport exception SHALL become one generic localized operation-failed message and SHALL never expose its raw message or stack.

The renderer SHALL NOT optimistically change residency. It may disable the control for its own single outstanding invocation, but the returned result and subscription are reconciled by `snapshotRevision`, and a higher revision always wins. Invoke settlement, rejection, renderer reload, replacement, or subscription replay SHALL clear stale renderer-local pending state and reconstruct the control from process-owned status without cancelling or duplicating the coordinator operation.

While a main-screen `load` or `unload` command is pending, any provider-switch request SHALL fail immediately with sanitized `OPERATION_CONFLICT`, SHALL leave Local Whisper committed and the control visible in its pending state, and SHALL neither cancel nor queue the switch. After the lifecycle operation settles, switching requires a new explicit user attempt and follows the existing provider-change unload contract. Settings saves, transcription, a second lifecycle click, and provider switching continue to share the coordinator's existing conflict arbitration; no rejected race may have a partial effect or trigger fallback.

## 9. Canonical Identities and Fingerprints

### 9.1 Runtime pack

Every runtime pack SHALL be immutable and identified by at least:

- app compatibility/catalog revision;
- engine ID and exact upstream repository, commit, Git tree, canonical source-manifest, patch-series, patched-tree, and build-input revisions;
- OS and architecture;
- target/backend and dependency family;
- compiled compute capabilities or `gfx` targets where relevant;
- worker protocol version;
- pack revision and signing key ID;
- archive byte size, SHA-256, signature, and allowlisted origin;
- exact expected file list, types, modes, sizes, and SHA-256 hashes;
- external driver/runtime/permission prerequisites;
- build provenance, SBOM/component inventory, and license notices.

Source-object transport size/SHA-256, canonical path/type/mode/content-manifest SHA-256, import tool versions, signature result where available, and every patch digest/order SHALL be recorded in protected build provenance. A transport checksum is additive evidence and SHALL NOT replace the commit, Git tree, or canonical extracted-content identity.

Packs for different backends, platforms, app/protocol revisions, or accelerator targets SHALL never overwrite one another.

**DIST-003, CAP-018** A CUDA catalog entry SHALL additionally declare exactly one normalized compute target, minimum driver prerequisite, applicable device-capability range, stable total-VRAM floor where one exists, and an immutable applicability-policy revision. Catalog authentication SHALL cover these fields. Main SHALL join them with its private host inventory and expose only the applicable artifact identity/action; the renderer SHALL never receive the full cross-platform/cross-target runtime catalog or private hardware evidence. Duplicate matching packs, zero matching packs for a supposedly available selection, ambiguous target ranges, missing driver/capacity metadata, or a catalog/runtime target disagreement fail closed before transfer.

### 9.2 Model artifact

**MODEL-003, MODEL-009** A selectable model artifact SHALL be the immutable tuple:

```text
engine + logical model + source checkpoint revision
+ artifact revision + native format + variant
```

Its catalog entry SHALL also contain exact expected files/sizes/hashes, download and installed bytes, license/provenance, runtime/protocol compatibility, estimated RAM/VRAM, qualification status, and empirically measured peak memory where available.

**CAP-013** Each selectable artifact SHALL contain a closed memory-estimate matrix covering every catalog-exposed target/backend/runtime/variant combination. Each record SHALL bind to that exact configuration identity and contain non-negative safe-integer `estimatedPeakRamBytes`, `estimatedPeakVramBytes` or an explicit CPU `notApplicable` value, an evidence basis (`upstream`, `derived`, or `qualified`), the source/build revision, and a short renderer-safe methodology label. A qualified measurement SHALL remain a separate value tied to its exact qualification profile and fingerprint.

A missing record, duplicate key, unsafe number, unit ambiguity, GPU record without VRAM, CPU record with VRAM, identity mismatch, or estimate from another runtime/model/variant/backend revision makes the relevant catalog configuration invalid. Such a record SHALL NOT be reused as a load gate or displayed as selected-configuration evidence. The UI SHALL fall back only to the Section 8.1.1 family range with `Exact estimate unavailable`; production/Preview catalog publication SHALL fail verification until every exposed configuration has a valid matching record.

Full and reviewed quantized `ggml` files are distinct variants. **MODEL-011, QUAL-003** The release-1 catalog matrix is closed to `tiny/full`, `base/full`, `small/full`, `medium/full`, `large-v3/q5_0`, and `large-v3-turbo/q5_0`, all sourced from immutable commit `5359861c739e955e79d9a303bcbc70fb988958b1` of public Hugging Face repository `ggerganov/whisper.cpp`. A different upstream repository/commit/file or an additional variant requires a reviewed specification/catalog revision and its own validation evidence.

The initial upstream byte identities are fixed below; each SHA-256 is the Hugging Face Git-LFS `sha256` object ID and SHALL be independently recomputed over the completed download before catalog admission and promotion:

| Family / variant      | Upstream file                  |   Exact bytes | SHA-256                                                            |
| --------------------- | ------------------------------ | ------------: | ------------------------------------------------------------------ |
| `tiny/full`           | `ggml-tiny.bin`                |    77,691,713 | `be07e048e1e599ad46341c8d2a135645097a538221678b7acdd1b1919c6e1b21` |
| `base/full`           | `ggml-base.bin`                |   147,951,465 | `60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe` |
| `small/full`          | `ggml-small.bin`               |   487,601,967 | `1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b` |
| `medium/full`         | `ggml-medium.bin`              | 1,533,763,059 | `6c14d5adee5f86394037b4e4e8b59f1673b6cee10e3cf0b11bbdbee79c156208` |
| `large-v3/q5_0`       | `ggml-large-v3-q5_0.bin`       | 1,081,140,203 | `d75795ecff3f83b5faa89d1900604ad8c780abd5739fae406de19f23ecd98ad1` |
| `large-v3-turbo/q5_0` | `ggml-large-v3-turbo-q5_0.bin` |   574,041,195 | `394221709cd5ad1f40c46e6031ca61bce88931e6e088c188294c6d5a55ffa7e2` |

GPT-Voice SHALL NOT convert, quantize, or republish those release-1 model bytes. The desktop app downloads and verifies the finished upstream native file directly. If a future release creates a project-specific conversion or quantization, that new artifact SHALL be produced only by reviewed project automation with its own provenance, license, identity, and qualification; conversion on the user's device remains prohibited.

File/download size is not a memory guarantee. **CAP-003** Catalog estimates are advisory until the exact model passes real allocation, full load, and warm-up. The application SHALL never derive RAM or VRAM requirements from artifact byte size at runtime.

### 9.3 Residency key

The single process residency key SHALL contain:

- engine and runtime-pack revision;
- target, backend, and opaque device ID;
- model artifact tuple;
- resolved CPU thread count for CPU residency.

Changing any key field requires unload before another key can become resident. Only one key may be loaded per application process.

### 9.4 Capability fingerprint

The exact capability fingerprint SHALL contain:

- OS build/family and architecture;
- app/catalog and worker protocol revisions;
- engine/runtime pack and verified file identity;
- target/backend/device;
- relevant driver, runtime, CPU ISA, and topology identifiers;
- model artifact tuple and verified file identity;
- load-affecting settings and resolved values.

Raw serial numbers and hardware UUIDs SHALL not be included. Driver change, device topology change, suspend/resume, external-GPU change, artifact modification, catalog denylisting, or any fingerprint setting change marks prior capability evidence `Stale`.

**CAP-015, PRIV-004** Stable renderer/settings device IDs SHALL be derived only in main by HMAC-SHA-256 over a versioned canonical private physical-device identity using a random 256-bit per-install salt from a dedicated owner-private device-identity repository. The persisted value is a bounded opaque encoding; raw UUIDs, serials, PCI topology/location, native registry records, and the HMAC input SHALL never be persisted in Local Whisper settings or exposed across preload/renderer, audit, routine diagnostics, logs, errors, cache identity, or process arguments. The repository SHALL version its derivation, reject collisions, and fail closed. If its salt/version is lost or reset, prior opaque selections become unavailable and SHALL NOT be silently rebound to another device.

The stable opaque ID is selection metadata, not worker authority. Every probe/load still requires the process-local CAP-014 authority built from fresh backend-native enumeration. Capability services SHALL return bounded evaluation/proof evidence only; **CAP-016** the coordinator alone owns configuration/inventory epochs and may commit `Validated`, `Loaded`, or `Ready` after the complete full-load transaction succeeds.

### 9.5 Transcription cache context

**CACHE-001** A successful Local Whisper cache key SHALL include the audio identity plus every output-affecting value: provider, engine, runtime/protocol revision, target/backend/device class, model artifact tuple, language, prompt, temperature, strategy, beam/best-of, CPU threads when output-affecting, and adapter mapping revision. Raw prompt text SHALL participate only through private in-memory comparison or a non-exported canonical digest; it SHALL not be serialized into a diagnostic/log-visible cache identifier. No cache result may cross a materially different configuration.

### 9.6 Candidate, package, and evidence identity

**REL-001, QUAL-001** Release identity SHALL be one directed acyclic graph with the following strict construction order. Every digest is lowercase SHA-256 over UTF-8 canonical JSON after removing exactly that document's own digest field. Canonical JSON recursively orders object keys lexicographically, preserves only schema-defined array order, contains no insignificant whitespace or byte-order mark, and rejects unknown fields, duplicate semantic identities, non-finite numbers, and values outside the schema before hashing. Every digest-bearing array whose order has no domain meaning SHALL be sorted lexicographically by its declared stable identity before hashing.

1. The **release-generation input** is frozen first as `candidateInputDigest`. It binds one explicitly supplied SemVer and UTC freeze timestamp; exact Git source commit/tree and clean-state proof; shared engine/source/patch locks; catalog, trust-purpose, transfer-profile, redirect-policy, evidence-schema, and release-policy revisions; the exact six public model objects; Task 17 fixture digest; required Linux/Windows CPU and RTX 50 output matrix; and predecessor-selection rule. It contains no release candidate, deployment, platform-smoke, alpha-aggregate, or final-lineage digest.
2. The **signed candidate set** is frozen next as `releaseCandidateDigest`. It binds `candidateInputDigest`, the complete six-output application/runtime set, exact filenames/roles/platforms/targets/lengths/SHA-256 values, native and artifact signatures, signed release manifest, catalogs, toolchain and reproducibility records, SBOMs, notices, provenance, compatibility metadata, and expected same-tag GitHub Release origins. It contains no tag, public release, platform-smoke, alpha-aggregate, or final-lineage digest.
3. The **release staging record** is sealed as `releaseStagingDigest` after the preserving merge, immutable tag, non-clobbering GitHub Release staging, and final-origin byte/signature/catalog verification, while the release remains non-installable. It binds `candidateInputDigest`, `releaseCandidateDigest`, exact source/tag/release identities, staged asset records, intended prerelease/stable state, and every separately authorized signing/legal/staging approval. It contains no physical platform-test result.
4. For an alpha, the **deployment record** is sealed as `deploymentDigest` only after explicit publication approval makes the verified staging public with the prerelease marker. It binds `releaseStagingDigest`, public release state, asset records, and publication authorization. Failed or incomplete deployment remains non-installable and cannot produce this digest.
5. Linux x64 and Windows x64 then independently freeze one **platform-smoke input** for that public alpha. Each `platformSmokeInputDigest` binds the alpha `deploymentDigest`, exact public same-tag installer/runtime assets, pinned public `base/full` model object, one non-private deterministic audio fixture, platform/architecture, representative CPU and RTX 50 host classes, driver/runtime/device-proof class, smoke algorithm, timeout/cleanup bounds, and privacy-safe tool identities. It contains no result, other-platform, or aggregate digest.
6. Each alpha platform test seals one **platform-smoke result** and evidence index against its own input. Passing requires the complete Section 19.2 CPU and RTX 50 flow; failure is a truthful immutable result. Linux evidence cannot satisfy Windows and Windows evidence cannot satisfy Linux. Public evidence excludes raw paths, device identifiers, audio, transcripts, prompts, environment, keys, and private logs.
7. The **alpha feedback aggregate** is sealed as `alphaAggregateDigest` only after both platform results exist. It binds the alpha `deploymentDigest`, exact Linux and Windows smoke-input/result/evidence digests, acceptance ownership, and the transition state. Final selection is allowed only when both results pass and no accepted fix is absent from that alpha; either failure permits only the next sequential alpha path. Platform records never hash the later aggregate.
8. For final, the **final lineage record** is sealed as `finalLineageDigest` after final staging and before stable publication, without a final physical-test branch. It binds the latest passing `alphaAggregateDigest`; an allowlisted source-delta manifest proving that only final SemVer, changelog, tag-derived preparation, and required release metadata changed; the fresh final `candidateInputDigest`, `releaseCandidateDigest`, `releaseStagingDigest`, deterministic automated checks, independent clean-build reproducibility records, and signing/legal approvals. It rejects every product/runtime/packaging/compatibility change, missing alpha pass, candidate substitution, or unbound staged asset.
9. The final **deployment record** is sealed as `deploymentDigest` only after stable publication. It binds `finalLineageDigest`, the exact unchanged `releaseStagingDigest`, public stable state, final asset records, and publication authorization. It contains no physical platform-test branch.

The final source-delta allowlist SHALL be a closed checked-in schema frozen by the latest alpha's `candidateInputDigest`. It may admit only canonical SemVer, changelog, tag-derived catalog/manifest/release-note preparation, and non-executable release metadata whose values are deterministic functions of the final identity. It SHALL NOT be expanded after alpha smoke, and it rejects any dependency, build-policy, runtime, application, packaging-behavior, compatibility, model, protocol, setting, trust-policy, or origin change.

A generation/input change invalidates every downstream record for that release. Changing one alpha platform-smoke input or result invalidates the alpha aggregate but never rewrites the public alpha, deployment record, or unchanged other-platform evidence. A final-only delta or reproducibility mismatch invalidates final lineage and blocks stable publication; it never mutates an alpha.

The candidate SemVer and UTC freeze timestamp SHALL be explicit; the worktree `package.json` value and an inferred “next version” are not authority. The platform predecessor SHALL be the highest non-draft, non-prerelease stable version published before that shared timestamp. As of this revision the latest stable project release is `v2.3.0`; if no later stable release exists at freeze, Linux downgrade evidence SHALL use `GPT-Voice-2.3.0.AppImage` with release-reported SHA-256 `80674b3a90222b51981fb43b5b757b7af9d3e38a5ff4ca41554ab965ae29f111`, and Windows downgrade evidence SHALL use `GPT-Voice.Setup.2.3.0.exe` with release-reported SHA-256 `0e2aa1ea97ba357db6d35f53debd01ca1c6124ae10b9f537b2af4427a0328cd0`. A later stable release automatically becomes the predecessor only when published before the shared freeze timestamp and must be recorded with independently verified project checksum evidence.

**REL-003, REL-004, QUAL-007** Every alpha and final release is a distinct immutable candidate generation. For each positive canonical decimal `N`, the alpha graph binds exact SemVer `2.4.0-alpha.N`, tag `v2.4.0-alpha.N`, its dedicated release-branch head, and its own UTC freeze. Alpha numbering starts at 1 and every later published alpha increments the latest number by exactly one without gaps. The final graph binds exact SemVer/tag `2.4.0` / `v2.4.0`, a later dedicated release-branch head, and a new UTC freeze. No earlier candidate, signature, catalog, manifest, or release asset may satisfy a later release edge. Latest-alpha smoke evidence may authorize final lineage only through the explicit `alphaAggregateDigest`; it never substitutes for fresh final candidate bytes.

The unfrozen, uncommitted qualification schema-v2 family SHALL be replaced in place by the release-evidence schema above before the first public alpha. No compatibility surface or migration is required because no prior candidate, platform, aggregate, or lineage evidence was frozen, published, or consumed. Validators reject legacy circular graphs, placeholders, fixed-point attempts, missing/backward edges, mixed release identities, duplicate platform records, incomplete alpha matrices, non-allowlisted final deltas, and unhashed cross-document bindings. This repair changes neither the catalog payload schema nor Task 17 fixture bytes/digest.

Release evidence SHALL never silently combine packages or artifacts from different release inputs, SemVer values, catalogs, results, or freeze records. Public-alpha smoke consumes only already-published production assets. Every published final asset is one already frozen signed final candidate with no post-freeze delta; final lineage separately proves latest-alpha passes, allowed source delta, reproducible final construction, exact promotion, and final-origin behavior.

**COMP-012** Every plan, task packet, candidate, profile, result, and evidence index SHALL identify the exact approved specification revision it implements. An artifact naming an older revision remains historical evidence only and cannot authorize new implementation or qualification until reconciled through the appropriate planning workflow.

## 10. Normative State Model

### 10.1 Support tier

`Production`, `Preview`, `Planned`, or `Unsupported` is derived only from the app-shipped support matrix and release evidence.

- Hardware probing SHALL NOT promote a tier.
- A Preview path remains Preview after successful warm-up.
- An absent Linux HIP allowlist intersection is Unsupported, not Preview-by-default.
- Planned permits explanatory UI but no executable operation.

### 10.2 Artifact setup

Runtime packs and model artifacts have independent states:

```text
Missing -> Downloading -> Resumable -> Verifying -> Installing -> Installed
Installed -> Deleting -> Missing
Installed -> Installed + Update available
Any operation -> Failed
Verification/inventory failure -> Corrupt
Security denylist -> Blocked
```

- `Resumable`, `Verifying`, and `Installing` are not executable/loadable.
- `Update available` never downloads or changes selection automatically.
- `Corrupt` and `Blocked` artifacts never execute or load.
- A failed new install leaves an older installed revision unchanged.

### 10.3 Capability validation

Capability state is separate from current residency:

```text
Unchecked -> Checking -> EstimateOnly | Validated | NotReady
Validated -> Stale | NotReady
EstimateOnly | NotReady | Stale -> Checking
```

- `EstimateOnly` may use catalog requirements, enumerated backend features, and current memory, but SHALL never be presented as `Ready`.
- `Validated` requires a verified runtime and model, exact backend initialization, bounded compute/allocation proof, full model load, and warm-up for the exact fingerprint.
- `Validated` evidence is process-local and is never authoritative after app restart.
- Intentional unload MAY retain `Validated` evidence for the unchanged fingerprint, but it does not retain operational `Ready`; a subsequent load rechecks dynamic resources and repeats load/warm-up.
- A failed actual allocation/load/warm-up overrides every earlier estimate and sets `NotReady` with a typed cause.

### 10.4 Residency and activity

Residency uses:

```text
Unloaded -> Loading -> Loaded -> Unloading -> Unloaded
Loading | Loaded | Unloading -> Failed -> Unloaded
```

Transcription activity is independently `Idle` or `Transcribing`.

- GPU `Loaded` means the exact model is resident through the selected GPU backend.
- CPU `Loaded` means the exact model is resident in worker RAM.
- `Load now` and lazy first-use loading execute the same validation path.
- Successful transcription normally retains `Loaded` residency.
- `Unload` requests engine-level free and then terminates the sidecar; confirmed process exit is required for deterministic ownership release.
- A crash or uncertain load fails activity, invalidates operational readiness, and forces cleanup.

### 10.5 Derived provider readiness

**CAP-011, LIFE-005, UI-006** The provider's operational status SHALL be derived as follows:

| Conditions                                                                                                        | Status                                                                  |
| ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| Valid settings, installed verified artifacts, exact capability `Validated`, residency `Loaded`, no blocking fault | `Ready` (or `Busy` while transcribing)                                  |
| Same fingerprint was validated but residency is `Unloaded`                                                        | `Validated · Unloaded`; operationally Not ready, eligible for lazy load |
| Artifacts missing/downloading/corrupt/blocked, capability unchecked/estimate/stale/not-ready, or residency failed | `Not ready` with exact safe reason                                      |
| Support tier Planned/Unsupported                                                                                  | `Planned`/`Unsupported`; never Ready                                    |

This rule preserves useful validation history after `Unload` without claiming that an unloaded model is presently Ready.

The existing authentication-oriented `isReady` guard SHALL NOT be reused to block lazy loading. Provider metadata/readiness SHALL add a discriminated local-runtime snapshot with at least `canAttempt`, derived operational status, and safe reason. `canAttempt` means the coordinator may receive a transcription request and either lazy-load or return its exact typed setup/capability failure; it is not a claim of success. Structurally invalid settings or an active conflict may disable the attempt, but missing artifacts, unsupported hardware, or unloaded residency SHALL never be misrepresented as a missing login/API key.

## 11. Current-Device Validation

**CAP-001** The settings screen and provider SHALL validate whether the exact selected Local Whisper configuration can run on the current device. The result SHALL include support tier, setup state, capability state, residency, engine, target, backend, sanitized opaque device ID, runtime/model revisions, observed timestamp, and safe reason codes.

### 11.1 Common stages

A full proof, initiated only by `Load now` or an eligible uncached transcription, SHALL check, in order:

1. supported OS and architecture;
2. support-matrix eligibility for engine/target/backend/device;
3. installed, non-blocked, signature/hash-verified runtime pack and compatible protocol;
4. backend/system prerequisites and exact device presence;
5. installed, hash-verified compatible model artifact;
6. current disk/RAM/VRAM advisory resources;
7. backend initialization and bounded allocation/compute dispatch;
8. full model load;
9. bounded local warm-up using non-personal embedded synthetic input;
10. confirmation that the worker actually used the selected target/backend/device and did not fall back internally.

For a GPU configuration, stages 4, 7, 8, and 10 SHALL use the Section 7.3 topology-bound device authority and CAP-017 backend-native proof. The opaque product device ID remains main-owned; the private runtime ordinal and proof are valid only for the exact engine/runtime/backend registry snapshot and worker lifetime. Reuse of the same number after registry reorder, authority substitution, or topology change SHALL fail even when the numeric ordinal is unchanged. Successful context/model construction without proof of weight ownership and the primary execution backend SHALL fail closed as `DEVICE_PROOF_FAILED`.

`Check compatibility` and predownload checks stop at an `EstimateOnly` result and SHALL list the exact missing artifacts or prerequisites. They MAY perform a bounded non-resident backend probe in a short-lived worker, but SHALL not load the selected model, retain a worker/allocation, or report Ready. No check initiates a download implicitly.

The exact selected-configuration memory estimate SHALL use the matching qualified peak when one exists, otherwise its matching catalog peak, then add at least `max(20% of that peak, 512 MiB)` headroom. The Section 8.1.1 family ranges never supply this block threshold. When the backend exposes a trustworthy current free-memory value below the exact threshold, `Load now` and uncached lazy inference SHALL be blocked with `INSUFFICIENT_VRAM` or `INSUFFICIENT_RAM`; release 1 has no override. When the backend cannot expose a trustworthy value, the UI SHALL show `Resource availability unknown` and MAY permit the real allocation attempt. A passing or unavailable estimate never guarantees success; dynamic real allocation/load failure remains authoritative.

**CAP-012, FAIL-006** This known-insufficient-versus-unknown resource policy is mandatory and has no release-1 user override.

### 11.2 CUDA

CUDA validation SHALL prove:

- a physical NVIDIA device matching the selected opaque ID;
- a driver version compatible with the pack's pinned CUDA dependency family;
- a compiled/allowed compute capability for the pack;
- required reviewed libraries from the pack, not arbitrary user paths;
- successful bounded device allocation and kernel/compute dispatch;
- full model load and warm-up on CUDA;
- worker confirmation of the actual CUDA device and positive model-weight allocation on that device.

### 11.3 Linux HIP

HIP validation SHALL prove:

- exact app-manifest match for Linux distribution/version and x64 architecture, kernel/amdgpu ABI, ROCm/HIP package family, AMD PCI device ID, and compiled `gfx` target;
- every declared system prerequisite and shipped/runtime SONAME identity rather than an arbitrary mixture of bundled and system ROCm libraries;
- required CPU PCIe atomics capability where the reviewed ROCm/device matrix requires it;
- usable `/dev/kfd` and DRM render-node permissions without elevation or permission modification;
- successful allocation and bounded HIP kernel dispatch;
- full model load and warm-up on the selected HIP device.

Any missing or unlisted intersection fails closed. Vulkan and CPU remain explicit alternative settings, not recovery paths.

### 11.4 Vulkan

AMD Vulkan validation SHALL prove:

- a physical hardware device, not a software ICD;
- Vulkan API at least `max(1.2, the runtime pack's pinned generated-shader target)`; the audited initial build target is Vulkan 1.3;
- `storageBuffer16BitAccess` and every feature required by the pinned worker;
- compatibility with the pinned loader, headers, SPIR-V inputs, shader compiler, and generated-shader target recorded by the runtime build;
- a usable memory budget when the driver exposes it;
- successful bounded compute dispatch;
- full model load and warm-up on the selected Vulkan device.

### 11.5 CPU

CPU validation SHALL prove:

- Windows or Linux x64 support for the selected runtime pack;
- required ISA features and a positive logical-processor count;
- a valid requested thread count or a resolved `auto` count within that total;
- sufficient current RAM with required headroom;
- bounded compute, full model load, and warm-up without GPU initialization.

### 11.6 Metal skeleton

The Metal adapter SHALL return one stable typed `PLANNED_UNAVAILABLE` result before any manifest lookup, download, spawn, allocation, or transcription. Remote data SHALL not be able to enable it in this release.

## 12. Artifact Trust, Storage, and Distribution

### 12.1 Catalog and signing

**SEC-008, PKG-005** The application SHALL ship an immutable Local Whisper catalog authenticated with Ed25519 by a public key/key ID embedded in the installed application. A key supplied by a downloaded artifact is never trusted.

**PKG-008, SEC-012** `catalog.json` SHALL remain one strict Ed25519 envelope with exact keys `schemaVersion`, `algorithm`, `keyId`, `payloadBase64`, and `signatureBase64`. `catalog.sha256` is package-staging integrity metadata, not a substitute for signature verification. The app-owned public `keyring.json` is a separate packaged trust input; there is no detached `catalog.sig` sidecar.

The immutable Task 17 fixture payload/envelope schema and bundle bytes remain byte-for-byte unchanged, including bundle digest `de8603f4c96a793ed3a3d3a03941f44d67592ae945d17d3b19ae0ed56e039226`. Qualification and production keep envelope schema version 1 but use catalog payload schema version 2. Payload v2 SHALL add authenticated catalog `purpose`, per-artifact `transferProfile`, canonical source repository/commit/file identity for upstream models, redirect-policy ID, qualification status/profile digest, notices/provenance/SBOM references, and every Section 9 identity needed by the selected transport. Unknown, missing, duplicate, purpose-incompatible, or schema-incompatible fields fail closed; there is no implicit fixture migration or remote format switch.

**PKG-011, SEC-014** Packaging SHALL have four disjoint fail-closed modes:

- `disabled`: the existing deferred-publication sentinel, empty Local Whisper keyring/origins, and no executable/model catalog action; ordinary remote-provider releases remain buildable;
- `fixture`: the existing credential-free Task 17 synthetic bundle only; fixture key IDs, purpose, origins, schema, and digest are never accepted by qualification or publishable release collection;
- `qualification`: one non-publishable candidate build containing an isolated qualification-purpose catalog, temporary qualification public key, and exact candidate inputs. Its private key is generated/held outside the repository and destroyed or revoked after the frozen evidence is complete. Qualification packages SHALL be visibly marked, SHALL NOT be emitted by production release collection, and SHALL reject fixture/production catalogs;
- `production`: only a frozen reviewed production-purpose payload/keyring/origin tuple may enter the protected release-candidate build after signing, legal, hosting-layout, and candidate-freeze gates. A frozen production candidate may enter release staging and structural final-origin verification. For an alpha, it becomes an installation and platform-smoke origin only after separately approved public prerelease publication; the post-release smoke consumes those public assets read-only. Final has no physical platform-test consumer. Production packages SHALL reject fixture and qualification keys/purposes.

Key IDs, public keys, and accepted purposes SHALL be pairwise disjoint between fixture, qualification, and production. A build-mode/purpose mismatch, missing tuple, test/qualification key in production collection, production key in a qualification build, or trust-resource substitution fails before package creation or collection. The release-readiness chain SHALL prove that every production installer and runtime archive is the exact frozen signed candidate bound by its `releaseCandidateDigest` and `releaseStagingDigest`. An alpha platform-smoke branch additionally binds the exact public deployed assets; final lineage instead binds the latest passing alpha aggregate, closed final-source delta, and final reproducibility evidence. No executable, native pack, model, protocol, setting, trust, catalog, origin, release metadata, native signature, timestamp, or package-byte delta is permitted after that candidate is frozen.

The non-packaged development activation from **DEV-001** MAY consume the same authenticated `qualification`-purpose inputs through the ordinary desktop composition root without creating a qualification package. It SHALL preserve every signature, purpose, keyring, origin, redirect, artifact, filesystem, process, privacy, and protocol check. The activation descriptor and public verification inputs MAY be generated into a private ignored task root; private signing material SHALL remain outside the repository and SHALL NOT be passed to the application. Fixture trust, production trust, unsigned documents, embedded test private keys, and relaxed verification are invalid substitutes. No development activation input may be collected into a packaged build or release artifact.

**PKG-012, SEC-014** Production application candidates SHALL receive Windows Authenticode and every applicable Linux package-native signature before release staging. Production catalogs and runtime packs SHALL retain their existing independent signatures. One separately signed release manifest SHALL bind every application installer, runtime pack, catalog, signature, SBOM, notice, and checksum asset by exact filename, byte length, SHA-256, platform, architecture, role, release tag, and source revision. A platform with no applicable package-native signature remains covered by the signed release manifest; no unsupported universal code-signing claim is permitted. Private signing material SHALL remain confined to the protected signing environment and SHALL never enter the repository, CI artifact store, post-alpha test host, application, runtime pack, logs, or release evidence.

This trust model assumes that the installed application and its embedded verifier keys are trusted after the installer and signed release manifest have been verified. Artifact signatures protect the app-owned download path against substitution under that assumption; they cannot protect a user who deliberately runs an unverified or tampered installer.

Every catalog entry SHALL contain the identities and metadata from Section 9 and one or more allowlisted HTTPS origins. Redirect targets SHALL be revalidated against a signed redirect-policy record. URLs and request headers SHALL not contain device identifiers, prompts, audio, transcripts, user settings, cookies, authorization tokens, or a stable per-install identifier.

Payload-v2 redirect policies SHALL be a closed bytewise-sorted list. Each record contains exact `id`, initial `scheme = https`, initial lowercase ASCII host, `port = 443`, initial path prefix, `maxRedirects <= 5`, and a non-empty list of allowed targets with exact lowercase ASCII host, port 443, path prefix, whether the original `Range`/`If-Range` headers may be forwarded, and `credentialForwarding = false`. Hosts, IP literals, ports, and paths are compared after strict WHATWG URL parsing and canonical percent-encoding checks; userinfo, fragments, non-HTTPS schemes, encoded separators/traversal, host suffix matching, DNS-derived trust, and catalog-undeclared headers are rejected. Query values MAY vary only on an already allowed target because public CDNs use expiring signatures; query data is never persisted, logged, or treated as identity.

**DIST-001, DIST-002, DIST-004** Release-1 origins are closed as follows:

- application installers and every project-built Local Whisper native/runtime asset that a user installs use immutable assets under `https://github.com/swimmwatch/gpt-voice/releases/download/<exact-tag>/<exact-asset>` in the one approved application release and a signed GitHub-release redirect policy;
- model files use `https://huggingface.co/ggerganov/whisper.cpp/resolve/5359861c739e955e79d9a303bcbc70fb988958b1/<exact-file>` and a signed public-Hugging-Face redirect policy. Only the six exact files in Section 9.2 are eligible.

GitHub Actions artifacts are bounded build/qualification inputs only. Branch archives, raw-content URLs, mutable tags, workflow artifacts, mirrors, prerelease scratch storage, and user- or renderer-supplied origins SHALL NOT be exposed or accepted as application or Local Whisper binary installation origins. App-owned native helpers and bundled browser binaries SHALL arrive only inside the authenticated application installer; on-demand inference workers and their native dependencies SHALL arrive only in an authenticated matching runtime pack from the same approved release tag. The pinned model files above are the sole non-GitHub-Release installation exception and remain non-executable data.

An optional qualification-purpose catalog MAY replace only the runtime origin with a single-use loopback HTTPS diagnostic server whose generated CA/public certificate, exact origin/port, range/ETag behavior, and archive digest are pinned in its private diagnostic record. It SHALL bind only `127.0.0.1`, require no credentials, expose only frozen diagnostic objects, terminate with the run, and is never accepted by production. This non-publishable path may exercise transfer/package behavior but creates no alpha or final release evidence. Required platform smoke instead installs exact already-public alpha assets from their immutable same-tag GitHub Release origins. Final staging performs structural final-origin verification only and has no physical install/runtime test.

The canonical URL, source repository/commit/file, expected length/hash, and initial host are artifact identity. A CDN redirect URL or its expiring query is not persisted as identity. Redirects remain HTTPS, use at most the Section 12.3 bound, preserve method/range semantics, and may target only exact host/path rules authenticated by the app-shipped catalog. The initial reviewed Hugging Face chain is `huggingface.co` to `us.aws.cdn.hf.co` under its model-content path. An added or changed host/suffix/path rule requires a newly signed catalog in a trusted app release; arbitrary `*.hf.co`, `*.xethub.hf.co`, user-supplied mirrors, URL shorteners, and cross-origin credential forwarding are prohibited.

All artifacts SHALL be verified against exact size and SHA-256 from the authenticated catalog. Executable runtime archives SHALL additionally carry an artifact signature. For an upstream model, the authenticated catalog signature binds its exact repository/commit/file/size/SHA-256 and replaces any requirement that GPT-Voice re-sign or rehost the upstream byte stream; Hugging Face ETag/Xet metadata is additive transport evidence only. Runtime executables and libraries SHALL be revalidated before every spawn. Model files SHALL be fully verified at promotion and before their first load in each app process; changed identity, size, or metadata forces a full rehash.

For qualification- or production-purpose catalogs in this release, runtime entries SHALL use only `restricted-tar-gzip-v1` and model entries SHALL use only `pinned-raw-model-v1`, both defined in Section 12.3. The profile is selected by trusted application code from artifact kind and must equal the authenticated catalog value; downloaded metadata, MIME type, filename extension, redirect response, or renderer input cannot select it. A different mapping requires a newly trusted application/specification revision.

The embedded verifier keyring SHALL support key IDs and rotation only through a newly trusted installed application release obtained under the project's existing distribution policy. This requirement does not imply a signed-update channel. An app-shipped security denylist MAY mark an installed revision `Blocked`; this prevents execution/load without deleting it or selecting a fallback.

### 12.2 Managed roots

**MODEL-006, SEC-004** Large artifacts SHALL live in a fixed, non-roaming, per-user application data root separate from configuration files:

- Windows: `%LOCALAPPDATA%/<canonical-app-id>/local-whisper`;
- Linux: `${XDG_DATA_HOME:-$HOME/.local/share}/<canonical-app-id>/local-whisper`.

The root SHALL contain separate `runtimes`, `models`, and `staging` namespaces. The private versioned Local Whisper settings file remains in the existing application configuration root. The macOS path resolver MAY exist for type completeness but SHALL not create/populate executable runtime storage in this release.

**SEC-007, RUN-004** All filesystem and worker-ownership operations SHALL satisfy the following path/process identity contract. All filesystem operations SHALL:

- canonicalize and prove containment under the expected managed artifact directory;
- reject Unix symlinks/mount-boundary escapes and Windows reparse points, junctions, and unexpected volume boundaries in every managed path component;
- bind verification, spawn, promotion, quarantine, and deletion to stable descriptor/handle-backed file and directory identities with no-follow semantics where the OS exposes them; a changed identity between check and use fails the operation;
- use owner-private permissions/ACLs without elevation;
- reconstruct inventory only from valid managed manifests;
- never execute or recursively delete unknown files/directories as repair behavior;
- use per-artifact locks, including across duplicate application instances.

Destructive removal SHALL first atomically rename the exact immutable artifact directory into an app-owned quarantine directory on the same filesystem while its anchored lock/identity remains held. It SHALL then delete only catalog-manifest entries through that anchored directory. An unexpected entry, identity swap, junction/reparse point, mount change, failed quarantine, or non-empty final directory makes removal fail without broad recursive cleanup.

Runtime spawn SHALL revalidate the executable/library identity immediately before creation and require the worker handshake to report the expected build/protocol digest. A path swap, file-ID change, or digest mismatch terminates the child before model data is accepted.

The UI SHALL show sanitized managed location and disk usage. Removal means ordinary filesystem unlinking; the app SHALL not promise SSD secure erasure or recoverability.

### 12.3 Download and install

**DL-001, DL-002, DL-003, PERF-001** Runtime and model downloads occur only after an explicit user action and use journaled app-owned staging on the same filesystem as the final artifact.

**DL-004, ARCH-010** The production main-process artifact port SHALL compose the catalog resolver, progress/journal store, bounded downloader, streaming verifier, transport-specific materializer, managed artifact store, inventory reconstruction, and coordinator epochs behind the existing typed command surface. Renderer/preload SHALL expose only artifact identity plus `download`, `resume`, `retry`, `cancelArtifact`, explicit `update`, and confirmed `remove`; URL, redirect, certificate, path, staging, hash, signature, and promotion authority remain main-only. Action availability is deterministic: `Missing` may download, a resumable partial may resume or cancel, a failed explicit operation may retry, an installed old revision with a newer immutable catalog entry may update, and installed artifacts may remove subject to lifecycle conflicts. Duplicate/stale epoch commands fail without starting work; one accepted command owns one operation ID and one terminal result.

**DL-005, IMPL-002** Task 19 SHALL complete this path in the ordinary development-activated application for every Section 9.2 release-1 model. Each visible `Download` action SHALL resolve the exact authenticated Hugging Face repository/commit/file identity in main, perform the real anonymous public transfer through `pinned-raw-model-v1`, expose bounded queue/progress plus `Resume`, `Cancel`, and `Retry`, verify exact length and SHA-256, atomically promote the managed file, refresh inventory, and enable normal selection/removal. Pre-seeded files, direct filesystem copying, a qualification-runner-only transfer, fixture bytes, a moving Hugging Face branch, or a project mirror SHALL NOT satisfy Task 19. A network or upstream failure remains a typed retryable setup state; it does not authorize an alternate origin or weaken completion criteria.

Download, hashing, signature verification, extraction, and inventory rehash SHALL be streaming/backpressured work outside the Electron main event loop. No full multi-GiB archive/file may be buffered in memory. At most two unrelated artifact transfers may run concurrently; additional explicit requests wait in a visible download queue, while worker lifecycle/destructive conflicts remain subject to Section 14 rather than that queue. Each transfer SHALL cap in-memory buffering at 32 MiB.

Initial transport bounds SHALL be a 20-second connection timeout, a 60-second no-progress timeout, at most five redirects, and a 12-hour total transfer timeout. Cancellation SHALL abort network activity promptly and make all hashing/extraction workers observe cancellation checkpoints; if a helper does not stop within 5 seconds it is terminated without promoting staging. These are operational constants, not user settings.

Before transfer, main SHALL calculate required free space for the partial/transfer bytes, expanded or materialized installed bytes, atomic promotion, any simultaneously retained installed revision, and a safety margin of at least `max(10% of installed size, 512 MiB)`. Insufficient space fails before network transfer when known and remains safe on mid-transfer `ENOSPC`.

A staged artifact SHALL never execute or load. Installation SHALL follow:

1. resolve a typed catalog artifact ID to an allowlisted HTTPS origin in main;
2. create a unique contained staging directory and journal;
3. stream bytes with bounded progress and cancellation;
4. verify complete length, catalog authenticity, transfer SHA-256, and the runtime artifact signature when applicable;
5. extract a runtime archive or materialize one raw model file with fixed limits and no execution;
6. verify exact expected file names, regular-file types, modes, sizes, and SHA-256 hashes;
7. verify app/protocol/backend compatibility;
8. atomically rename/promote to its immutable identity directory;
9. reconstruct inventory and report `Installed`.

Runtime archive extraction SHALL reject absolute paths, `..` traversal, symlinks, hard links, device nodes, FIFOs, sockets, unexpected names, duplicate entries, excessive file count, and expanded-size overflow. No installer, `pip`, Conda, compiler, or package manager is invoked on the user's system.

The `restricted-tar-gzip-v1` production profile SHALL use no new runtime codec dependency. Decompression SHALL use the maintained gzip/DEFLATE implementation shipped with the app's pinned Node/Electron runtime, and a project-owned streaming reader SHALL parse the resulting tar bytes outside the Electron main thread. The reader SHALL enforce all of the following before promotion:

- exactly one RFC 1952 gzip member using compression method 8, flags 0, `MTIME=0`, no optional extra/name/comment/header-CRC fields, a valid CRC32/ISIZE trailer, and no concatenated member or trailing compressed bytes;
- one deterministic POSIX ustar stream with 512-byte records, valid header checksums, exact `ustar\0`/`00` identity, canonical ASCII-octal numeric fields, and exactly two terminal zero records with no following decompressed data;
- regular-file entries only, using type flag NUL or `0`; PAX/GNU extensions, base-256 numbers, sparse files, directories, links, devices, FIFOs, sockets, and every unknown type are invalid;
- one flat portable ASCII entry name of at most 100 bytes that exactly equals an authenticated expected-file ID; the ustar prefix field and path separators are forbidden;
- exact manifest mode and size; deterministic producer metadata uses `uid=0`, `gid=0`, `mtime=0`, empty user/group names, and zero-filled unused header bytes;
- entry order exactly matching the authenticated expected-file manifest, with no missing, duplicate, case-colliding, undeclared, or reordered member;
- an exact maximum decompressed tar length computed as `sum(512 + ceil(fileSize / 512) * 512) + 1024`, checked with safe-integer arithmetic before transfer; decompression, header parsing, padding, file bytes, and end records may never exceed that bound;
- backpressure, cancellation, per-entry hashing, and the existing 32 MiB aggregate transfer-buffer ceiling across compressed input, decompressor output, parser state, and queued file chunks.

The archive URL suffix and HTTP `Content-Type` are advisory only. Complete compressed size, compressed SHA-256, artifact signature, strict gzip structure, strict ustar structure, every extracted file hash, and compatibility evidence SHALL all pass independently. Any mismatch returns the existing safe `ARCHIVE_INVALID`, `HASH_MISMATCH`, or `SIGNATURE_INVALID` result as applicable, discards only proven staging owned by that operation, preserves installed revisions, and never retries the same untrusted object automatically.

**DIST-002** The `pinned-raw-model-v1` profile SHALL accept exactly one HTTP response body and materialize exactly one regular model file; it has no decompressor, archive parser, entry names, directories, modes from transport metadata, or extraction phase. The destination file ID/mode are taken only from the authenticated catalog. The downloader SHALL stream the body directly into an owner-private no-follow staging file while computing SHA-256, reject a response larger than the exact catalog length before the extra bytes are retained, reject short length, range/validator inconsistency, content-encoding transformation, multipart bodies, and any attempt to derive a path from URL or `Content-Disposition`, then fsync/close/reopen through the managed identity boundary and verify the entire staged file before atomic promotion. `Content-Type`, `Content-Disposition`, ETag, Xet hash, and linked hash are advisory/additive only; exact catalog SHA-256 is authoritative.

The first release model source is the public anonymous Hugging Face URL set in Section 12.1. The app SHALL send no Hugging Face token, cookie, referrer containing private data, or user/device identifier. HTTP `Content-Encoding` SHALL be identity; CDN compression is rejected so ranges and the byte hash refer to the same model object. An upstream outage, rate limit, deleted commit/file, changed redirect policy, changed length/hash, or inability to resume is a typed setup failure and SHALL NOT trigger a mirror, moving branch, model substitution, project upload, or automatic retry loop.

Expected-file count and expanded-byte limits SHALL come from the authenticated exact manifest; extraction cannot exceed them. Progress updates SHALL be rate-limited and renderer-safe, and large-artifact work SHALL not block settings, recording controls, provider switching, or application shutdown responsiveness.

### 12.4 Resume, cancel, restart, and retry

An unexpected network loss or app restart MAY retain a partial only when artifact identity, manifest revision, canonical URL, expected length, and server validator such as ETag still match. A fresh allowlisted redirect with a different expiring query MAY be resolved for the same immutable Hugging Face source identity; the prior CDN URL/query is never persisted as authority. Resume SHALL require `Accept-Ranges`, a valid `206` response, exact `Content-Range`, matching validator/object identity, and full-object verification from beginning to end. Any mismatch discards the partial and restarts from byte zero after explicit resume/retry.

Explicit `Cancel` SHALL stop the request and remove that unverified staging data. A failure SHALL preserve a previous installed revision and require explicit `Retry`; there is no unbounded automatic retry loop. On startup, abandoned journals SHALL be classified as safely resumable or removed without touching installed artifacts.

**FAIL-003** Offline, DNS/TLS, HTTP, redirect, range, length, hash, signature, extraction, protocol, permissions, and disk-space failures SHALL produce distinct safe setup errors and SHALL never promote a partial artifact.

### 12.5 Updates and rollback

An app catalog may advertise a new immutable model or runtime revision as `Update available`.

- Download is explicit.
- The old revision remains installed and selected.
- The new revision is installed alongside it.
- Selection changes only through an explicit user save and unload/reload.
- Rollback is an explicit selection of a still-installed compatible old revision.
- App-incompatible packs remain stored but cannot execute.
- No app upgrade or downgrade automatically deletes an artifact.

## 13. Model and Runtime Management Scenarios

### 13.1 Download selected model revision

1. The screen shows the fixed `whisperCpp` engine identity, and the user selects logical family, revision, and variant.
2. The screen shows exact download/installed sizes, source/provenance, license, format, estimated memory, and support limitations.
3. `Download` starts only the exact catalog artifact.
4. On verified atomic promotion, state becomes `Installed`; selection is unchanged.
5. The model remains unloaded until `Load now` or a transcription request.

Changing selection during download does not retarget the existing download. The unrelated download may finish into inventory, while its original artifact identity remains explicit.

### 13.2 Delete a model revision

Every catalog revision SHALL have state-specific actions:

| Revision state                                | Available destructive/setup action                                                                                                                                                                                          |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Missing`                                     | No Delete action; Download is available when supported.                                                                                                                                                                     |
| `Downloading` or `Resumable`                  | `Cancel` controls staging; Delete is unavailable.                                                                                                                                                                           |
| `Verifying`, `Installing`, or `Deleting`      | Conflicting actions are unavailable and forged commands return `OPERATION_CONFLICT`.                                                                                                                                        |
| `Installed` or `Installed + Update available` | Delete is available for selected and unselected revisions.                                                                                                                                                                  |
| `Corrupt`, `Blocked`, or `Delete failed`      | Remove/Retry removal is available only when the authenticated catalog and repository can prove the exact managed identity directory; otherwise automatic deletion is prohibited and safe manual-recovery guidance is shown. |
| Unknown/unmanaged directory                   | Never exposed as a deletable catalog artifact.                                                                                                                                                                              |

**MODEL-008, VRAM-003, FAIL-001** Deletion of any eligible model revision SHALL:

1. show confirmation naming logical model, engine, artifact revision, variant, and disk impact;
2. reject during active transcription or conflicting install/delete/lifecycle work;
3. unload and stop the worker first if that exact artifact is resident;
4. use the authenticated catalog/repository identity even when model contents are corrupt;
5. atomically quarantine the exact identity directory within its managed parent, then delete only manifest-owned entries through the anchored path-safety contract in Section 12.2;
6. on partial deletion, report `Delete failed`, keep the exact selection, mark the revision unusable, and reconstruct inventory;
7. only after complete proven deletion, retain the persisted selection as `Model missing` when it was selected;
8. never select/download another model or revision automatically.

Deleting an unselected installed revision does not affect the active worker. Unknown files are not deleted as part of this action.

### 13.3 Remove a runtime pack

Runtime management SHALL list actions per revision, not only for the selected pack, and SHALL use the same state-action table as models. **RUNTIME-004, FAIL-004** Removal SHALL use equivalent confirmation, conflict rejection, worker unload/termination, containment/quarantine, exact-file deletion, and partial-failure rules. If selected, the revision remains selected as `Runtime missing`. No compatible-looking pack is selected automatically.

### 13.4 Transcription preflight, cache, and lazy load

**ARCH-009, CACHE-002** Local Whisper SHALL not use the current browser-auth readiness/cache ordering. The shared transcription orchestration SHALL dispatch by provider readiness kind and apply this exact order for Local Whisper:

1. capture provider/configuration/inventory epochs and validate the canonical audio plus structural settings;
2. run local dispatch eligibility: support tier, exact device presence, external prerequisites, trustworthy resource insufficiency, and selected runtime/model setup, compatibility, integrity, and denylist state;
3. fail with the exact typed state code before cache lookup if that gate fails;
4. construct the complete private cache context from the same epochs;
5. on a valid cache hit, enter the existing successful completion flow without starting/loading a worker and leave residency unchanged;
6. on a cache miss, acquire the lifecycle lock, recheck every epoch/gate, and run the lazy load sequence below.

Thus missing, corrupt, blocked, incompatible, unsupported, absent-device, and known-insufficient-resource states cannot be bypassed by an old cache entry. `Validated · Unloaded` may serve an eligible cache hit without allocating VRAM/RAM. Lazy loading occurs on the first eligible cache miss that actually requires inference.

`Load now` skips cache and executes the full load sequence directly:

1. Capture the exact configuration epoch and validate settings.
2. Verify support eligibility, runtime/model state, integrity, and prerequisites.
3. Acquire the lifecycle lock and recheck the epoch.
4. Acquire and revalidate the exact read-only model lease, then create its one-use native handoff authority.
5. Create a fresh full-load worker through the platform launcher, bind only the duplicated model authority to logical slot `3` (actual fd `3` on Linux; the authenticated arbitrary inherited `HANDLE` binding on Windows), and complete the authority acknowledgement and worker handshake.
6. Initialize the exact backend/device in that worker and repeat the topology-bound native activation proof.
7. Allocate and fully load the selected model through the descriptor/handle-relative loader, then verify the returned artifact identity/digest and allocation evidence.
8. Run the bounded warm-up and prove no internal fallback.
9. Set capability `Validated`, residency `Loaded`, and operational `Ready` only if every stage succeeds; otherwise close every authority copy and terminate the worker before returning the typed failure.

An eligible cache-miss transcription from `Unloaded` SHALL perform the same sequence and wait for it. It SHALL never initiate an implicit download. Any stage failure terminates the child, sets a typed Not-ready reason, preserves selection/install state, and does not submit audio for a successful completion.

### 13.5 Unload

`Unload` SHALL reject active transcription, enter `Unloading`, request a bounded cooperative drain/best-effort engine release, then always stop the worker. Upstream in-process unload flags, allocator-cache calls, or state booleans are not proof that VRAM/RAM was released. Completion requires confirmed worker exit, closure of every model authority and runtime handle, and the platform settling check. If graceful exit exceeds its bound, the supervisor SHALL terminate the child tree and confirm exit. Confirmed forced termination may complete with a sanitized warning; unconfirmed termination remains `Failed` and blocks destructive artifact operations.

After successful unload:

- residency is `Unloaded`;
- provider operational status is not Ready;
- same-fingerprint capability evidence may remain `Validated` for display;
- current dynamic resources are not assumed available for the next load;
- worker-owned RAM/VRAM must no longer be allocated after the platform settling bound.

### 13.6 Replacement, provider switch, and exit

Engine, runtime, target, backend, device, model, variant, or CPU-thread changes SHALL unload the old residency before activating the new epoch. A failure to unload leaves the old selection unchanged and the save failed.

An idle provider switch SHALL serialize Local Whisper unload/shutdown before another provider becomes active. A switch requested during `Loading`, `Unloading`, or `Transcribing` SHALL return `OPERATION_CONFLICT`; it SHALL not cancel, wait in a hidden queue, or change the current provider. Unrelated downloads owned by the composition root MAY continue.

Application exit is the lifecycle exception: it SHALL stop accepting work, cancel an active load/transcription, discard partial output, preserve only valid resumable journals, clean private in-memory audio, and perform bounded graceful-then-forced child-tree termination. Exit cleanup SHALL never wait beyond the declared cancellation/termination bounds.

On OS suspend, the coordinator SHALL mark the capability `Stale`, stop accepting work, and request cancellation/unload within the platform's available shutdown window. On resume it SHALL treat any prior residency as uncertain, terminate any still-proven-owned worker, set residency `Unloaded`, re-enumerate devices, and require explicit/lazy reload; it SHALL never auto-reload. Hot-unplug, driver reset, or external-GPU change during operation SHALL fail the activity, clean up the worker, and produce the same `Stale`/`Unloaded` recovery boundary.

## 14. Concurrency and Lifecycle Rules

Exactly one worker lifecycle or transcription operation may own the active worker at a time. Load, unload, resident replacement, selected runtime/model deletion, and transcription are mutually conflicting.

- The renderer SHALL disable conflicts it knows about; main SHALL independently reject stale/forged races.
- Every mutating command SHALL include or be checked against configuration and inventory epochs.
- A conflict returns `OPERATION_CONFLICT` immediately. Hidden destructive queues are prohibited.
- The user must explicitly retry after the active operation finishes.
- A download for an artifact unrelated to the worker or destructive target MAY continue.
- Per-artifact locking SHALL serialize download, verification, promotion, deletion, integrity check, and load use of the same revision.
- Multiple application instances SHALL not concurrently promote/delete/load the same managed artifact.
- Closing the settings window does not cancel a process-owned download; its state remains visible when reopened.

**LIFE-006** An explicit cancellation during lazy `Loading` SHALL abort the stage, terminate the partial/uncertain worker, leave residency `Unloaded`, and retain only capability evidence that predated the cancelled attempt. During `Transcribing`, main SHALL send a bounded worker cancel request. A `whisperCpp` worker may remain Loaded only after its patched cooperative callback confirms cancellation, all inference ownership joins, no late frame can arrive, and the worker remains healthy. Any timeout, failed acknowledgement, or uncertain backend state terminates the worker and leaves residency `Unloaded`. In every case partial text is discarded, result is `CANCELLED`, and no success-side effect occurs. Provider switching during active work is a conflict and is rejected; application exit is authoritative and cancels/terminates the operation.

## 15. Failure Contract

Local Whisper commands SHALL return a typed renderer-safe result with:

- stable error code;
- failing stage;
- retryable boolean;
- localized recovery action identifier;
- safe artifact or sanitized device identity where relevant;
- current support/setup/capability/residency snapshot.

At minimum, the error union SHALL cover:

| Class                | Required codes                                                                                                                                                                                                                                                                                                                                         |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Settings/support     | `INVALID_SETTINGS`, `SETTINGS_VERSION_UNSUPPORTED`, `STALE_CONFIGURATION`, `CATALOG_UNAVAILABLE`, `UNSUPPORTED_PLATFORM`, `UNSUPPORTED_ARCHITECTURE`, `TARGET_UNSUPPORTED`, `BACKEND_UNSUPPORTED`, `PLANNED_UNAVAILABLE`                                                                                                                               |
| Device/prerequisites | `DEVICE_NOT_FOUND`, `DEVICE_NOT_ALLOWLISTED`, `DRIVER_INCOMPATIBLE`, `RUNTIME_PREREQUISITE_MISSING`, `DEVICE_FEATURE_MISSING`, `GPU_PERMISSION_DENIED`, `CPU_FEATURE_MISSING`                                                                                                                                                                          |
| Resources            | `INSUFFICIENT_DISK`, `INSUFFICIENT_RAM`, `INSUFFICIENT_VRAM`                                                                                                                                                                                                                                                                                           |
| Artifact setup       | `RUNTIME_MISSING`, `RUNTIME_INCOMPATIBLE`, `RUNTIME_BLOCKED`, `RUNTIME_CORRUPT`, `MODEL_MISSING`, `MODEL_INCOMPATIBLE`, `MODEL_BLOCKED`, `MODEL_CORRUPT`, `DOWNLOAD_OFFLINE`, `DOWNLOAD_FAILED`, `DOWNLOAD_CANCELLED`, `UNSAFE_REDIRECT`, `RESUME_INVALID`, `SIGNATURE_INVALID`, `HASH_MISMATCH`, `ARCHIVE_INVALID`, `INSTALL_FAILED`, `DELETE_FAILED` |
| Worker/capability    | `WORKER_START_FAILED`, `WORKER_PROTOCOL_MISMATCH`, `WORKER_PROTOCOL_VIOLATION`, `WORKER_CRASHED`, `OPERATION_TIMEOUT`, `BACKEND_INIT_FAILED`, `DEVICE_PROOF_FAILED`, `ALLOCATION_FAILED`, `MODEL_AUTHORITY_INVALID`, `MODEL_LOAD_FAILED`, `WARMUP_FAILED`, `CLEANUP_FAILED`                                                                            |
| Operation            | `OPERATION_CONFLICT`, `AUDIO_FORMAT_UNSUPPORTED`, `TRANSCRIPTION_FAILED`, `EMPTY_TRANSCRIPTION`, `CANCELLED`                                                                                                                                                                                                                                           |

Raw native messages, stdout/stderr, arbitrary filesystem paths, environment values, command lines, prompt/audio/transcript data, and stack traces SHALL not cross IPC.

The normalized recovery mapping SHALL be deterministic:

| Condition                                                                                                                                                       | Code                                           | Retryable now                                                             | Recovery action ID                   |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------ |
| Malformed/cross-field settings                                                                                                                                  | `INVALID_SETTINGS`                             | No                                                                        | `edit-settings`                      |
| Newer settings schema                                                                                                                                           | `SETTINGS_VERSION_UNSUPPORTED`                 | No                                                                        | `upgrade-or-reset-settings`          |
| Planned or unsupported path                                                                                                                                     | `PLANNED_UNAVAILABLE` or exact `*_UNSUPPORTED` | No                                                                        | `select-supported-configuration`     |
| Selected device is absent or its opaque ID no longer resolves before launch                                                                                     | `DEVICE_NOT_FOUND`                             | After refresh or device return                                            | `refresh-or-select-device`           |
| Linux HIP combination unlisted                                                                                                                                  | `DEVICE_NOT_ALLOWLISTED`                       | No for same combination                                                   | `select-allowlisted-backend`         |
| Driver/runtime prerequisite or permission missing                                                                                                               | Exact prerequisite/driver/permission code      | After external repair                                                     | `show-prerequisites`                 |
| Trustworthy disk/RAM/VRAM check below threshold                                                                                                                 | Exact `INSUFFICIENT_*`                         | After resources change                                                    | `free-resources`                     |
| Selected runtime/model absent                                                                                                                                   | Exact `*_MISSING`                              | After download                                                            | `download-selected-artifact`         |
| Selected runtime/model incompatible                                                                                                                             | Exact `*_INCOMPATIBLE`                         | After selection/download                                                  | `select-compatible-artifact`         |
| Selected runtime/model denylisted                                                                                                                               | Exact `*_BLOCKED`                              | No for same revision                                                      | `update-or-remove-artifact`          |
| Installed runtime/model integrity failed                                                                                                                        | Exact `*_CORRUPT`                              | After redownload/removal                                                  | `redownload-or-remove-artifact`      |
| Offline/transport failure                                                                                                                                       | `DOWNLOAD_OFFLINE` or `DOWNLOAD_FAILED`        | Yes                                                                       | `retry-download`                     |
| Redirect/resume/signature/hash/archive trust failure                                                                                                            | Exact trust code                               | No for same received object                                               | `discard-and-fetch-trusted-revision` |
| Active conflict/stale epoch                                                                                                                                     | `OPERATION_CONFLICT` or `STALE_CONFIGURATION`  | After refresh/idle                                                        | `refresh-and-retry`                  |
| Topology/registry authority, activated-device proof, or positive GPU model-weight allocation evidence does not match after launch                               | `DEVICE_PROOF_FAILED`                          | After cleanup and fresh enumeration                                       | `refresh-and-retry`                  |
| Installed model bytes or manifest identity changed before the lease is issued                                                                                   | `MODEL_CORRUPT`                                | After redownload/removal                                                  | `redownload-or-remove-artifact`      |
| The held model object becomes shorter/different than its authenticated size/hash, or exact-read reaches partial data before a defined record boundary           | `MODEL_CORRUPT`                                | After redownload/removal                                                  | `redownload-or-remove-artifact`      |
| A same-identity, hash-matching model is rejected by bounded format, dimension, tensor-count/name, or checked-arithmetic validation                              | `MODEL_LOAD_FAILED`                            | No for the same artifact/runtime pair; after update or selection change   | `update-or-change-model`             |
| Model authority is missing, writable, replayed, stale, attached to the wrong peer/slot, duplicated unexpectedly, or differs from the active lease after handoff | `MODEL_AUTHORITY_INVALID`                      | After cleanup and a fresh lease; repeated failure requires support action | `retry-load-or-change-settings`      |
| Control body, parser depth/event/member/array/key/string, frame sequence, or transport queue limit is exceeded by either peer                                   | `WORKER_PROTOCOL_VIOLATION`                    | No for the same malformed exchange; fresh worker required                 | `retry-load-or-change-settings`      |
| Declared or accumulated WAV size/sample count exceeds the canonical audio contract                                                                              | `AUDIO_FORMAT_UNSUPPORTED`                     | After a new bounded recording                                             | `record-again`                       |
| A valid in-range allocation fails after trustworthy preflight passed or availability was unknown                                                                | `ALLOCATION_FAILED`                            | Yes after cleanup/resources/settings change                               | `retry-load-or-change-settings`      |
| Worker crash/timeout/backend/load/warm-up failure                                                                                                               | Exact worker/capability code                   | Yes after cleanup unless configuration is unsupported                     | `retry-load-or-change-settings`      |
| Cleanup cannot prove termination/release                                                                                                                        | `CLEANUP_FAILED`                               | After app restart/support action                                          | `restart-application`                |
| Invalid audio container/format                                                                                                                                  | `AUDIO_FORMAT_UNSUPPORTED`                     | After new canonical recording                                             | `record-again`                       |
| User cancellation                                                                                                                                               | `CANCELLED`                                    | Yes                                                                       | `retry-operation`                    |

Main SHALL return the most specific applicable code from this table; renderer adapters SHALL not collapse it to login/not-configured or expose a raw exception. Automated fixtures SHALL assert code, retryability, action ID, and resulting states together.

**FAIL-008** These mappings have precedence over a generic worker or load failure. `DEVICE_PROOF_FAILED` SHALL mark capability evidence `Stale`, force uncertain residency through `Failed` to `Unloaded`, and terminate the worker. `MODEL_AUTHORITY_INVALID` and `WORKER_PROTOCOL_VIOLATION` SHALL mark capability `NotReady`, close every authority/transport copy, and terminate the worker before model parsing or inference. A same-authority size/hash/partial-read failure SHALL mark the installed model `Corrupt`; a hash-matching bounded-format rejection SHALL leave installed bytes unchanged but mark that artifact/runtime pair Not ready with `MODEL_LOAD_FAILED`. `AUDIO_FORMAT_UNSUPPORTED` detected before submission SHALL leave an already healthy resident worker unchanged; `ALLOCATION_FAILED` SHALL leave residency `Unloaded` and capability `NotReady`. No case may be retried transparently or converted to CPU or another device, model, or backend.

On load or transcription failure:

- no automatic engine, target, backend, device, model, revision, or variant fallback occurs;
- no partial transcript is accepted;
- no clipboard, successful history, or successful cache mutation occurs;
- a crashed, hung, or allocation-uncertain child is terminated before retry;
- installed artifacts and selections remain unchanged unless a separate verified install/delete already completed;
- the current capability/readiness state records the typed cause.

## 16. Privacy, Security, and Network Boundaries

Inference SHALL be local and offline. Network access is permitted only to the main-owned artifact downloader after explicit user action.

The worker SHALL receive the existing canonical in-memory mono PCM16, 16 kHz WAV recording through bounded stdin frames. Canonical means one fixed 44-byte little-endian header and non-empty sample data: `RIFF`, exact file-size-minus-eight, `WAVE`, a `fmt ` chunk of size 16 with PCM format 1, one channel, sample rate 16000, byte rate 32000, block alignment 2, 16 bits per sample, then one `data` chunk whose even byte length exactly equals the remaining file bytes. Extra chunks, alternate header sizes, compressed/extensible formats, metadata, trailing bytes, odd sample data, and inconsistent length/rate/alignment fields are invalid.

Local Whisper SHALL validate that exact container twice: in main before worker submission and in the worker before conversion/inference. The request SHALL contain 1 through 28,800,000 samples and no more than 57,600,044 total WAV bytes (30 minutes). Main SHALL derive sample count and duration from the canonical header/data length, reject a mismatching external duration, and declare exactly that total on the protocol. Both peers SHALL reject zero samples, one-sample-over-limit, total/chunk sum mismatch, duplicate/omitted terminal chunks, and cancellation residue as `AUDIO_FORMAT_UNSUPPORTED` or the applicable protocol/cancellation failure without inference. The `whisperCpp` adapter MAY convert validated PCM16 samples to its private in-memory representation using checked sizing; it may not add a new recording format, silently reinterpret compressed data, or create a temporary audio file. Audio and prompt buffers SHALL be released on success, cancellation, failure, provider switch, and shutdown.

**PRIV-001, PRIV-002** Routine logs, audit records, diagnostic capture, crash reports, process titles, and download telemetry SHALL NOT contain:

- raw or encoded audio;
- transcript or partial transcript text;
- the initial prompt or language vocabulary;
- raw worker stdout/stderr or exception messages;
- command lines, environment variables, usernames, or full paths;
- full URLs, request headers, device serials, GPU UUIDs, or arbitrary native structures.

Metadata-only audit MAY include operation ID, engine, target, backend, logical model/runtime revision IDs, byte counts, durations, state transitions, support tier, and typed failure code. An explicit user-generated diagnostics export MAY include reviewed vendor/device IDs and driver/runtime versions, but never serial numbers, full hardware UUIDs, model/runtime bytes, partial downloads, private text, or raw child output.

**DIAG-002, PRIV-003** The app-generated diagnostics archive SHALL advance to schema version 2 when Local Whisper snapshot support is written, while readers/analyzers SHALL continue accepting the existing schema version 1 contract. Schema v2 adds at most one `local-whisper/snapshot.json` member, includes its exact byte length and SHA-256 in `manifest.json`, declares `localWhisperSnapshot: 1` in the manifest schema-version map, and caps the member at 64 KiB. Absence means the producer had no Local Whisper snapshot capability; it SHALL NOT be interpreted as Ready, Unsupported, or an empty inventory.

The snapshot SHALL be one exact canonical JSON object containing only capture/schema version, support/setup/capability/residency/activity/operational states, engine/target/backend and logical runtime/model/artifact IDs, selected artifact states and bounded counts, stable failure codes, and reviewed driver/runtime versions. It MAY contain normalized non-unique numeric PCI/Vulkan vendor and product device IDs and a sanitized bounded device display label. It SHALL NOT contain the application opaque device ID, native handle/index, PCI bus/domain/function or instance path, subsystem IDs, serial, GPU UUID, topology or registry fingerprint, authority ID/salt/proof, allocation address, full native structures, command/environment/process data, path/URL, user/private text, or artifact bytes. Unknown fields, oversized values, duplicate keys, or an invalid member hash invalidate that snapshot without relaxing the rest of the archive parser.

**DIAG-003** Local Whisper provider audit extensions SHALL remain a closed additive schema. Allowed operations are `local-runtime-check`, `local-artifact-transfer`, `local-artifact-remove`, `local-model-load`, `local-model-unload`, the existing `transcribe-batch`, `recovery`, and `shutdown`; allowed phases are existing `configuration`, `readiness`, `model-lifecycle`, `process`, `result`, `cleanup`, and `shutdown`. Local-only metadata keys/types SHALL be enumerated for engine, target, backend, artifact kind, logical model family, logical runtime/artifact revision, support/setup/capability/residency state, bounded byte count/duration, and stable failure code. No free-form native value is accepted, and audit sink/validation failure remains fail-open without changing provider behavior.

Worker crash/core dumps SHALL not be automatically collected or uploaded. The application SHALL not claim a cross-platform OS network sandbox; instead, the worker contract contains no network/listening feature and qualification SHALL observe zero worker/main inference egress.

Trusted IPC SHALL accept only catalog IDs and validated setting values. Main resolves every path, executable, origin, hash, and argument from authenticated catalogs and repositories. Untrusted senders and forged identifiers SHALL fail before filesystem or process action.

## 17. Persistence, Migration, Rollback, and Recovery

### 17.1 Settings persistence

The Local Whisper provider settings SHALL live in a dedicated, versioned private JSON file owned by a main-process repository, following existing provider-specific settings precedents. It SHALL contain only normalized configuration IDs/values and no operational `Ready`, residency, worker PID, raw device serial, URL, or filesystem authority.

Opening the feature for the first time SHALL create defaults in memory only. The settings file is created on an explicit save. Migration SHALL not download, probe deeply, load, move, convert, or delete an artifact.

Invalid known values SHALL produce field/repair reasons. Defaults may repair only non-material presentation/request fields. An invalid engine identity SHALL remain Not ready until an explicit reset restores fixed `whisperCpp`; an invalid target, backend, device, runtime, or model identity SHALL remain Not ready until an explicit user choice or reset. Unknown fields within a supported schema version SHALL be ignored and preserved across a write. A newer unsupported schema version SHALL be opened read-only with `SETTINGS_VERSION_UNSUPPORTED` and SHALL not be overwritten except by an explicit reset.

An explicit Local Whisper settings reset SHALL confirm, reject active transcription, unload the worker, replace only the provider settings with documented defaults, and clear the initial prompt. It SHALL not delete installed runtime packs, models, or resumable downloads.

### 17.2 Inventory and startup

Inventory SHALL be reconstructed from authenticated managed manifests, locks, and filesystem evidence. `Ready` and `Loaded` are never persisted as truth. Every process starts `Unloaded`; prior capability evidence may be displayed only as historical until the exact current process revalidates it.

Startup recovery SHALL:

- classify interrupted staging journals as safely resumable or removable;
- mark changed/corrupt/blocked installed artifacts unusable;
- detect an orphan worker from the prior process only through safe process-ownership evidence and terminate only a proven owned child;
- never delete unknown files or choose another artifact automatically.

### 17.3 Upgrade and rollback

**COMP-010** Downgrade recovery SHALL use the verified manual known-provider path below; an unknown previous-binary outcome is not acceptable release evidence.

After app upgrade, incompatible runtime packs remain stored but unusable. The selected configuration becomes `Runtime incompatible/missing` until the user explicitly downloads/selects a compatible pack. A model may be reused only when its exact hash/format remains listed compatible.

A downgraded app SHALL not be expected to execute newer packs. New Local Whisper settings/artifacts remain in their dedicated namespaces so an older app does not rewrite or delete them.

The immediately preceding application version currently preserves an unknown persisted provider string, rejects provider construction, and remains Not ready while keeping its known-provider chooser available. Before downgrade, the documented procedure SHALL select a provider known to the older version; if already downgraded with `local-whisper` selected, its chooser SHALL select a known provider. The older app SHALL not execute or delete Local Whisper artifacts/settings.

Deterministic current-code compatibility fixtures SHALL verify the legacy provider-registry/chooser contract during ordinary implementation. A real immediately preceding packaged binary, exact version/hash, nonprivate fixture profile, downgrade and chooser recovery MAY be run as an optional extended diagnostic, but it is not an alpha or final release gate. Any published rollback guidance SHALL remain exact-version-bound and shall not claim unknown behavior as verified.

No uninstall or upgrade flow SHALL silently promise preservation or deletion beyond the project's documented application-data policy.

## 18. Packaging, Publishing, and Maintenance

### 18.1 Native source acquisition and disconnected builds

**SEC-009, PKG-006** Automatically generated GitHub `Source code (zip)` and `Source code (tar.gz)` outer bytes SHALL NOT be a source identity, reproducibility root, or sole trust anchor. GitHub documents that their compression/byte layout may change while commit-addressed extracted contents remain the same. Refreshing such a checksum is not routine dependency maintenance and SHALL NOT unblock a build automatically.

The initial `whisperCpp` source inputs SHALL remain:

- `ggml-org/whisper.cpp` commit `f049fff95a089aa9969deb009cdd4892b3e74916`, Git tree `f49541eaed447bce9b5e3598cc7a487ce5e54678`;
- `nlohmann/json` commit `55f93686c01528224f448c19128836e7df245f72`, Git tree `1eb780542e829bf1615828ed0d5f407497bbce7b`.

The initial `whisper.cpp` source object is the complete pinned tree: 1,882 paths, 36,382,209 expanded regular-file bytes, 39 executable-mode files, no symlinks, no gitlinks/submodules, and no Git LFS pointers. A materializer SHALL force inclusion of tracked files even when upstream `.gitignore` matches them; reconstructing the imported content with forced Git staging SHALL reproduce tree `f49541eaed447bce9b5e3598cc7a487ce5e54678`.

The initial reviewed `nlohmann/json` source object MAY be the exact two-file subset below rather than the complete tree, but only with its own subset identity and excluded-tree provenance:

- `single_include/nlohmann/json.hpp`: Git blob `82d69f7c5d044c9887c96b90c97f5639083ecd14`, 953,436 bytes, SHA-256 `aaf127c04cb31c406e5b04a63f1ae89369fccde6d8fa7cdda1ed4f32dfc5de63`;
- `LICENSE.MIT`: Git blob `a1dacc8dbbd907c4b622ff1f08e279c27465dcbc`, 1,076 bytes, SHA-256 `46a65cffd1ea955132d95a8dd921640714a8d6b537d2e4e482d31145ae95b603`.

Mixing the subset identity with a full-tree identity, silently adding another file, or omitting either reviewed file is invalid. A complete-tree import remains valid only when its complete canonical manifest is independently pinned and the build consumes the same reviewed header/license content.

A different repository, commit, tree, source subset, signature/key result, license, or patch series requires an explicit reviewed pin revision. Tags and version labels are descriptive only; no import/build path may resolve a branch, tag, `latest`, or moving dependency.

Networked source import SHALL be a separate explicit operator/release action, never a CMake configure/build side effect. It SHALL use an allowlisted repository/origin, fetch the exact full commit, prove the commit and tree above, run strict Git object validation, record commit/tag signature results and pinned key fingerprints where available, reject unpinned gitlinks/submodules and LFS pointers, and export only from verified Git objects. A signature result is provenance evidence, not a substitute for the pinned commit/tree/content identities. The importer SHALL never rewrite a pin from observed upstream data.

Each dependency SHALL then be materialized as a canonical first-party source object in an explicit local content-addressed store or a reviewed project-controlled immutable snapshot. Its lock SHALL contain:

- upstream repository, exact commit and Git tree;
- source-object schema/version, root prefix, byte size, and SHA-256 when a transport object exists;
- one canonical bytewise-sorted manifest of normalized relative path, entry type, executable/non-executable mode, and regular-file SHA-256 or explicitly permitted safe relative symlink target;
- the canonical manifest SHA-256, expected path/file count, expanded-byte limit, license path/hash, provenance, and signature result;
- ordered patch file SHA-256 values, patch application rules, and canonical patched-tree manifest SHA-256;
- importer image/tool versions and every compiler/SDK/build-tool input identity.

Import/extraction SHALL use a fresh bounded temporary root, reject absolute/drive/UNC/traversal paths, duplicates and case-fold collisions, special files, hard links, escaping links, unexpected modes, overwrite, undeclared files, expanded-size/count overflow, and identity changes, then atomically promote only the exact verified source object. Two imports in the same pinned importer SHALL produce the same canonical manifest and source-object identity. Licenses and provenance are mandatory inputs, not optional documentation files.

Normal configure, compile, test, and pack assembly SHALL accept only explicit verified local source roots/objects and SHALL run with network access denied from the first configure. Every upstream source directory and build dependency SHALL be supplied directly; `FetchContent`, `ExternalProject`, package-manager, clone, URL, model-download, and ambient user toolchain resolution SHALL have no network-capable path. Merely setting `FETCHCONTENT_FULLY_DISCONNECTED` without explicit source overrides is insufficient. Configuration SHALL fail if a source object/manifest/license/patch/toolchain lock is absent or mismatched, if an upstream option could fetch content, or if the generated build graph records a URL/Git/download step. Backend SDKs and companion libraries are separate pinned inputs under the same provenance/offline rule.

**PKG-010** Each backend-specific production build SHALL record an explicit, reviewed option set rather than inherit upstream host-detection defaults. `whisperCpp` production builds SHALL set `WHISPER_CURL=OFF`, `WHISPER_BUILD_EXAMPLES=OFF`, `WHISPER_BUILD_TESTS=OFF`, `GGML_BUILD_EXAMPLES=OFF`, `GGML_BUILD_TESTS=OFF`, `GGML_NATIVE=OFF`, `GGML_BACKEND_DL=OFF`, `GGML_CPU_KLEIDIAI=OFF`, `GGML_CUDA_CUB_3DOT2=OFF`, and `GGML_CUDA_NCCL=OFF`; explicitly disable every non-selected accelerator backend; set `FETCHCONTENT_FULLY_DISCONNECTED=ON` in addition to supplying every source locally; pin the `GGML_OPENMP` decision and runtime when enabled; and set explicit `CMAKE_CUDA_ARCHITECTURES` or `AMDGPU_TARGETS` rather than `native`. Exactly one accelerator backend may be linked into an accelerator worker. Project-owned tests remain separate from the production upstream test option. CUDA packs SHALL pin the toolkit/compiler compatibility pair and stage only the reviewed redistributable runtime closure; Vulkan builds SHALL pin the loader, headers, SPIR-V inputs, generated-shader target, and `glslc` used at build time, while excluding the SDK/compiler from runtime packs; HIP builds SHALL pin the non-static shared-library closure. A CMake install result, successful link, or build-host execution is not dependency-closure evidence: each pack SHALL pass expected-file, dynamic-library, relocation, malicious-working-directory/environment, and clean-machine startup checks before catalog inclusion.

The reviewed upstream source SHALL remain unmodified. Project changes SHALL be the smallest checked-in ordered patch series, applied only after source verification; both original and patched canonical identities SHALL be retained in provenance and the SBOM. Native implementation SHALL NOT begin from a newly observed transport until the source object, negative verifier tests, licenses, and disconnected first-configure gate pass.

### 18.2 Runtime-pack publishing and maintenance

**PKG-009** Base installers SHALL contain shared integration code, immutable catalog resources/stubs, and exactly the two small app-owned native helper roles already defined by the architecture: the process-owned filesystem guard that holds/revalidates managed artifact authorities and the operation-scoped platform launcher that establishes process-tree ownership and maps one-use model authority into the worker. Their authority-broker behavior is only the bounded **SEC-011** handoff between those roles; it SHALL NOT add a third daemon or persistent/listening service, and the guard's one-use Windows endpoint remains the sole addressed local-control exception. Both helpers SHALL be built for the exact package platform, placed outside ASAR, hash/manifest-verified, and unavailable on macOS in this release. Inference-engine workers, CUDA/cuDNN/ROCm/Vulkan engine libraries, and model artifacts remain on-demand and SHALL NOT enter the base installer.

Every runtime pack SHALL be built by protected release automation from pinned inputs and publish:

- reproducible build provenance where supported;
- engine/protocol/app compatibility metadata;
- exact component/SBOM and license inventory;
- backend/driver/ISA prerequisites;
- signatures and hashes verified before catalog inclusion;
- the full expected-file manifest.

The runtime/model catalog is immutable per app release. Project-built runtime packs SHALL use the project-controlled HTTPS GitHub Release origin in Section 12.1. Model bytes SHALL remain at the pinned public upstream Hugging Face source; the project SHALL NOT copy them into GPT-Voice releases solely for distribution. Signing private keys SHALL not be stored in the repository or delivered to clients. Public verifier keys, key IDs, rotation policy, and blocked revisions SHALL be reviewable.

Redistribution review is mandatory for every component physically included in a runtime pack, including CUDA/cuBLAS/cuDNN, ROCm/HIP, Vulkan, and `whisper.cpp` components. A separate legal/provenance review is mandatory for app-catalog-facilitated public model downloads and their notices even though GPT-Voice does not host those bytes. MIT metadata does not remove component notice, provenance, trademark, model-source, or vendor redistribution/facilitation obligations.

Documentation SHALL cover optional setup, supported/Preview/Planned distinctions, exact external driver/permission prerequisites, storage/disk impact, downloads, validation, model/runtime update and deletion, VRAM/RAM load/unload, offline inference, privacy, typed troubleshooting, AMD's untested Preview boundary, and macOS's unavailable skeleton. The privacy text SHALL explicitly state that “local inference” prevents audio/prompt inference egress but does not disable GPT-Voice's existing successful-result behavior: transcript text may still be copied to the clipboard and persisted in local transcription history, and the short-lived cache still applies under its existing policy.

### 18.3 Candidate artifact production and trust promotion

**PKG-002, PKG-003, PKG-004, QUAL-004** Candidate tooling SHALL be deterministic and evidence-producing:

- build each CPU/CUDA runtime pack only from Section 18.1 locks in a clean network-denied build phase; generate exact expected-file manifests, component SBOM, notices, provenance, and deterministic `restricted-tar-gzip-v1` bytes;
- repeat each runtime build in an independent clean root and require identical source manifest, installed-file manifest, archive length, archive SHA-256, and artifact-signature input digest before candidate admission;
- consume each selected upstream model through `pinned-raw-model-v1`; record the exact Hugging Face repository commit/file, source length/SHA-256, model-card/license metadata, app catalog signature, and installed-file identity. No project model conversion, archive production, or model upload step exists;
- when an optional nonblocking diagnostic profile is run, generate its qualification catalog/keyring/package from the frozen candidate record, never from a mutable working directory or moving branch. Qualification private key material remains outside repository/evidence and is destroyed or revoked after use; public key/catalog/package digests remain diagnostic evidence only;
- after implementation readiness and protected production trust/legal inputs are available, freeze the release pull-request head and expected tag value, generate the production catalog/keyring, build and natively sign the application candidates, sign the exact runtime packs and release manifest, and make those immutable candidate bytes the sole input to release staging. Alpha platform smoke later consumes the exact public final-origin objects; final lineage instead consumes latest-alpha smoke evidence plus the allowlisted final delta and reproducibility records.

Provenance, SBOM, notices, legal review, and signing evidence SHALL be privacy-safe and checksum-linked. A missing report, unresolved license/component, stale source URL, mutable Hugging Face revision, unsigned catalog, failed reproducibility comparison, unavailable runtime asset, or unapproved production trust input is a release blocker; it SHALL NOT be converted into synthetic `Passed` evidence. Alpha publication, each post-release platform-smoke result, feedback transition, and final publication are distinct states.

### 18.4 CI/CD and immutable release delivery

**CI-001, CI-002** Hosted Linux and Windows CI SHALL own production-equivalent builders and verifiers for all six release outputs: the Linux and Windows application installers, Linux and Windows CPU runtime packs, and Linux and Windows RTX 50 `sm_120a-real` runtime packs. Native workers and accelerator libraries remain outside the base installers. Each runtime builder SHALL consume only the pinned, disconnected Section 18.1 inputs and explicit platform/toolchain profile. It SHALL emit the exact archive, expected-file manifest, SBOM, notices, provenance, compatibility metadata, and signature input required by Sections 12 and 18.3. A physical GPU is not required to compile or structurally verify a CUDA pack; physical execution remains independently gated.

**CI-003** Pull-request and main-branch workflows SHALL run deterministic source/profile/schema checks, real CPU and RTX 50 native compilation for both supported operating systems, runtime-pack reproducibility and malicious-environment checks, application build/package checks, fixture isolation, typecheck, tests, lint, formatting, dependency audit, and release-workflow policy tests. These workflows SHALL use read-only repository permissions and synthetic or non-production trust only. They SHALL NOT receive production signing or publication secrets; modify or push source commits; create or move tags; create releases; upload user-installable assets; or claim hardware qualification.

**CI-004, REL-002** Production work SHALL run only for one immutable reviewed release-branch head and one matching SemVer tag through a protected, explicitly approved release workflow. A dedicated release pull request into `main` SHALL own the complete committed release preparation and SHALL remain blocked until every applicable deterministic/reproducibility gate, redistribution/legal approval, and required reviewer approval is bound to that exact head and candidate generation. Alpha publication does not wait for physical platform smoke; **AC-MAN-017** and **AC-MAN-018** run afterward against its public final-origin assets. Final preparation additionally requires the latest alpha's two passing smoke records, explicit final transition, and allowlisted final-only delta; stable publication later requires the complete **AC-MAN-021** lineage. Ordinary feature pull requests, branch pushes, reusable-workflow callers, unreviewed manual dispatches, and a release pull request with mutable or incomplete preparation SHALL be unable to enter the production environment, merge, tag, or publish.

**CI-005** Separate Linux and Windows post-release alpha workflows SHALL execute **AC-MAN-017** and **AC-MAN-018** only after manual approval of trusted test hosts. They consume the already-public immutable alpha assets read-only, are isolated from pull requests and forks, scrub task-local state after execution, expose no production signing/publication secret, and hold no `contents: write` or release authority. They do not block that alpha's publication, but both results are mandatory before feedback may select final; either failure allows only a fresh sequential alpha. Optional all-six-model/performance diagnostics remain nonblocking and cannot replace the bounded alpha smoke records.

**CI-006** Production signing SHALL use one protected GitHub Environment with required reviewers, environment-scoped least-privilege secrets, fork and pull-request exclusion, auditable approvals, and documented rotation/revocation. Final Windows installers SHALL be Authenticode-signed; applicable Linux packages SHALL receive their native package signature; catalogs and runtime packs SHALL retain their independent artifact signatures; and the release manifest SHALL be signed. The signing job SHALL disclose no private key, token, certificate secret, or sensitive signer response in logs or artifacts.

**CI-007, DIST-004** One approved application GitHub Release tag SHALL contain the compatible application installers and separate CPU/RTX 50 runtime-pack assets for both supported operating systems, together with their required signatures, signed release manifest, catalogs, checksums, SBOMs, notices, and provenance. Every production catalog entry SHALL bind its same-tag runtime asset by exact name, length, SHA-256, signature, platform, architecture, target, app revision, and immutable release URL. End-user installation SHALL reject every origin outside this approved release except the exact pinned Hugging Face model objects in Section 12.1.

The six release outputs are logical classes, not a six-file public inventory. The Linux application class expands to every configured x64 release format (`AppImage`, `deb`, and `rpm`); the Windows application class expands to the configured x64 NSIS installer; and the four runtime classes each expand to one independently installable `restricted-tar-gzip-v1` archive for Linux/Windows CPU and Linux/Windows `sm_120a-real`. The release also SHALL publish or embed, as defined by the signed manifest, every detached/native signature, production catalog and public keyring input, checksum set, SBOM, notice, provenance record, and compatibility record required for clean installation and final-origin verification. A release is incomplete if either platform smoke would need a CI artifact, qualification-purpose pack, local substitution, unpublished catalog/signature, disabled packaging state, or any installable binary outside the same tag. Static workflow shape, validator success, or synthetic metadata never proves these bytes exist.

**REL-003, REL-004** The protected release workflow SHALL recognize the ordered target family `v2.4.0-alpha.N` followed eventually by final `v2.4.0`. The first prerelease target is `v2.4.0-alpha.1`; every later alpha target increments the latest publicly deployed alpha number by exactly one, and skipped, repeated, zero, negative, noncanonical, or post-final alpha identities fail closed. Each target has its own release branch, committed version/changelog inputs, newly built signed six-output candidate set, signed manifest/catalogs, tag, GitHub Release, final-origin verification, and deployment authorization. Every alpha GitHub Release is public and marked prerelease before its Linux and Windows smoke begins. A later-alpha workflow rejects execution until the immediately preceding alpha is deployed, both platform results are recorded, and feedback selects the next-alpha path; failure may select only that path. The final workflow rejects execution until both latest-alpha smokes pass and feedback selects final, and it rejects every attempt to reuse alpha candidate bytes as final bytes.

**CI-008, PKG-011, PKG-012, QUAL-004** The protected workflow SHALL build and sign candidates from the frozen release pull-request head. For final, it SHALL prove that the source differs from the latest passing alpha only by the allowlisted final identity/changelog/tag-derived preparation and required release metadata, repeat deterministic builds in independent clean roots, and reject every product/runtime/packaging/compatibility delta. After automated, reproducibility, signing, legal, and reviewer gates pass, the pull request may merge only by a method that preserves the frozen head unchanged and reachable from `main`; squash, rebase, conflict-resolution mutation, or any new release-branch commit after candidate freeze invalidates the generation. Publication promotes the same bytes without rebuild, re-signing, timestamp replacement, repackaging, catalog regeneration, or metadata mutation. A filename collision, existing asset, tag/source mismatch, digest mismatch, stale approval, rewritten head, unapproved delta, reproducibility mismatch, or post-freeze byte change fails closed; asset clobbering is prohibited.

For `v2.4.0-alpha.1`, the protected `publish=false` versioned candidate run
freezes the complete release-target inventory for the exact release-PR head.
After the preserving merge proves that head reachable from `main`, the
`publish=true` promotion run SHALL identify that prior run by immutable run ID,
revalidate repository/workflow/conclusion/source SHA and candidate descriptor,
download its private artifacts, and publish those same bytes. Every
construction, signing, application packaging, runtime build, attestation, and
candidate-assembly job SHALL be skipped during promotion. A changed source SHA
invalidates the candidate and requires a new release PR and versioned candidate
before another promotion.

The release workflow SHALL preserve one and only one guarded tag-and-publication path while production-pipeline construction is implemented and verified. Its publication input defaults to `false`; every candidate construction, attestation, and inventory job remains read-only and nonpublishing. The publication job alone may receive `contents: write`, SHALL depend on the verified complete candidate, SHALL require an explicit release tag matching that candidate, and SHALL reject any existing tag or GitHub Release before creating immutable release state. Task 32 verifies this path with publication disabled. Task 33 explicitly authorizes and enables it for the real public alpha. Removing the later capability is not a valid implementation of the earlier nonexecution boundary.

Before public state exists, the private exact candidate and descriptor are the
recoverable non-installable staging authority. The protected publication job
first revalidates that candidate, then creates the absent tag and complete
prerelease without clobbering. In the same job it downloads every public asset
through its canonical GitHub Release endpoint, streams and verifies exact
length/digest plus manifest inventory and release identity, seals
`deploymentDigest`, and preserves bounded deployment evidence. Physical
install/runtime smoke is not part of this workflow: it occurs separately in
Tasks 34/35. Before tag/release creation, a failed attempt may repair forward
and construct a new exact-SHA candidate. Once either the tag or release becomes
public, alpha.1 is immutable; any upload or final-origin verification failure
blocks the process and requires a separately planned sequential alpha. It never
overwrites or deletes public state.

### 18.5 Release branch, pull request, and tag lifecycle

**CI-004, CI-008, REL-002** A release attempt SHALL use one dedicated release branch whose frozen head contains every tracked source and release-preparation change. Before candidate freeze, maintainers SHALL:

- review every registered manual release, packaging, signing, final-origin, post-release Linux alpha-smoke, and post-release Windows alpha-smoke script applicable to the release and verify that the registry contains no stale, missing, duplicate, unbounded, or cross-generation command;
- update the repository changelog with the candidate version's user-visible changes, compatibility scope, known limitations, security/privacy or migration effects, and release blockers resolved or intentionally retained;
- update every canonical version-bearing file and tag-derived catalog, manifest, compatibility, package, and release-note input through one deterministic version manifest, with no inconsistent or ambient version source;
- commit the complete bounded preparation on the release branch and prove the working tree clean before freezing the head, source tree, SemVer, expected tag, builder inputs, candidate generation, and manual-script identities;
- open or update one release pull request into `main` whose required checks compare the committed changelog, version manifest, expected tag, source head, signed candidate manifest, and manual evidence identities.

Release automation SHALL validate committed version data; it SHALL NOT silently rewrite tracked version or changelog files only inside a build, signing, merge, tag, or publication job. Any release-branch source, dependency, version, changelog, tag-derived input, manual script, candidate, or evidence change after freeze SHALL create a new head and require a new signed candidate generation plus every affected automated and manual gate.

The release pull request SHALL be current with its protected `main` base before freeze. It SHALL NOT merge until its deterministic, reproducibility, signing, legal, reviewer, and release-preparation gates pass. An alpha has no pre-merge physical platform gate. A final additionally requires both latest-alpha post-release smokes, explicit final selection, and an allowlisted final-only source delta; it has no physical test of final bytes. The merge preserves the frozen pull-request head as an unchanged commit reachable from `main`; squash and rebase merging are prohibited for release pull requests. A conflict, required base update, or any content change returns the attempt to release preparation and invalidates prior candidate/evidence identities rather than weakening the gate.

Only after the frozen release head is reachable from `main` SHALL the protected release workflow create the final immutable `v<SemVer>` Git tag, pointing to that exact head. The workflow proves the tag did not exist, the tag value matches every committed version and candidate identity, and the tagged commit equals the frozen release pull-request head. It never creates the tag before merge, moves or reuses a tag, tags a replacement merge commit, or publishes from a different commit. Approval of a release pull request alone grants no tag or publication authority. For alpha.1 only, the explicit version-scoped Watch invocation in `OPS-005` grants the complete closed external sequence without repeated phase approvals; all other releases retain their own explicit authority gates.

**OPS-005, REL-005** The exact invocation
`$watch-process scenario=local-whisper-alpha-release timeout=6h` identifies one
logical target, `swimmwatch/gpt-voice@v2.4.0-alpha.1`, and one shared six-hour
deadline. It may finish Task 32 with `publish=false`, repair allowed files,
create atomic commits and normal pushes, merge the exact feature PR by merge
commit, create and review `release/v2.4.0-alpha.1`, build the versioned
candidate, preserve the exact release head through a merge commit, approve the
existing `local-whisper-production` environment, and dispatch the protected
promotion that creates the tag and prerelease. Dispatch, PR, approval, and
merge operations use durable correlation/remote reconciliation so an ambiguous
response cannot create a duplicate.

That authority excludes force-push, amend, rebase, squash, tag/release/asset
overwrite or deletion, repository-setting changes, deploy, platform smoke,
Tasks 34/35, unrelated files, and every other repository/version/branch/
workflow/environment. External changes, authentication expiration, exact-SHA
conflicts, or ambiguous remote state block safely. The Watch succeeds only
after the protected publication workflow is green and the public alpha.1
prerelease identity is revalidated, then stops without downloading, installing,
or smoke-testing artifacts.

For this release sequence, the preceding lifecycle applies first to `release/v2.4.0-alpha.1` and tag `v2.4.0-alpha.1`. After each alpha is public and final-origin verified, Linux and Windows run independent bounded smoke against that exact tag. Feedback review then chooses exactly one transition: create the next sequential `release/v2.4.0-alpha.(N+1)` containing every accepted fix, or proceed to `release/v2.4.0` only when both smokes pass and no accepted product change requires another alpha. Either smoke failure forbids final. Each later alpha restarts release construction, deployment, and both post-release smokes from its own identity. The final release branch may add only allowlisted final preparation and restarts deterministic/reproducibility/signing/staging gates without physical platform testing. Publishing any alpha grants no authority over a later alpha or final release.

## 19. Acceptance Criteria

### 19.1 Deterministic automated acceptance

| ID          | Test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Required result                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-AUTO-001 | Normalize a never-configured Local Whisper settings object.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Produces `whisperCpp`, `gpu`, `base`, app-pinned runtime/model recommended revisions, `auto` language, empty prompt, `temperatureHundredths = 0`, `greedy`, and `auto` CPU threads without starting a deep probe, download, worker, or allocation. Backend/device follow the deterministic 0/1/N-device rules.                                                                                                                                           |
| AC-AUTO-002 | Round-trip every valid settings union through persistence, sanitized renderer view, and the `whisperCpp` worker mapping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Canonical values survive exactly; privileged paths/URLs/argv are absent; inactive controls are absent from worker requests.                                                                                                                                                                                                                                                                                                                              |
| AC-AUTO-003 | Submit minimum, maximum, and malformed prompt, temperature-hundredths, beam, best-of, thread, enum, language, revision, and device values directly over IPC.                                                                                                                                                                                                                                                                                                                                                                                                                            | Valid boundaries pass; every non-safe-integer, fractional integer, off-grid, out-of-range, forged, unknown, or cross-field-invalid value is rejected by main without partial persistence. A 1,001-code-point prompt is rejected, not truncated.                                                                                                                                                                                                          |
| AC-AUTO-004 | Render every target/backend/strategy state at normal and minimum window dimensions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Field visibility exactly matches Section 8; the fixed `whisperCpp` engine value and all controls/errors/actions remain reachable and labeled; Advanced, progress, disabled reasons, and support badges are keyboard/screen-reader perceivable.                                                                                                                                                                                                           |
| AC-AUTO-005 | Exercise the complete mocked support matrix, including NVIDIA, Windows AMD Vulkan, exact/unlisted Linux AMD HIP, AMD Vulkan, Intel, CPU, macOS arm64, and unsupported architectures.                                                                                                                                                                                                                                                                                                                                                                                                    | Only specified combinations are actionable; tiers and reasons match Section 6; unlisted HIP and unsupported devices fail closed.                                                                                                                                                                                                                                                                                                                         |
| AC-AUTO-006 | Persist a selected GPU, then remove it while another eligible GPU remains.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | The old opaque ID remains selected and unavailable. No device/backend/target changes automatically.                                                                                                                                                                                                                                                                                                                                                      |
| AC-AUTO-007 | Change every load-affecting field around a fake resident worker and every request-affecting field while idle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Load-affecting changes unload before epoch activation and stale capability/readiness; request-only changes do not reload and affect only the next captured request.                                                                                                                                                                                                                                                                                      |
| AC-AUTO-008 | Enumerate the `whisperCpp` model catalog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | It exposes only the six logical multilingual families. Native `ggml` artifacts are immutable; Distil/English-only/VAD/timestamp/translation/diarization entries are absent.                                                                                                                                                                                                                                                                              |
| AC-AUTO-009 | Add a newer catalog runtime/model while an older revision is selected and installed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `Update available` appears; no background download, selection change, unload, or old-artifact deletion occurs.                                                                                                                                                                                                                                                                                                                                           |
| AC-AUTO-010 | Run independent CUDA failure fixtures for vendor/device mismatch, driver, compute capability, dependency, allocation, dispatch, load, warm-up, and actual-device confirmation.                                                                                                                                                                                                                                                                                                                                                                                                          | Each yields its safe Not-ready code and no fallback to CPU, Vulkan, another device, model, or engine.                                                                                                                                                                                                                                                                                                                                                    |
| AC-AUTO-011 | Run Vulkan fixtures for software ICD, Vulkan 1.1/1.2 under the initial 1.3 shader target, generated-target mismatch, missing required feature, allocation/dispatch failure, warm-up failure, and a valid Vulkan 1.3 path.                                                                                                                                                                                                                                                                                                                                                               | Every version/target/feature or execution mismatch is Not ready. Only the exact physical-device and pack-target match may validate, and the valid mocked AMD path remains Preview.                                                                                                                                                                                                                                                                       |
| AC-AUTO-012 | Run Linux HIP fixtures for distro/kernel/amdgpu ABI, ROCm/HIP package/SONAME/build identity, PCI device/`gfx` allowlist, PCIe atomics, system/bundled-library mixing, `/dev/kfd`, render-node permission, allocation, dispatch, load, and warm-up.                                                                                                                                                                                                                                                                                                                                      | Only the exact immutable allowlist and dependency-closure intersection reaches full load; every missing, mixed, or unlisted prerequisite fails closed with a specific safe code.                                                                                                                                                                                                                                                                         |
| AC-AUTO-013 | Run `whisperCpp` CPU fixtures with supported/unsupported OS, architecture, ISA, RAM, thread counts, runtime, load, and warm-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Only the exact passing CPU configuration validates; no GPU worker or allocation is initialized.                                                                                                                                                                                                                                                                                                                                                          |
| AC-AUTO-014 | Test trustworthy resource estimates immediately below, equal to, and above peak plus `max(20%, 512 MiB)`, plus an unavailable-metric fixture.                                                                                                                                                                                                                                                                                                                                                                                                                                           | Below blocks with exact `INSUFFICIENT_*` and no override; equal/above may continue; unavailable warns and may attempt. A later actual allocation/load failure still wins.                                                                                                                                                                                                                                                                                |
| AC-AUTO-015 | Reuse capability evidence after changing every fingerprint component, modifying an artifact, driver/topology/suspend event, and app restart.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Evidence becomes Stale/Unchecked and cannot authorize readiness. Intentional unload alone retains same-process `Validated` evidence but derived provider status becomes `Validated · Unloaded`, not Ready.                                                                                                                                                                                                                                               |
| AC-AUTO-016 | Select Local Whisper under Planned, unsupported, missing, incompatible, corrupt, blocked, known-insufficient-resource, and warm-up-failure states, seed a matching cache entry, then request transcription.                                                                                                                                                                                                                                                                                                                                                                             | Selection persists, state is visible, pre-cache gate fails with the exact typed code, and no cache hit, fallback, clipboard/history/cache mutation, or partial transcript occurs.                                                                                                                                                                                                                                                                        |
| AC-AUTO-017 | Download valid, interrupted, resumed, cancelled, offline, `ENOSPC`, changed-ETag, bad-length, bad-hash, bad-signature, unsafe-redirect, traversal, symlink, hard-link, device-node, unexpected-file, archive-bomb, wrong-protocol, and `restricted-tar-gzip-v1` malformed fixtures. Malformed coverage includes optional/concatenated gzip members, bad gzip headers/trailers, bad ustar identity/checksum/numeric fields, PAX/GNU/base-256/sparse/unknown entries, non-deterministic metadata, reordered/duplicate members, length overflow, and bytes after either terminal boundary. | Only the valid exact artifact is atomically promoted. Invalid staging is non-executable, previous revisions remain intact, every malformed production archive returns a safe typed trust failure, aggregate buffering stays within 32 MiB, and recovery follows Section 12.                                                                                                                                                                              |
| AC-AUTO-018 | Restart with valid resumable, invalid resumable, and abandoned install journals.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Only the exact validator-matching transfer can resume; no staging entry appears Installed; unknown paths are not deleted.                                                                                                                                                                                                                                                                                                                                |
| AC-AUTO-019 | Delete unselected, selected, missing, corrupt, and loaded model revisions, plus deletion during transcription.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Exact managed files only are targeted; loaded deletion unloads; selected deletion preserves `Model missing`; active/conflicting deletion is rejected; no fallback occurs.                                                                                                                                                                                                                                                                                |
| AC-AUTO-020 | Remove runtime packs in equivalent states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Section 13.3 semantics hold and selected removal yields `Runtime missing`.                                                                                                                                                                                                                                                                                                                                                                               |
| AC-AUTO-021 | Exercise `Load now`, lazy load, failed load, successful transcription retention, `Unload`, provider switch, settings reset, and app exit.                                                                                                                                                                                                                                                                                                                                                                                                                                               | Transitions are serialized; only full load/warm-up produces Ready; unload/switch/exit terminate the owned worker; lazy load never downloads.                                                                                                                                                                                                                                                                                                             |
| AC-AUTO-022 | Race transcription, load, unload, delete, runtime replacement, and epoch changes while an unrelated artifact downloads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Worker/file conflicts return `OPERATION_CONFLICT` and are not queued; the unrelated download may continue.                                                                                                                                                                                                                                                                                                                                               |
| AC-AUTO-023 | Run two app instances against download, promotion, deletion, and load locks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The same artifact is never promoted/deleted/loaded concurrently; losing instance reports a safe conflict.                                                                                                                                                                                                                                                                                                                                                |
| AC-AUTO-024 | Inject handshake mismatch, malformed/oversized/out-of-order frames, stdout flood, each stage timeout including a worker hung during inference, crash during load/inference, failed graceful free, hung exit, and parent stream closure.                                                                                                                                                                                                                                                                                                                                                 | Output remains bounded, exact timeout stage is returned, child tree is gracefully then forcibly cleaned up as needed, and no orphan/listener/fallback/partial result remains.                                                                                                                                                                                                                                                                            |
| AC-AUTO-025 | Invoke privileged IPC from an untrusted sender with forged filesystem, URL, executable, hash, device, and artifact inputs.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Trusted-sender and catalog validation reject every call before privileged effect.                                                                                                                                                                                                                                                                                                                                                                        |
| AC-AUTO-026 | Capture settings files, logs, audit, diagnostics, process argv, crash handling, and network activity during successful and failed inference.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Prompt persists only in private settings; audio/transcript/prompt/raw output/full paths/serials are absent elsewhere; inference generates zero network requests.                                                                                                                                                                                                                                                                                         |
| AC-AUTO-027 | Exercise Local Whisper success/failure against cache, clipboard, history, audit, notification, and provider-switch fakes.                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Success enters the existing final-text flow once with complete cache context; every non-success has no success-side effect.                                                                                                                                                                                                                                                                                                                              |
| AC-AUTO-028 | Build/run macOS arm64 adapter, catalog, and UI fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `metal` exists in the typed backend union and renders only as Planned/unavailable; runtime and model catalogs/downloads plus spawn/load/transcribe remain impossible. CPU does not bypass the gate.                                                                                                                                                                                                                                                      |
| AC-AUTO-029 | Migrate absent, valid, malformed, future-field, missing-artifact, incompatible-runtime, corrupt-inventory, and downgraded settings fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                            | No migration performs download/load/delete/fallback; dedicated settings/artifacts survive; operational Ready/residency never restore from disk.                                                                                                                                                                                                                                                                                                          |
| AC-AUTO-030 | Inspect base installer and every advertised catalog/pack fixture.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | No model/unrequested accelerator pack is bundled; every entry has exact identity, provenance, sizes, hashes, signature policy, compatibility, licenses, and allowlisted origin.                                                                                                                                                                                                                                                                          |
| AC-AUTO-031 | Apply an app-shipped denylist to an installed selected runtime/model revision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Artifact becomes Blocked and cannot execute/load; files and selection remain; no fallback or silent deletion occurs.                                                                                                                                                                                                                                                                                                                                     |
| AC-AUTO-032 | Run format, lint, application/test type checks, full deterministic tests, production audit, and Windows/Linux production builds.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | All applicable project checks pass without weakening trusted IPC, types, integrity verification, or privacy assertions.                                                                                                                                                                                                                                                                                                                                  |
| AC-AUTO-033 | Submit valid canonical WAV plus malformed, truncated, compressed, wrong-rate, wrong-channel, and wrong-sample-format buffers.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Only mono PCM16/16 kHz WAV reaches the worker; every other input fails as `AUDIO_FORMAT_UNSUPPORTED`, creates no temp file, and has no success-side effect.                                                                                                                                                                                                                                                                                              |
| AC-AUTO-034 | Run `Check compatibility` with missing and installed artifacts, then run `Load now`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Check returns at most `EstimateOnly` and leaves no worker/allocation; only Load now's successful full load/warm-up produces resident Ready.                                                                                                                                                                                                                                                                                                              |
| AC-AUTO-035 | Exercise provider selection/transcription guards for unloaded, missing-artifact, unsupported, invalid-settings, and loaded Local Whisper snapshots.                                                                                                                                                                                                                                                                                                                                                                                                                                     | No state is mapped to browser/API authentication. Eligible unloaded reaches the cache gate and lazy-loads on a miss; missing/unsupported reaches its exact safe failure; structurally invalid/conflicting state blocks `canAttempt`; loaded validated state is Ready.                                                                                                                                                                                    |
| AC-AUTO-036 | Exercise 0, 1, and multiple eligible GPU fixtures and switch every parent field away and back across new and previously saved selection keys.                                                                                                                                                                                                                                                                                                                                                                                                                                           | Zero/multiple leaves device unset; exactly one initializes deterministically; unseen children use documented defaults once; returning restores prior values, including missing/unavailable choices; failures and catalog updates never rewrite them.                                                                                                                                                                                                     |
| AC-AUTO-037 | Table-test every Section 15 condition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Code, retryable flag, recovery action ID, and resulting support/setup/capability/residency states match exactly; no case collapses to authentication.                                                                                                                                                                                                                                                                                                    |
| AC-AUTO-038 | Render and invoke model/runtime actions in Missing, Downloading, Resumable, Verifying, Installing, Installed, Update available, Corrupt, Blocked, Delete failed, and unknown-directory states for selected/unselected revisions.                                                                                                                                                                                                                                                                                                                                                        | Actions and confirmations match Section 13; corrupt/blocked managed revisions can use proven quarantine removal, while missing/unknown/unprovable data cannot trigger broad deletion.                                                                                                                                                                                                                                                                    |
| AC-AUTO-039 | Seed cache hits for eligible loaded, eligible `Validated · Unloaded`, missing, corrupt, blocked, incompatible, unsupported, absent-device, and known-insufficient states.                                                                                                                                                                                                                                                                                                                                                                                                               | Local pre-cache gate runs first; only eligible states may use cache; eligible unloaded hit performs no allocation; eligible miss runs lazy load.                                                                                                                                                                                                                                                                                                         |
| AC-AUTO-040 | Force-kill the main process with a worker and descendants on Windows/Linux, then restart with stale locks, reused PID, wrong start identity, and forged ownership nonce.                                                                                                                                                                                                                                                                                                                                                                                                                | Job Object/Linux parent-death ownership kills the tree; restart never kills an unrelated process; only full proven identity can be cleaned/recovered; locks become safely stale.                                                                                                                                                                                                                                                                         |
| AC-AUTO-041 | Swap managed paths/files between validation and spawn/delete using symlinks, hard links, Windows junction/reparse points, mount/volume changes, and rename races.                                                                                                                                                                                                                                                                                                                                                                                                                       | Stable anchored identity or quarantine checks reject every escape/race; no unverified executable runs and no path outside the exact managed revision is deleted.                                                                                                                                                                                                                                                                                         |
| AC-AUTO-042 | Suspend/resume and hot-unplug/reset during idle Loaded, Loading, and Transcribing states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Work is cancelled/cleaned within bounds, prior capability becomes Stale, residency becomes Unloaded, devices re-enumerate, and nothing auto-reloads/falls back.                                                                                                                                                                                                                                                                                          |
| AC-AUTO-043 | Use multi-GiB fake streams with slow connect/no-progress, redirect loop, cancellation, hash/extract cancellation, two active plus queued downloads, and UI heartbeat probes.                                                                                                                                                                                                                                                                                                                                                                                                            | Declared time/redirect/concurrency/32-MiB bounds hold; helpers terminate within bounds; no full artifact buffers on main; renderer/main lifecycle remains responsive; nothing partial promotes.                                                                                                                                                                                                                                                          |
| AC-AUTO-044 | Enumerate the complete pinned Whisper.cpp language catalog through the `whisperCpp` adapter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Every canonical ID maps deterministically in the worker and round-trips persistence/UI; incomplete aliases are absent.                                                                                                                                                                                                                                                                                                                                   |
| AC-AUTO-045 | Inspect renderer snapshots and user documentation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Renderer sees only sanitized storage labels/relative location and can request main-owned folder opening without an absolute path; docs explicitly disclose clipboard/history/cache behavior despite local inference.                                                                                                                                                                                                                                     |
| AC-AUTO-046 | Run deterministic compatibility fixtures for the immediately preceding provider registry/chooser contract with `local-whisper` selected and new settings/artifacts present.                                                                                                                                                                                                                                                                                                                                                                                                             | The legacy contract remains Not ready, preserves new namespaces, and recovers to a known provider without executing or deleting Local Whisper data; the fixture records the exact prior contract and does not masquerade as real-binary evidence.                                                                                                                                                                                                        |
| AC-AUTO-047 | Cancel during lazy load and `whisperCpp` inference, switch provider during active work, and exit during active work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Load cancellation terminates to Unloaded; only patched, joined, healthy `whisperCpp` cooperative cancellation may retain Loaded; switch returns conflict without changing provider; exit cancels/forces cleanup within bounds; no partial or late success occurs.                                                                                                                                                                                        |
| AC-AUTO-048 | Inspect built Windows/Linux artifacts, catalogs, and trust text with current unsigned base packaging.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Runtime/model signature verification works under the explicitly trusted-installed-app assumption, while UI/docs/tests make no false base-application code-signing claim.                                                                                                                                                                                                                                                                                 |
| AC-AUTO-049 | Enumerate all six model families before selection, then vary target, backend, runtime, revision, variant, qualified evidence, malformed estimates, and current free memory.                                                                                                                                                                                                                                                                                                                                                                                                             | Every family shows the exact approximate range from Section 8.1.1; the selected configuration shows only its matching estimate/qualified peak; stale, missing, duplicate, unsafe, or unit-invalid records never authorize load; exact peak plus headroom controls blocking and a real load remains authoritative.                                                                                                                                        |
| AC-AUTO-050 | Import every pinned native source twice in the pinned importer, mutate each commit/tree/object/manifest/path/type/mode/license/key/patch/tool field, substitute a valid but byte-different generated GitHub archive, and run a clean first configure/build with network denied.                                                                                                                                                                                                                                                                                                         | Repeated imports produce the same canonical source identities; every identity/provenance/patch mutation fails before compilation; generated archive byte changes neither authorize nor alter source; missing local inputs and every attempted Git/URL/FetchContent/package-manager/model-hub download fail closed.                                                                                                                                       |
| AC-AUTO-051 | Reuse one GPU ordinal across changed registry order/topology, remove durable native identity, substitute authority IDs/fingerprints/proof digests, echo values without activation, return zero/wrong-device weight bytes, change the primary state backend, and exercise exact CPU/GPU fixtures.                                                                                                                                                                                                                                                                                        | Missing durable identity yields `DEVICE_FEATURE_MISSING` and NotReady. Every authority/activation/buffer/state mismatch yields `DEVICE_PROOF_FAILED`, Stale, and Unloaded. Only the exact topology-bound physical-device proof with positive selected-device weight ownership and primary execution succeeds, without fallback.                                                                                                                          |
| AC-AUTO-052 | Swap the managed model identity after verification, omit/replace/multiply the inherited descriptor/handle, make it writable, bind it to the wrong logical slot, alter size/hash, and run valid plus truncated/overflowing custom-loader fixtures.                                                                                                                                                                                                                                                                                                                                       | Only the exact read-only duplicate of the active lease reaches the bounded engine loader; every partial field/read, overflow, out-of-object read, identity mismatch, or path attempt fails before unsafe allocation; changed content returns `MODEL_CORRUPT`, invalid handoff returns `MODEL_AUTHORITY_INVALID`, and every copy closes once before retry.                                                                                                |
| AC-AUTO-053 | Send minimum, 30-minute maximum, zero-sample, one-sample-over-limit, malformed-header, extra-chunk, odd-data, trailing-byte, declared-length mismatch, reordered/duplicate/missing-terminal, cancellation, and canonical WAV fixtures through main and the `whisperCpp` worker.                                                                                                                                                                                                                                                                                                         | Both peers accept only the exact 46..57,600,044-byte canonical envelope, derive the same sample count/duration, remain within bounded buffering, create no temporary audio file, release cancelled/failed bytes, and return `AUDIO_FORMAT_UNSUPPORTED`, protocol violation, or cancellation according to Section 15 without inference.                                                                                                                   |
| AC-AUTO-054 | Exercise probe-only/full-load launches with exact, wrong, writable, replayed, stale, wrong-peer, and extra authorities; inject Linux descriptor-bearing launcher requests, low-fd collisions, missing/forged/duplicate credentials/rights/truncation, wrong launcher/guard PID/start identities, and Windows numeric-handle/logical-slot/bootstrap/job failures.                                                                                                                                                                                                                        | Probe receives no model authority. Linux first accepts exactly one descriptor-free credentialed launcher request, then one same-record guard credential plus rights descriptor and collision-safely maps it to fd `3`; Windows binds one arbitrary inherited `HANDLE` to logical slot `3`, assigns while suspended, resumes, acknowledges the private bootstrap, and only then starts ordinary handshake/parsing; every other case fails before parsing. |
| AC-AUTO-056 | Feed all peers duplicate/deep/wide/oversized JSON, invalid numeric spellings, reordered semantic residency objects, duplicate responses during delayed revalidation, post-terminal frames, and maximum audio conversion/cancellation paths.                                                                                                                                                                                                                                                                                                                                             | Every codec enforces the exact resource grammar; limit/sequence violations return `WORKER_PROTOCOL_VIOLATION`; valid bounded allocation failures return `ALLOCATION_FAILED`; one response settles once; races fail closed; complete audio storage never exceeds 172,800,044 bytes and is released; cancellation is distinct from compute failure.                                                                                                        |
| AC-AUTO-057 | Build `disabled`, `fixture`, `qualification`, and missing/invalid `production` catalog modes; consume the unchanged fixture bundle in independent Linux/Windows package-policy jobs; inspect purpose/key isolation, release collection, and trusted transfer-profile selection.                                                                                                                                                                                                                                                                                                         | Disabled remote-provider packages remain buildable; both smoke jobs verify Task 17 digest `de8603f4c96a793ed3a3d3a03941f44d67592ae945d17d3b19ae0ed56e039226`; fixture, qualification, and production keys/purposes never cross; qualification packages are non-publishable; runtime/model entries select only `restricted-tar-gzip-v1`/`pinned-raw-model-v1`; production without frozen approved inputs stops before collection/publication.             |
| AC-AUTO-058 | Produce diagnostics schema-v1 and schema-v2 archives with absent/valid/invalid Local Whisper snapshots and privacy canaries in every allowed/forbidden hardware, authority, artifact, audio, prompt, transcript, process, and path field.                                                                                                                                                                                                                                                                                                                                               | Existing v1 remains readable; v2 hashes and bounds one snapshot; only the exact sanitized allowlist survives; unknown/oversized/duplicate/private fields invalidate the snapshot and never leak into audit, manifest, analyzer output, or logs.                                                                                                                                                                                                          |
| AC-AUTO-059 | Exercise settings-window and main-window sender substitution, atomic subscription/replay/order, prompt mutations, selected-but-unavailable options, stale epochs, load-affecting save conflicts, the main-window residency command, and failed provider switches.                                                                                                                                                                                                                                                                                                                       | Only the exact window capability is authorized; renderer DTOs remain path/prompt-free; revisions increase strictly; stale/conflicting saves are atomic failures; the main status surface remains read-only and its separate command surface authorizes only stale-checked `load`/`unload`; provider selection commits only after main success and otherwise keeps the prior provider.                                                                    |
| AC-AUTO-060 | Feed the patched `whisperCpp` loader truncated data at every field boundary, oversized/overflowing dimensions, tensor counts, names and products, inconsistent object size/hash, and close/error races.                                                                                                                                                                                                                                                                                                                                                                                 | Exact-read and checked-arithmetic guards reject every malformed object with a typed safe failure before out-of-bounds allocation/read; EOF is accepted only at the defined record boundary; the same authority supplies size/content and closes exactly once.                                                                                                                                                                                            |
| AC-AUTO-061 | Place malicious backend libraries in the current directory and environment paths, vary upstream default/fetch/native-architecture flags, inspect dynamic dependencies, relocate packs, and start them in a clean network-denied environment.                                                                                                                                                                                                                                                                                                                                            | Workers ignore ambient backend discovery; every build uses the reviewed explicit option/toolchain/architecture set; no configure/build fetch occurs; expected-file and dynamic-library closure is complete; relocated clean startup succeeds only with manifest-owned libraries.                                                                                                                                                                         |
| AC-AUTO-062 | Exercise patched `whisperCpp` cancellation before mel, during mel, between graph stages, during CPU compute, and against an unresponsive mocked GPU backend; then attempt late responses.                                                                                                                                                                                                                                                                                                                                                                                               | Cooperative CPU/stage cancellation returns `CANCELLED` distinctly and joins cleanly; an unresponsive backend is forcibly terminated within bounds; no late success is accepted, and residency is retained only after the exact healthy-worker proof allowed by LIFE-006.                                                                                                                                                                                 |
| AC-AUTO-063 | Enumerate accepted engine literals and inspect active Local Whisper catalogs, source locks, build definitions, tests, runtime-pack manifests, and model mappings.                                                                                                                                                                                                                                                                                                                                                                                                                       | Only the fixed `whisperCpp` literal is accepted; forged `fasterWhisper` is rejected; no active Faster-Whisper, CTranslate2, embedded-Python inference, alternate-engine pack, source lock, model mapping, build definition, or test remains. Git history may retain prior evidence.                                                                                                                                                                      |
| AC-AUTO-064 | Exercise `pinned-raw-model-v1` with exact/short/long bodies, content encoding, multipart, changed commit/file/length/hash, unsafe redirects, expiring CDN URLs, `Content-Disposition` path attacks, interrupted ranges, changed ETag/object identity, cancellation, restart, and rate-limit/outage responses.                                                                                                                                                                                                                                                                           | Only the exact catalog-pinned public Hugging Face object streams into one owner-private staging file and promotes after whole-file verification; fresh allowlisted signed redirects may resume the same object, no transport metadata selects a path/profile, no token/cookie/private header is sent, and every mismatch preserves installed revisions with a typed setup failure.                                                                       |
| AC-AUTO-065 | Mutate every qualification/production payload-schema field, purpose, key ID, keyring, origin/redirect policy, transfer profile, upstream model identity, qualification status/profile digest, and Task 17 fixture byte.                                                                                                                                                                                                                                                                                                                                                                 | Strict schema/purpose/key isolation rejects every mismatch; fixture bytes/digest remain unchanged; production release collection rejects qualification inputs; remote metadata cannot enable a transfer profile, mirror, moving branch, or unknown redirect host.                                                                                                                                                                                        |
| AC-AUTO-066 | Compose the production artifact port with real catalog resolution and streaming lifecycle; exercise download, queued concurrency, resume, retry, cancel, verify, runtime extraction, raw-model materialization, atomic promotion, update alongside selected old revision, inventory refresh, and safe removal through renderer/preload/main contracts.                                                                                                                                                                                                                                  | Every explicit renderer action reaches exactly one trusted main-owned operation with revision/epoch checks and bounded progress; no URL/path authority crosses IPC; state/inventory transitions are atomic; prior installed revisions remain usable on failure; the current removal behavior remains compatible.                                                                                                                                         |
| AC-AUTO-067 | Construct the corrected v2 shared candidate, Linux/Windows platform-input, profile, platform-graph, result, and evidence-index documents in valid order; then remove, duplicate, reorder, mix, or back-reference every digest edge, inject the legacy candidate/profile cycle and placeholders, vary SemVer/source/dirty state, and test predecessor releases before/at/after the shared freeze timestamp.                                                                                                                                                                              | Only the complete acyclic shared-core/platform-branch graph validates. The later Linux task can seal Linux without Windows artifacts; the later Windows task adds Windows without changing the shared input or Linux branch; only the aggregate task seals the final root. Legacy circular v2 shapes, fixed-point/placeholders, unhashed bindings, mixed candidates/platforms, mutable versions, and wrong predecessors fail closed.                     |
| AC-AUTO-068 | Resolve the release-1 model matrix against immutable `ggerganov/whisper.cpp` commit `5359861c739e955e79d9a303bcbc70fb988958b1`; add/remove/substitute files, variants, repository commits, mutable-branch URLs, estimates, notices, and qualification statuses.                                                                                                                                                                                                                                                                                                                         | Exactly tiny/base/small/medium `full` plus large-v3/large-v3-turbo `q5_0` are qualification-eligible; all six have exact source/size/hash/notice/memory records; every extra variant is `estimateOnly`; missing or changed upstream identity makes the catalog invalid rather than triggering project conversion/rehosting.                                                                                                                              |
| AC-AUTO-069 | Materialize the pinned FLEURS `en_us`/`ru_ru` corpus twice from the exact upstream commit using the checked-in selection manifest; mutate TSV/audio/license/tool/conversion/normalization hashes and inject private-path/audio metadata.                                                                                                                                                                                                                                                                                                                                                | Both runs produce identical privacy-safe clip, reference-text, WAV, performance-fixture, and corpus-manifest digests; every identity/license/format mutation fails before qualification; no private user recording, local source path, speaker identifier, or raw audio is committed to platform-result evidence.                                                                                                                                        |
| AC-AUTO-070 | Build each CPU/CUDA runtime pack twice in independent clean network-denied roots; mutate source/toolchain/options/dependency closure/file manifest/mode/hash/archive metadata/signature input/SBOM/provenance/notice fields.                                                                                                                                                                                                                                                                                                                                                            | Independent outputs have identical canonical manifests and deterministic restricted archives; every undeclared or missing component fails candidate admission; build, provenance, SBOM, notice, and artifact digest chains are complete and checksum-linked.                                                                                                                                                                                             |
| AC-AUTO-071 | Compare latest-alpha source/candidates/evidence with final source and independently rebuilt final candidates; vary product, runtime, packaging, compatibility, model, setting/protocol, version, changelog, tag-derived metadata, signing, trust, and origin fields.                                                                                                                                                                                                                                                                                                                    | Final admits only the allowlisted SemVer/changelog/tag-derived preparation and required release metadata delta. Product/runtime/packaging/compatibility changes fail and require another alpha; deterministic/reproducibility/signing evidence binds the fresh final candidates; latest-alpha platform results remain distinct lineage inputs rather than exact-final-byte test evidence.                                                                |
| AC-AUTO-072 | Validate Linux/Windows profiles, measurement series, results, and evidence indexes with a wrong candidate-input/platform-input/platform-graph/profile edge or missing/wrong timing source, sample interval, process ownership, RAM/VRAM tool/API, rounding, tolerance, settling interval, WER tokenizer, warm-up/discard rule, repetition count, corpus/direct-engine hash, or evidence-series digest.                                                                                                                                                                                  | Only the exact acyclic platform branch and Section 19.2 algorithms/bounds are accepted; profiles and the platform graph are frozen before measurement, units remain bytes/nanoseconds, result/index evidence is checksum-linked and privacy-safe, and reviewers cannot alter a method, tolerance, artifact branch, or candidate binding after observing results.                                                                                         |

| AC-AUTO-073 | Evaluate implementation readiness with complete Windows and Linux CPU/CUDA production-path code, platform adapters, packaging contracts, and deterministic artifacts but no public alpha or platform-smoke result; then vary missing implementation contracts, alpha deployment evidence, platform smoke, and support claims. | Complete code and deterministic contracts may pass implementation readiness while release evidence remains absent. Missing implementation evidence fails readiness; stable eligibility remains blocked until one public latest alpha has separate passing Linux and Windows smoke and the final lineage/reproducibility gate passes. |
| AC-AUTO-074 | Exercise development activation with packaged/non-packaged identity, absent/valid/malformed descriptors, qualification/fixture/production purposes, altered keys/signatures/origins/compatibility, renderer and persisted-setting injection, and the ordinary production composition root. | Only an explicit non-packaged launch with one authenticated compatible qualification descriptor activates the normal Local Whisper graph. Packaged, implicit, fixture, production-purpose, unsigned, forged, renderer-selected, or persisted activation fails before privileged setup with `CATALOG_UNAVAILABLE`; packaged release collection remains unchanged and no activation claims Production support. |
| AC-AUTO-075 | Feed the ordinary development-activated application catalog complete and incomplete release-1 model sets; enumerate renderer options and per-revision artifact actions; drive `Download`, queue/progress, `Resume`, `Cancel`, `Retry`, verified promotion, inventory refresh, and removal for exact model fixtures through the normal IPC and managed `pinned-raw-model-v1` pipeline; vary family/revision/variant/source/size/hash and CPU/CUDA selection mappings. | Exactly `tiny/full`, `base/full`, `small/full`, `medium/full`, `large-v3/q5_0`, and `large-v3-turbo/q5_0` are present and each normal action reaches one main-owned exact Hugging Face identity without URL/path authority crossing IPC. Every model is independently downloadable/installable/verifiable/removable, selectable for CPU and eligible CUDA settings, and mapped to the same production coordinator/worker contracts. Missing, corrupt, mismatched, or estimate-only substitutions remain explicit Not-ready states and never fall back. |
| AC-AUTO-076 | Table every main-control input across absent/initial status, active and inactive providers, support tiers, selected availability, runtime/model setup states, capability states, all residency/activity states, `canAttempt`, blocking codes, sanitized failures, renderer-local pending, and higher/lower snapshot revisions; exercise keyboard, focus, tooltip, status, alert, and reduced-motion semantics. | The Local Whisper-only stable control has exactly the Section 8.8 visibility, Load/Free/pending label, enabled state, perceivable reason, and accessible announcement. Eligible available-on-demand and retry states invoke Load; only Loaded plus Idle invokes Free; no state optimistically changes, leaks a raw error, becomes cancellation, opens settings, downloads an artifact, or changes another provider's control. |
| AC-AUTO-077 | Invoke the main residency command with exact and substituted windows, inactive provider, stale/current revisions, unknown/extra/malformed fields, duplicate clicks, settings/transcription/lifecycle conflicts, coordinator-safe failures, transport exceptions, provider-switch attempts during load/unload, renderer reload/replacement, and reordered command results/subscription events. | Only one exact active-provider current-revision `load` or `unload` reaches the coordinator and one sanitized lifecycle audit entry. Every rejection occurs before effects with a closed safe result; a pending command blocks provider switching and keeps Local Whisper selected without cancellation or queuing; the highest authoritative snapshot reconstructs state after every race/reload; no full settings, prompt, path, URL, artifact, authority, audio, transcript, stderr, or raw exception crosses the main boundary. |
| AC-AUTO-078 | Feed the main-owned pre-install NVIDIA inventory valid, absent, malformed, oversized, duplicated, reordered, stale, spoofed-name, changed-driver/topology, mixed-device, and unsupported compute-capability records on Linux and Windows. | Only bounded exact-path shell-free inventory becomes private host evidence; raw PCI/UUID/driver/command data never crosses IPC or diagnostics; opaque identities are stable per install; every malformed, stale, ambiguous, or unsupported record fails closed without a CUDA setup action, process launch, download, or fallback. |
| AC-AUTO-079 | Generate authenticated Linux and Windows catalogs with CPU plus the platform's sole `sm_120a-real` CUDA row; inject `sm_86-real`, `sm_89-real`, unlisted, duplicate, missing, and mutated target/platform/architecture/driver/capacity/policy/archive/signature/qualification rows. | The applicability resolver returns exactly one CUDA runtime only for a supported RTX 50 device and no other CUDA artifact; CPU remains independently explicit when applicable; every out-of-scope, zero, duplicate, forged, or incompatible match is safely unavailable before transfer. |
| AC-AUTO-080 | Build the Linux and Windows `sm_120a-real` CUDA packs twice from clean roots and inspect generated code, dependency closure, expected files, SBOM, notices, provenance, signatures, archive identity, and worker handshake target. | Both packs are deterministic and independently authenticated, contain exactly the `sm_120a` real target with no PTX or other target, load only the reviewed CUDA 12.8.1 closure, and reject `sm_86`, `sm_89`, and every other cross-target substitution. |
| AC-AUTO-081 | Exercise fresh, existing-`sm_120a`, CPU, unsupported, disappeared, and multi-GPU settings across migration, device selection, catalog refresh, driver/topology change, transient resource pressure, download commands, and renderer snapshot decoding. | Fresh keys initialize only the sole applicable revision; valid existing selections are preserved; incompatible saved selections remain selected-but-unavailable; renderer receives only suitable actions and sanitized reasons; transient shortages disable/retry without hiding, rewriting, downloading, or falling back. |
| AC-AUTO-082 | Validate post-release alpha smoke inputs/results/indexes for Linux `sm_120a` and Windows `sm_120a` with missing public deployment, substituted asset, cross-OS, cross-target, desktop/laptop, driver, runtime, device-proof, fixture, result, and evidence edges; also submit `sm_86` and `sm_89`. | Each active platform admits only its exact public same-tag assets, `sm_120a-real` runtime, representative RTX 50 evidence, and bounded Section 19.2 result. One operating system cannot satisfy the other; `sm_86`, `sm_89`, prepublication, mixed-alpha, failed, and out-of-scope evidence cannot authorize final. |
| AC-AUTO-083 | Run pull-request and main-branch workflows against production-equivalent Linux/Windows application and CPU/RTX 50 builders, then inspect tokens, permissions, environments, outputs, and repository state. | All six production output classes compile and pass their applicable deterministic checks with non-production trust; workflows remain read-only, create no source commit/tag/release, upload no user-installable asset, receive no production secret, and claim no physical-device qualification. |
| AC-AUTO-084 | Build each Linux/Windows CPU and `sm_120a-real` runtime pack twice in independent clean, disconnected roots from the same locks and explicit toolchain profile; vary an input, ambient tool, network-fetch attempt, target, expected file, dependency, SBOM, notice, provenance, or signature input. | Identical inputs produce identical archive bytes and metadata identities; every changed, ambient, missing, networked, cross-platform, or cross-target input fails before candidate admission. Application builders consume only compatible catalog/runtime identities and keep native workers out of base installers. |
| AC-AUTO-085 | Exercise release workflow policy with feature/release pull requests, forks, branch pushes, unreviewed dispatches, dirty or mutable heads, stale manual-script registry, incomplete changelog/version/tag data, post-freeze commits, stale base, rewritten merge, premature tag, absent reviewer/signing input, platform-smoke jobs, protected contexts, a missing publication job, default-enabled publication, or candidate jobs with release-write authority. | Only one clean prepared release head enters protected candidate production and only a preserving merge may pass. Task 32's path remains read-only and nonpublishing by default while preserving one Task 33 publication job gated by explicit authorization, matching tag, and the verified complete candidate. Alpha publication requires deterministic/signing/legal gates but no prepublication platform smoke; alpha smoke jobs run afterward read-only with no write/secrets. Final additionally requires latest-alpha dual-pass evidence, allowed source delta, and reproducibility. Every authority/order/identity violation fails closed. |
| AC-AUTO-086 | Validate a complete same-tag release candidate containing every configured Linux x64 application package (`AppImage`, `deb`, and `rpm`), the Windows x64 NSIS installer, four separate Linux/Windows CPU/RTX 50 `sm_120a-real` runtime archives, production catalogs and public keyring material, every required detached/native signature, checksums, signed release manifest, SBOMs, notices, provenance, and compatibility records; inject missing, extra, duplicate, synthetic, qualification-purpose, cross-tag, renamed, reordered, wrong-size/hash/signature/platform/target/app-revision assets. | The exact complete physical inventory and all bidirectional catalog/manifest/signature bindings pass. Any missing, extra, duplicate, mutable, disabled, qualification-only, cross-release, or mismatched identity blocks publication; a static policy or metadata fixture cannot substitute for constructed bytes, and no asset is clobbered. |
| AC-AUTO-087 | Build and sign alpha or final candidates, freeze candidate/staging digests, then attempt release-time rebuild, re-sign, timestamp replacement, repackaging, catalog regeneration, metadata mutation, substitution, or final product delta after the latest passing alpha. | Publication accepts only the exact frozen signed bytes and manifest. Every post-freeze change fails closed. Final also requires allowed-delta and independent clean-build reproducibility evidence; latest-alpha smoke evidence authorizes lineage only and never substitutes for fresh final candidates. |
| AC-AUTO-088 | Fault-inject cancellation, timeout, duplicate invocation, partial upload, verification failure, pre-existing filename, and retry around protected release staging and publication. | Incomplete work remains non-installable; only attempt-owned proven staging may be removed or quarantined; published assets are never overwritten; a retry resumes from immutable verified candidates; rollback selects a prior approved release instead of mutating a tag. |
| AC-AUTO-089 | Resolve and install application, bundled helper/browser, CPU worker, and RTX 50 worker candidates from approved same-tag GitHub Release URLs and from CI artifacts, raw/branch URLs, mutable tags, mirrors, arbitrary origins, cross-tag assets, and redirected substitutions; separately resolve the six pinned model objects. | Only exact signed GitHub Release application/native assets and exact catalog-pinned Hugging Face model objects pass. Every other binary origin fails before download, installation, or execution, and renderer/user input cannot expand the allowlist. |
| AC-AUTO-090 | Download every staged candidate through its canonical final GitHub Release asset endpoint and compare it with the signed manifest, catalog, native/artifact signatures, release input, and platform/target applicability before publication. | Every byte, identity, binding, applicability rule, and signature matches while staging remains non-installable. Any unavailable or divergent final-origin object blocks publication. Physical install/runtime behavior is deliberately absent from staging verification and is exercised only by separate post-publication alpha platform smoke. |
| AC-AUTO-091 | Exercise one and multiple sequential alpha generations followed by final, including invalid alpha numbers, branch/tag/prerelease state, release/test/feedback order, missing or failed platform result, mixed generation, six-output set, final allowlisted delta, reproducibility, and candidate/evidence reuse. | Only `Release alpha.N → Linux smoke + Windows smoke → feedback → either Release alpha.(N+1) or Release v2.4.0` is admitted. Alpha numbering is gap-free; every alpha is public before either smoke; either failure permits only next alpha; final requires both latest-alpha passes, final-only source delta, and fresh reproducible signed outputs; final has no physical platform-test branch. |

### 19.2 Public-alpha platform smoke and optional extended diagnostics

**IMPL-001, IMPL-002, QUAL-004, QUAL-007** The required representative release test is one bounded post-publication smoke on Linux x64 and one on Windows x64 for every alpha. Each operating system independently consumes the exact public same-tag application installer, CPU runtime pack, RTX 50 `sm_120a-real` runtime pack, signed manifest/catalog, and pinned Hugging Face `base/full` model. The smoke begins from a clean task-owned application/runtime inventory and covers install, exact runtime/model download, compatibility check, CPU load/warm-up/transcription/unload, RTX 50 load/warm-up/transcription/unload with device/no-fallback proof, offline restart and reuse, and cleanup with no orphan.

The fixture is one checksum-pinned non-private deterministic mono 16-kHz signed 16-bit PCM WAV with a normalized expected non-empty transcription contract. Each smoke binds the alpha `deploymentDigest`, public asset records, platform/architecture, representative host class, driver/runtime/device proof, fixture digest, tool versions, exact start/end timestamps, bounded timeout, sanitized result, and evidence digest. Raw host paths, device identifiers, audio, transcripts, prompts, environments, keys, and private logs stay outside repository and public evidence.

Linux and Windows smoke are separate and neither substitutes for the other. Both may begin only after the alpha is public and final-origin verified. A failed, cancelled, missing, mixed-alpha, wrong-origin, fallback, orphan, privacy-violating, or incomplete result remains truthfully failed, never mutates the alpha, and blocks final. Feedback may then select only a fresh sequential alpha containing every accepted fix. Both passes allow—but do not themselves authorize—feedback to select final.

The former all-six-model FLEURS/direct-engine accuracy, RTF, resource, repetition, crash, and lifecycle profile is retained below as optional nonblocking diagnostic tooling. It is not required for any `v2.4.0-alpha.N` or `v2.4.0` merge, tag, publication, support label, or release, and it never substitutes for the required public-alpha `base/full` platform smokes.

When an extended diagnostic is explicitly authorized, it SHALL use one checked-in immutable profile frozen before measurement. The profile binds the Section 9.6 release identity, exact OS build/family/architecture, reference hardware, driver/runtime/ISA, engine/backend/device proof, tool versions, all six canonical model identities, corpus/direct-engine manifests, repetitions, algorithms, and pass limits. A schema validator rejects any missing algorithm, bound, unit, source identity, graph edge, or evidence digest; reviewers never choose or relax values after observing results. Passing or failing this optional profile changes no current release decision.

**QUAL-002, PRIV-005** When the optional extended diagnostic is run, its shared accuracy corpus SHALL be derived only from public FLEURS repository `google/fleurs` commit `70bb2e84b976b7e960aa89f1c648e09c59f894dd`. Before execution, a checked-in manifest SHALL select at least ten `en_us` and ten `ru_ru` test clips with distinct normalized reference text and at least 120 seconds total audio per locale. It SHALL bind the upstream TSV/audio-archive URLs and hashes, selected member names and hashes, reference text, CC BY 4.0 notice, materializer source/tool version, and resulting canonical WAV hashes. A project-owned diagnostic-only materializer MAY use a pinned Python standard-library toolchain, but SHALL stream only exact allowlisted regular members from verified archives into a fresh temporary root, reject traversal/links/special/duplicate entries, validate mono 16-kHz signed 16-bit PCM, and emit no dependency into the shipped application. Raw corpus archives/audio remain reproducible caches; private user audio and local source paths are forbidden.

WER normalization is fixed: decode valid UTF-8; apply Unicode NFKC; map `’` to ASCII apostrophe; apply locale-specific lowercase with the profile-pinned Node/ICU runtime; tokenize only maximal Unicode letter sequences with an optional internal apostrophe and maximal decimal-number sequences; discard all other punctuation/symbols; compare the resulting token sequences with unit-cost Levenshtein insertions, deletions, and substitutions. Corpus WER is total edit distance divided by total reference-token count, not an average of per-clip percentages. Empty references are invalid. Every application run and direct-engine run uses the identical WAV, model, language, prompt, greedy temperature-zero decoding, thread/device mapping, and normalization implementation.

The direct-engine reference SHALL be a checksum-pinned qualification-only binary built from the same verified patched `whisper.cpp` source/toolchain/runtime backend as the candidate worker, but calling the engine directly without Electron, IPC, coordinator, cache, or worker framing. Its command/config/mapping manifest and binary hash are frozen in the profile. For every canonical model on the cell, application WER SHALL be no more than 1.00 absolute percentage point above its direct reference and no request may drop, duplicate, partially emit, or cross-contaminate text.

Performance fixtures SHALL be five checksum-pinned, exactly 60.000-second WAVs deterministically assembled from the selected FLEURS clips with 250-ms zero-valued PCM separators and truncation/padding only at a clip boundary plus terminal silence. With `base/full` already Ready, one unmeasured warm-up transcription is discarded; then each fixture is measured once. RTF uses `process.hrtime.bigint()` from immediately before the complete audio request is handed to the production worker transport until the one final response is accepted, divided by exactly 60 seconds. It includes production framing/worker inference but excludes artifact download, model load, warm-up, UI rendering, cache hits, clipboard/history, and evidence serialization. Median RTF across the five fixtures SHALL be `<= 1.0`.

Resource sampling SHALL run every 100 ms from immediately before load through full load, warm-up, measured transcription, unload, and settlement:

- Linux RAM: sum `Pss` from `/proc/<owned-pid>/smaps_rollup` for the exact start-identity-validated Local Whisper worker/helper process tree, converting KiB to bytes with factor 1024;
- Windows RAM: sum `PROCESS_MEMORY_COUNTERS_EX.PrivateUsage` from `GetProcessMemoryInfo` for exact Job-Object-owned PIDs/start identities;
- Linux NVIDIA VRAM: sum `usedGpuMemory` from NVML `nvmlDeviceGetComputeRunningProcesses_v3` for exact owned worker PIDs on the selected device UUID, with driver/NVML versions pinned;
- Windows NVIDIA VRAM: sum the PDH `GPU Process Memory` `Dedicated Usage` counters whose instance PID and adapter LUID match the exact Job-Object-owned worker PIDs and selected adapter; the counter-path resolver and PDH version are pinned.

No unrelated system/application usage is subtracted because every metric is filtered to owned PIDs; pre-load/unloaded baseline is zero owned process/allocation. Missing permissions, unavailable per-process memory, PID/LUID/UUID ambiguity, a sample gap over 500 ms, or tool/API failure invalidates the result rather than estimating it. For each model/backend, the qualified peak is the maximum observed byte value across required runs rounded upward to 64 MiB. The acceptance tolerance is `max(5% of published qualified peak, 128 MiB)` for RAM and `max(5% of published qualified peak, 64 MiB)` for VRAM. Catalog `qualifiedPeak` records SHALL bind the result/profile digest; advisory Section 8 ranges remain separate.

Unload, cancellation, crash, provider switch, suspend/resume, and application exit each have a 10-second settlement bound. Passing requires the owned process tree absent and zero matching GPU allocation for ten consecutive 100-ms samples before the bound. Across 10 consecutive load/unload cycles and 20 sequential transcriptions per profile, no load/transcription peak may exceed the first five-run median by more than its tolerance, no five consecutive settled peaks may be strictly increasing with final-minus-first above 64 MiB, and every unload must return to zero owned allocation. The profile SHALL also require all Section 7 stage bounds, cancellation of download/load/transcription, injected worker crash followed by fresh reload/transcription, exact provider-switch behavior, suspend/resume cleanup, app-exit cleanup, and offline restart/load/transcription after verified installation.

**MODEL-011, QUAL-003** Release 1 exposes the six canonical variants fixed by Section 5. Physical release evidence covers only `base/full` on CPU and RTX 50 for each operating system; it makes no qualified per-model accuracy, performance, RAM, or VRAM claim for the other five variants. Optional extended diagnostic results may inform later catalog guidance but do not gate `v2.4.0`.

**QUAL-005** NVIDIA smoke has two independent compute-target/platform cells: Linux `sm_120a` and Windows `sm_120a`. Each cell SHALL bind one representative physical RTX 50-series desktop or laptop device, its exact driver/runtime/app identities, and the bounded Section 19.2 public-alpha evidence. Passing one form factor establishes bounded functional support for that platform's RTX 50 compute-target cell, not every RTX 50 SKU's performance; stable capacity filtering remains authoritative.

**QUAL-006, OPS-004** The current user-controlled RTX 50 hardware pool can execute both active `sm_120a` alpha-smoke cells. RTX 30 / `sm_86` and RTX 40 / `sm_89` are outside the current implementation, release, test, and support scope; they SHALL NOT appear as Pending gates or block RTX 50 release. Their retained future roadmap packet is non-executable until a later approved specification revision defines support, delivery, migration, and representative Linux/Windows evidence.

### 19.3 Manual and hardware gates

| ID         | Environment and procedure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Passing condition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-MAN-001 | Optional extended Linux NVIDIA diagnostic: install the pinned `whisperCpp` CUDA pack and run all six canonical model configurations plus hybrid-device selection under the frozen diagnostic profile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Integrity, device/backend, model, parity/resource/lifecycle, base RTF, repetition, crash recovery, and cleanup evidence is recorded for advisory analysis only. Passing or failing does not gate alpha or final release.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| AC-MAN-002 | Optional extended Windows/Linux x64 CPU diagnostic with GPU target explicitly not selected and all six canonical model configurations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | No GPU initializes and the selected correctness/resource/lifecycle measurements remain advisory only. The required release gate is the separate `base/full` CPU path inside **AC-MAN-017** or **AC-MAN-018** for the latest public alpha.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| AC-MAN-003 | Optional extended representative Windows x64 NVIDIA diagnostic using the pinned `whisperCpp` CUDA pack and all-six-model profile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Diagnostic results remain Windows-only advisory evidence and cannot substitute for **AC-MAN-018**, block publication, or authorize final.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| AC-MAN-004 | Available hybrid NVIDIA/Intel laptop: persist NVIDIA selection, restart, change availability where possible, and enumerate again.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Opaque selection is stable; Intel is not advertised as supported; no automatic device/backend switch occurs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| AC-MAN-005 | Qualified NVIDIA hardware: inject crash, repeat at least 10 load/unload and 20 transcription cycles, switch provider, suspend/resume, and exit while observing process and GPU ownership.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | No orphan child/listener remains; worker-owned allocation disappears within profile bounds; later reload/transcription succeeds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| AC-MAN-006 | Disconnect network after artifact installation, restart, load, warm up, transcribe, unload, and inspect traffic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Inference/lifecycle succeeds offline and emits no request; network begins only after an explicit artifact action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| AC-MAN-007 | Platform qualification: interrupt/resume exact runtime `restricted-tar-gzip-v1` bytes through the isolated loopback HTTPS qualification origin and one pinned public Hugging Face `pinned-raw-model-v1` transfer, cancel another, exercise a fresh expiring Hugging Face CDN redirect, update alongside an old selected revision, then delete a selected loaded revision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Both qualification transports satisfy exact range/validator/redirect/privacy/trust contracts; runtime extraction and raw-model materialization pass independently; recovery/progress are correct; the previous revision remains usable; deletion confirms/unloads/removes exact files and preserves the missing selection without fallback. This is not final GitHub-origin publication evidence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| AC-MAN-008 | Settings UI at 560×680 and 440×520, keyboard-only and screen reader, with long labels, Advanced, progress, errors, confirmations, and all tier badges.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Every control, error, status, and disabled reason is reachable, associated, perceivable, and unambiguous.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| AC-MAN-009 | AMD release review without AMD hardware execution. Inspect manifests, mocks, UI, docs, and claims.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | AMD is consistently Preview and explicitly untested; only the specified `whisperCpp` paths are present; no Production or hardware-success statement is made. This review is not evidence that AMD inference works.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| AC-MAN-010 | Future representative Windows and Linux AMD cards for every claimed backend: install, exact probe, qualifying models, repeated inference, crash, load/unload, suspend/resume, and exit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Windows Vulkan and Linux HIP/Vulkan pass separate profiles; HIP matches exact allowlists. AMD cannot be promoted until representative cards for every claimed OS/backend pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| AC-MAN-011 | macOS arm64 build fixture or future M1+ host without executable runtime installation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | UI reports Planned/unavailable, no download/Ready/execution path exists, and Metal skeleton fails safely. This is not production support evidence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| AC-MAN-012 | Review runtime redistribution plus public-upstream model-download facilitation: licenses, provenance, signing, key rotation/denylist, deterministic runtime archives, pinned Node/Electron gzip implementation, SBOM/notices, Hugging Face/model-card terms, and production trust/publication authority. If the optional FLEURS diagnostic is run or its evidence is published, separately review its CC BY 4.0 source and notice.                                                                                                                                                                                                                                                                                                                                                                                                                                           | Every redistributed runtime file/codec is accounted for and legally distributable; every upstream model source and required notice is approved for production use; protected signing/publication evidence and checksum-linked provenance/SBOM are complete before production catalog inclusion. Any optional corpus source and notice is approved before that diagnostic runs or its evidence is published, without becoming a release gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| AC-MAN-013 | Optional extended diagnostic: run the immediately preceding packaged application binary for a representative platform using a recorded version/hash and nonprivate fixture profile with `local-whisper` selected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The result may inform rollback guidance but is not a `v2.4.0` release gate. Any result published as advisory evidence is privacy-safe, exact-version-bound, and never fabricates compatibility.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| AC-MAN-014 | For any alpha or final target, freeze the prepared release head, expected tag, catalog/runtime/application inputs, and use the protected environment to build/sign the exact six-output set. Preserve that head through merge, create the immutable tag on it, stage without clobbering, and final-origin verify the same bytes before publication.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Source, candidate, tag, signed manifest, catalogs, staged assets, and public assets remain one immutable chain. No rebuild, re-sign, timestamp replacement, repackaging, catalog regeneration, or metadata mutation occurs after freeze. Alpha physical smoke is deliberately post-publication; final uses latest-alpha lineage and reproducibility instead of final physical tests. Every external action remains separately authorized.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| AC-MAN-015 | On the available Linux NVIDIA laptop, launch the ordinary non-packaged desktop application through the explicit authenticated development channel. Through normal model-management controls, invoke `Download` for all six exact release-1 artifacts from their real anonymous public Hugging Face origins; exercise visible queue/progress and at least one bounded cancel/resume or retry; verify and install every object through the ordinary renderer/preload/main artifact flow. Confirm that every installed model can be selected and saved for CPU and the eligible NVIDIA CUDA device. Then use the same real `base/full` model to complete the normal settings/provider flow once on CPU and once on CUDA: `Check compatibility`, `Load now`, transcribe one non-private deterministic WAV through provider dispatch, verify successful completion, and `Unload`. | All six exact pinned Hugging Face objects are simultaneously installed, integrity-valid, independently manageable, and selectable for both targets without pre-seeding, direct copying, an estimate-only substitution, or fallback. Download progress and recovery remain responsive and privacy-safe. The bounded `base/full` runs complete through renderer/preload/main and the production coordinator without mocks or the isolated qualification runner. CPU initializes no GPU; CUDA proves the selected physical NVIDIA device and no CPU fallback; both runs fully load, warm up, return expected non-empty transcription through the normal completion path, unload with confirmed worker exit/allocation release, and leave no orphan. Other five models are ready for user testing but need not run the later hour-long qualification matrix in Task 19. The development label remains visible, packaged production remains fail-closed, and no qualification result, resource claim, or Production verdict is created. |
| AC-MAN-016 | With Local Whisper selected in the ordinary main window, use keyboard-only navigation and a screen reader to invoke the new main-screen control on the installed real `base/full` model once for CPU and once for the eligible NVIDIA CUDA device. Observe status loading, Load, Loading, Free, Transcribing, Freeing, retryable failure, provider-switch-during-pending, renderer reload, and switch-away-after-settlement behavior without opening settings for lifecycle actions.                                                                                                                                                                                                                                                                                                                                                                                         | Labels, focus, disabled reasons, status/alert announcements, pending motion, and sanitized recovery remain perceivable without layout collision. Each Load performs full validation/load/warm-up and reaches the exact Ready target; each Free confirms worker exit and RAM/VRAM release; a pending action blocks provider switching and survives renderer reload without cancellation, duplication, fallback, download, raw error disclosure, or orphan; remote-provider and Ollama Prettify controls remain unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| AC-MAN-017 | After `v2.4.0-alpha.N` is public and final-origin verified, on a representative Linux x64 RTX 50 host begin from a clean task-owned inventory and use only that tag's public Linux installer, CPU pack, and `sm_120a-real` pack plus the pinned Hugging Face `base/full` model. Execute Section 19.2 on CPU and RTX 50.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Clean install/download, exact origin/identity, compatibility, load/warm-up, one non-private deterministic transcription, unload, offline restart/reuse, cleanup, CPU-no-GPU, RTX-device/no-fallback, no-orphan, and privacy checks pass. The result binds the public alpha and cannot substitute for Windows. Failure preserves the alpha and blocks final.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| AC-MAN-018 | Independently repeat the public-alpha Section 19.2 smoke on representative Windows x64 RTX 50 hardware through the packaged installer, native dependency, launcher/Job Object, and platform cleanup path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The exact public Windows installer, CPU pack, and `sm_120a-real` pack pass the same bounded origin/install/runtime/model/transcription/offline/cleanup/privacy contract. Linux evidence cannot substitute. Failure preserves the alpha and blocks final.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

| AC-MAN-019 | For final `v2.4.0`, prepare a dedicated release branch after both latest-alpha platform smokes pass and feedback selects final. Review registered release/smoke scripts, complete changelog/version/tag inputs, prove a clean head, and prove an allowlisted final-only delta. Rebuild/sign all six outputs, complete independent clean-build reproducibility plus legal/reviewer/signing gates, preserve the head through merge/tag, stage/final-origin verify without clobbering, and authorize stable publication without physical final tests. | The latest alpha aggregate, allowed source delta, final candidates, reproducibility records, source/tag identities, signatures, and approvals are genuine and complete. Published final assets are byte-identical to the staged final generation; no alpha asset substitutes for them. Missing alpha pass, product delta, mismatch, or authority blocks stable publication while every alpha remains unchanged. |
| AC-MAN-020 | For every positive sequential `N`, build and deploy `v2.4.0-alpha.N` from `release/v2.4.0-alpha.N`: construct and sign the complete physical inventory represented by the six logical output classes—Linux x64 `AppImage`, `deb`, and `rpm`; Windows x64 NSIS; four separate Linux/Windows CPU/RTX 50 `sm_120a-real` runtime archives; and every required catalog, public keyring, detached/native signature, checksum, signed manifest, SBOM, notice, provenance, and compatibility asset—then pass deterministic/reproducibility/legal/reviewer gates, preserve the frozen head through merge, create the immutable tag, stage and final-origin verify without clobbering, and publish the complete GitHub prerelease before any physical platform smoke begins. | Alpha 1 is mandatory and later alpha numbering is gap-free. The public prerelease contains the exact complete signed Linux/Windows physical inventory under the same tag and remains immutable; neither platform needs a CI artifact, qualification pack, unpublished trust input, local substitution, or binary origin other than the release and the six pinned Hugging Face models. Publication success makes **AC-MAN-017** and **AC-MAN-018** eligible but does not imply either pass, authorize final, or create a Production claim. Failure before publication remains non-installable and recoverable; failure afterward requires a later version, never mutation. |
| AC-MAN-021 | After the latest public alpha has separate passing **AC-MAN-017** and **AC-MAN-018** records and feedback selects final with no accepted fix absent from that alpha, build and deploy `v2.4.0` through the final lineage in **AC-MAN-019**. Run deterministic automated, independent clean-build reproducibility, signing/legal, exact-promotion, staging, final-origin, and publication checks, but no physical Linux or Windows final test. | Fresh final identity and all six signed outputs are required; latest-alpha smoke authorizes lineage only and does not substitute bytes. Only allowlisted final preparation differs from the tested alpha source. Stable publication succeeds from exact staged bytes without a final platform-test branch; any product delta, reproducibility mismatch, missing alpha pass, failed authority, or asset divergence blocks final and leaves every alpha unchanged. |

## 20. AMD Feasibility and Promotion Boundary

The research supports AMD feasibility through `whisperCpp`, not production readiness:

- Windows x64: Vulkan Preview only;
- Linux x64: exact-allowlisted HIP Preview, with explicitly selected Vulkan Preview alternative;
- DirectML/Windows ML: excluded because it would add a separate ONNX/decoder stack and does not improve the untested confidence boundary.

No AMD hardware testing occurs in this specification task. AMD remains Preview until representative physical cards on every claimed OS/backend pass installation, pack compatibility, exact device detection, model load/warm-up, repeated transcription, worker crash recovery, load/unload leak checks, suspend/resume, and app-exit cleanup. Mocking or upstream documentation cannot satisfy this gate.

## 21. macOS Future Skeleton

Apple Silicon has a credible future path through `whisper.cpp` Metal and optional Core ML, but it is not a release-1 execution path. The current code contract is limited to shared typed protocol/state shapes, the `metal` identifier, one unavailable adapter, and tests proving that macOS arm64 cannot download an executable pack, become Ready, load, or transcribe. Future production support requires physical Apple Silicon research, a specification revision, packaging/signing work, and dedicated qualification; this specification promises none of those outcomes.

## 22. Release Blockers and Completion Definition

**IMPL-001, QUAL-004, OPS-003, REL-002, REL-003, REL-004, QUAL-007** Product implementation is the input to this ordered release loop:

`Release v2.4.0-alpha.N → Linux smoke + Windows smoke → review feedback → either Release v2.4.0-alpha.(N+1) or Release v2.4.0`

`N` starts at 1 and increments by exactly one. One release operation contains Build followed by Deploy for that version. Linux and Windows smoke are separate, independent post-publication activities and may run in parallel; feedback is a transition gate. No test activity may mutate a public alpha or hold release write/signing authority.

### 22.1 Alpha release and post-publication verification

For every alpha, the release operation SHALL:

- consume only reviewed immutable source, toolchain, catalog, model, policy, and signing inputs; build the Linux/Windows installers, CPU packs, and RTX 50 `sm_120a-real` packs through one target-aware implementation; and keep required build phases network-denied;
- repeat deterministic outputs in independent clean roots and reject missing, extra, ambient, cross-platform, cross-target, unsigned, unlicensed, or unreproducible objects;
- freeze a dedicated release head, exact alpha SemVer/tag, committed version/changelog/manual-registry preparation, six signed candidates, signed manifest/catalogs, legal/provenance inputs, and Section 9.6 candidate/staging graph;
- under separately authorized actions preserve the frozen head through merge, create the immutable tag, stage without clobbering, final-origin verify every asset structurally, and publish the complete public GitHub prerelease;
- perform no physical Linux or Windows smoke before publication and make no final Production support claim.

After publication, separate Linux and Windows activities SHALL run **AC-MAN-017** and **AC-MAN-018** against only that alpha's public final-origin assets. Each records an independent privacy-safe pass or failure. Both passing results permit feedback to select final or another alpha. Either failure blocks final and permits only the next sequential alpha after accepted fixes. Every next alpha restarts release construction, deployment, and both smoke activities with a fresh identity. Publishing or testing an alpha grants no authority for a later release.

### 22.2 Final release without physical platform testing

Final may begin only when the latest public alpha has two passing platform-smoke results, final-origin verification, and an explicit feedback transition selecting final. Every accepted product/runtime/packaging/compatibility/user-visible fix SHALL already be present in that latest alpha.

The final release operation SHALL:

- prove through a canonical delta manifest that the final source differs from the latest passing alpha only by SemVer, changelog, tag-derived preparation, and required release metadata;
- bind exact `2.4.0` / `v2.4.0`, freeze a distinct clean final head, freshly build and sign all six outputs, repeat deterministic builds in independent clean roots, and pass every automated, reproducibility, legal, reviewer, manifest, catalog, signature, and provenance gate;
- preserve the frozen head through merge, create the immutable tag, stage without clobbering, download and structurally final-origin verify the exact staged assets, seal `finalLineageDigest`, and publish stable only after separate approval;
- run no physical Linux or Windows test against final bytes and never treat alpha bytes as final candidates.

Any non-allowlisted final source change requires a new sequential public alpha. Any reproducibility, signature, staging, origin, identity, or authority failure blocks stable publication and leaves every alpha unchanged. A successful final release promotes the fresh final six-output set based on latest-alpha bounded functional evidence plus final deterministic lineage; it makes no all-six-model performance/resource claim.

Release labels remain independently gated:

- Linux CPU and RTX 50 / `sm_120a` Production require the latest alpha's passing Linux smoke;
- Windows CPU and RTX 50 / `sm_120a` Production require the latest alpha's passing Windows smoke;
- RTX 30 / `sm_86` and RTX 40 / `sm_89` remain planned/unavailable with no delivery, test, or support claim;
- AMD remains **Preview · Untested** and requires a future specification before promotion;
- macOS remains **Planned · Unavailable**;
- missing latest-alpha smoke, final lineage, production trust, legal, signing, or publication input blocks stable release.

No private signing material, upload, publication, tag, support-tier promotion, or release may be inferred from specification or task approval. Before execution resumes, `/plan` SHALL combine Build and Deploy for each release version and represent Linux and Windows alpha smoke as separate executable units while preserving every external manual gate and the next-alpha feedback loop.

No task ordering, estimate, implementation packet, commit, release, or publication is authorized by approval of this specification. Implementation planning requires a separate `/plan` invocation.
