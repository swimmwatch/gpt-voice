# Local Whisper Native Review Remediation Handoff

## State

- Packet 01 is contained in user-authored commit `0b4d7825`; that commit also contains unrelated renderer/test edits, which remain outside this workstream.
- Packet 02 completed under `authorization.packet-02-execution` revision 1. The Linux and Windows backends now use RAII for transient descriptors, directory streams, handles, process tokens, security descriptors, and BCrypt hash resources. Instance-scoped constructor injection deterministically fails each resource-acquisition ordinal without production globals. A shared `kMaxLiveLeases` constant caps all retained lease kinds at 64 with `IO_FAILED` before lease-producing mutation.
- Packet 03 completed under `authorization.packet-03-execution` revision 1. A bounded 262,144-byte reader fail-stops on the first over-limit byte without draining; the TypeScript transport bounds raw stdout before newline, rejects pending work once on guard death, and starts a fresh guard for the next request. Common parsing now owns closed platform/artifact/namespace/operation domains, checked process IDs and modes, decoded write bytes, and typed `LIST` entries. Linux and Windows backend `Impl` methods consume typed commands directly; supplied `LIST` expectations require exact name/mode equality, while the existing no-expectation discovery path remains available for partial-staging cleanup.
- Planning decision `plan.remote-completion-backfill` revision 1 keeps Packets 01–03 complete under their former evidence rule. Packet 03 is still uncommitted and must be isolated in its own local commit before Packet 04 implementation begins.
- Beginning with Packet 04, every packet uses a two-phase remote completion gate: candidate commit/push and exact-SHA CI, followed by a completion-record commit/push and exact-SHA CI. Every required Windows job must execute and conclude successfully; a skipped Windows job is never evidence.
- The revised plan is approved under **PLAN-APPROVAL-003**. Approval does not start Packet 04; a new `incremental-implementation` invocation remains required.
- Packet 04 candidate is ready for its first remote gate. Linux launcher and model-launch control directions now become terminal once and are removed from later polls; Windows control directions close once and are no longer probed. The Linux authority receiver owns every installed ancillary descriptor before validation, and Windows revokes an unconfirmed worker duplicate before job termination.
- `WorkerApplication` now owns inference through `std::jthread` and a cancellation-before-join RAII guard. The control owner waits on a platform-neutral control/closed/completion contract, emits all terminal frames, preserves committed transcripts, and returns to warmed state after `cancelTooLate`.
- POSIX uses an owned `eventfd` with `poll`; Windows uses an owned event handle with `WaitForMultipleObjects`. Both serializers use `nlohmann::json::error_handler_t::replace`.
- Private protocol-v1 fixtures and runtime identity digest inputs now cover `cancelTooLate`; TypeScript supervisor/coordinator preserve a transcript when cancellation loses and return nonterminal `OPERATION_CONFLICT` for the cancel request.
- Packet 05 candidate is ready for its first remote gate. Native consumers now share the hardened common SHA-256 implementation, Windows `LIST` hashing retains CNG with digest-agreement coverage, and audio-frame maximums derive from the canonical 136-byte protocol-v1 overhead on both worker channels.

## Changed Files

- Native worker protocol/application and deterministic worker tests under `runtime/local-whisper/whisper-cpp/`.
- Protocol validator, supervisor/coordinator behavior, conformance fixtures, and focused TypeScript tests under `src/` and `tests/`.
- Worker-vector generator and regenerated `tests/fixtures/local-whisper/protocol/v1/` fixture set, including `control/cancel-too-late.bin`.
- Native runtime identity inputs in `scripts/local-whisper/whisper-cpp-build-core.mjs` and Windows MSVC worker-core CI wiring in `.github/workflows/pr-checks.yml`.
- Filesystem-guard ownership, capacity, and cross-platform baseline tests in `runtime/local-whisper/fs-guard/`, including Linux `/proc/self/fd` and Windows `GetProcessHandleCount` coverage.
- Filesystem-guard bounded reader, typed command/domain values, exact expected-list checks, and real-backend matrix in `runtime/local-whisper/fs-guard/`; native guard transport buffering/restart coverage in `src/main/localWhisper/filesystem/` and `tests/main/localWhisper/filesystem/`.
- Packet 04 launcher/model-launch lifecycle and authority-transfer cleanup under `runtime/local-whisper/launcher/` and `runtime/local-whisper/fs-guard/`; the launcher fixture now closes model-launch ownership control after a successful handshake.
- Packet 05 common SHA-256 lifecycle/vector tests and frame-bound validation under `runtime/local-whisper/common/`; duplicated launcher and Linux guard hash implementations are removed, while the Windows CNG adapter and agreement test are added under `runtime/local-whisper/fs-guard/`.
- Packet 05 launcher/guard CMake linkage, Linux/Windows hash readers, worker protocol pre-allocation validation, and Windows MSVC common-codec workflow coverage.

## Checks

- Passed: `npm run verify:local-whisper:worker-vectors -- --check-clean`
- Passed: `npm run test:local-whisper:worker-codec`
- Passed: `npm run test:local-whisper:whisper-cpp-core` (Linux GCC and Clang ASan/UBSan core suites, formatting, and clang-tidy)
- Passed: `npm run test:local-whisper:supervisor`
- Passed: `npm run test:local-whisper:coordinator`
- Passed: `npm run format:check:local-whisper:worker-common`, `npm run lint:local-whisper:worker-common`, `npm run format:check`, `npm run lint`, `npm run typecheck`, and `npm run test:types`
- Passed: `npm run format:check:local-whisper:fs-guard`, `npm run lint:local-whisper:fs-guard`, `npm run test:local-whisper:fs-guard:unit`, `npm run test:local-whisper:fs-guard:integration`, and `npm run test:local-whisper:fs-guard:native` (Linux Clang ASan/UBSan)
- Passed: `npm run test:local-whisper:filesystem`, `npm run typecheck`, and `npm run test:types`
- Packet 04 local candidate passed: `npm run format:check:local-whisper:launcher`, `npm run lint:local-whisper:launcher`, `npm run test:local-whisper:launcher:native`, `npm run verify:local-whisper:worker-authority -- --platform=linux`, `npm run format:check:local-whisper:fs-guard`, `npm run lint:local-whisper:fs-guard`, `npm run test:local-whisper:fs-guard:native`, `npm run format:check`, `npm run lint`, `npm run typecheck`, and `npm run test:types`.
- Packet 05 local candidate passed: `npm run format:check:local-whisper:worker-common`, `npm run lint:local-whisper:worker-common`, `npm run test:local-whisper:worker-codec`, `npm run test:local-whisper:whisper-cpp-core`, `npm run format:check:local-whisper:fs-guard`, `npm run lint:local-whisper:fs-guard`, `npm run test:local-whisper:fs-guard:native`, `npm run format:check:local-whisper:launcher`, `npm run lint:local-whisper:launcher`, `npm run test:local-whisper:launcher:native`, `npm run verify:local-whisper:worker-vectors -- --check-clean`, `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test:types`, and `npm test` (1,901 passed, 1 skipped). `git diff --check` is clean.

## Remote CI And Final Windows Evidence

- Pull request 58 currently uses head `feat/local-whisper-provider` and base `main`. Packet 04–15 pushes must be non-force pushes to the verified PR head and must bind evidence to the exact pushed SHA.
- Required remote evidence includes **Local Whisper Native Quality (Linux)**, **Local Whisper Native Quality (Windows)**, **Quality Gates**, **Package Smoke (Fedora Linux)**, **Package Smoke (Windows)**, **Actionlint**, every selected `Local Whisper Fixture Packaging` job, and every packet-added native job.
- Automated Windows MSVC build/test/analysis/ASan and PE evidence is packet-local from Packet 04 onward. Packet 15 retains only the final supported-host manual Windows package, manual-only remediation, and final cross-platform regression/closure.
- Linux/shared checks are not Windows evidence, and a skipped Windows job is never evidence.
- Candidate commit `06c6442c23074ed582449c4ee81263d449e99a5c` was pushed non-force to PR 58 and launched [Pull Request Checks 31277185750](https://github.com/swimmwatch/gpt-voice/actions/runs/31277185750) plus [Local Whisper Fixture Packaging 31277185741](https://github.com/swimmwatch/gpt-voice/actions/runs/31277185741). It is not passing evidence: Actionlint rejected the permanent-false Windows AMD placeholder; Linux source provisioning rejected the stale importer implementation digest before compiling; Windows materialized GoogleTest but then rejected the same stale digest, so its MSVC/native steps were skipped. This run provides no Linux or Windows native evidence.
- Authorized CI remediation updates all three reviewed source locks to the current canonical importer identity, turns the Windows AMD entry into an executed static contract check without hardware, driver, or SDK access, and asserts the workflow never contains a permanent-false Windows step. Local checks passed: native-source lock tests and provisioning, AMD formatting/tests plus the Windows AMD contract profile, and the native-CI workflow test.
- Candidate commit `bdad657276255871f08adae6f79202c86404c447` launched [Pull Request Checks 31278062859](https://github.com/swimmwatch/gpt-voice/actions/runs/31278062859). Actionlint and fixture packaging passed, native source materialization now passed on both platforms, but the Windows job failed before MSVC compilation because its developer environment was never initialized and the test script looked for unavailable cached MSVC/SDK directories. Candidate `e827f0685a2ff3fbaf79e6b5ee97bc7198689184` exposed a bootstrap-capture defect before compilation: `vcvars64.bat` did not yield `INCLUDE`. Candidate `1cfaa41faf8290aa2ccb3736635607acca05a5a3` corrected the capture to `vcvarsall.bat x64`; the Windows job then initialized MSVC, compiled the guard, and passed all 20 unit tests. Its integration failures showed transient handles were not reclaimed on `NtCreateFile` failure paths, exact expected `LIST` modes were not enforced, and a case-only artifact alias could be opened. The in-scope repair now gives every `NtCreateFile` result immediate `UniqueHandle` ownership, checks stored modes against supplied expectations, and rejects case aliases before a case-sensitive artifact open. Local Linux native suites, source/native-build/hosted-toolchain tests, workflow test, formatting, lint, and type checks passed.
- Candidate `f5f3fa9c325dfaa74d2da1bd8821372920d84971` launched [Pull Request Checks 31279400549](https://github.com/swimmwatch/gpt-voice/actions/runs/31279400549) and [Local Whisper Fixture Packaging 31279400546](https://github.com/swimmwatch/gpt-voice/actions/runs/31279400546). Actionlint and fixture packaging passed, and the Windows job executed rather than skipping; it initialized MSVC and passed unit tests but failed the fs-guard integration handle-baseline checks plus the direct case-alias regression. The next repair removes raw ownership escapes entirely: `CreateFileW`, `OpenProcess`, `NtCreateFile`, namespace-open, and duplicate-handle results are RAII `UniqueHandle` values from acquisition through validation; Linux and Windows revalidate canonical artifact names before opening. Focused Linux sanitizer, lint, native, and filesystem checks pass; Windows evidence is pending the next candidate.
- Candidate `85aab20c20ff002b9799c429ba50357338dc9b8f` launched [Pull Request Checks 31279831978](https://github.com/swimmwatch/gpt-voice/actions/runs/31279831978). Windows compiled the updated backend, passed unit tests, and passed the case-alias regression, but the two handle-baseline integration tests still failed with the same residual count. The next candidate adds deterministic Windows-only live-handle assertions at root, staging, collision, file, and each release boundary to identify the leaking ownership edge without relying on wall-clock timing or changing production semantics.
- Candidate `f83f8d087d71220071f379e10dd21e2d58201782` launched [Pull Request Checks 31280158823](https://github.com/swimmwatch/gpt-voice/actions/runs/31280158823). The Windows native job executed source provisioning, MSVC initialization, and native unit/integration tests; it failed the new handle assertions before later Windows steps could run. The trace showed a stable one-time 10-handle delta on the first full Windows path, while every subsequent root, staging, and file lease changed the count by its expected two handles and returned to that stabilized count. The follow-up warms the full Windows path before taking each baseline, retaining the per-lease and failure-path checks without treating process-scoped runtime initialization as a leaked lease.
- Candidate `570d0f0bad16fcfd69e5c5c14e7d279650e944ea` launched [Pull Request Checks 31280514614](https://github.com/swimmwatch/gpt-voice/actions/runs/31280514614). Its Windows job executed and passed the MSVC filesystem-guard and launcher native suites, including the stabilized handle regressions, then failed the Whisper.cpp core verifier because raw CRLF checkout bytes changed the loader-limit tool identity. The follow-up canonicalizes CRLF to LF for that reviewed text-tool digest, adds direct line-ending coverage, and regenerates the unchanged limit/layout evidence, provenance, and C++ table identity to the new digest `e625802a6fbf31aba996150df44babc2f5784ab7ce28aa1408ae67bacd53f715`.
- Native quality work is now runner-resource-aware: existing CMake builds retain `resolveNativeBuildJobs`; every native CTest invocation receives that same bounded parallel level; and clang-format/clang-tidy execute independent source checks in bounded cross-platform process batches. The new native-file parallelism unit coverage and focused fs-guard, launcher, worker-common, Whisper.cpp, Linux authority, filesystem, lint, type, and format checks passed locally. This is Linux evidence only; the mandatory Windows job remains pending for the next candidate SHA.
- Candidate `822d5fcc50acad2196e34bd7cacef814dbf03450` launched [Pull Request Checks 31281085873](https://github.com/swimmwatch/gpt-voice/actions/runs/31281085873), [Actionlint 31281085871](https://github.com/swimmwatch/gpt-voice/actions/runs/31281085871), and [Local Whisper Fixture Packaging 31281085882](https://github.com/swimmwatch/gpt-voice/actions/runs/31281085882). Actionlint and fixture packaging passed. Windows executed source provisioning, MSVC initialization, and filesystem-guard unit/integration suites, then its launcher/Job Object integration fixture observed the state file before its two PID records had both been written. The repair publishes that fixture state only after the temporary file is closed, by renaming it into place; five consecutive Linux launcher runs, launcher formatting, and clang-tidy passed locally. The required Windows native evidence remains pending the replacement candidate.
- Candidate `ceb6e5a07cbf85c9aa1d25ba3bd3d4dae45c82c6` launched [Pull Request Checks 31281591825](https://github.com/swimmwatch/gpt-voice/actions/runs/31281591825), [Actionlint 31281591864](https://github.com/swimmwatch/gpt-voice/actions/runs/31281591864), and [Local Whisper Fixture Packaging 31281591837](https://github.com/swimmwatch/gpt-voice/actions/runs/31281591837). Actionlint and fixture generation passed; Windows executed source provisioning, MSVC initialization, filesystem-guard, and launcher/Job Object suites successfully, proving the atomic state publication repair. It then rejected the exact Whisper.cpp patch `exact-loader-reads-v1`: Git converted its reviewed LF bytes to CRLF during the Windows checkout. The new repository attributes force LF for every reviewed Whisper.cpp patch and a native-source regression simulates a `core.autocrlf=true` checkout while asserting byte-for-byte identity. Native-source validation, Linux Whisper.cpp core, and formatting passed locally. The required Windows worker-core evidence remains pending the replacement candidate.
- Candidate `393cfa61e732f8f94ea00e7b6e5440291961dd2b` launched [Pull Request Checks 31281907344](https://github.com/swimmwatch/gpt-voice/actions/runs/31281907344), [Actionlint 31281907340](https://github.com/swimmwatch/gpt-voice/actions/runs/31281907340), and [Local Whisper Fixture Packaging 31281907341](https://github.com/swimmwatch/gpt-voice/actions/runs/31281907341). The patch checkout policy worked: Windows executed source provisioning, MSVC initialization, filesystem-guard, and launcher/Job Object suites, then passed exact patch verification. Whisper.cpp configuration then incorrectly required a nonexistent cached MSVC toolchain although the workflow had prepared a real MSVC environment. The repair captures absolute `cmake`, `ctest`, `cl`, and `ninja` commands after `vcvarsall.bat`, uses them only for hosted Windows quality configuration, and leaves the production locked-toolchain path unchanged. Native-source validation, Linux Whisper.cpp core, workflow contract tests, formatting, lint, and type checks passed locally. The required Windows worker-core evidence remains pending the replacement candidate.
- Candidate `dff35d4e277147f8a5591f7d8bd3011b5fc9a493` launched [Pull Request Checks 31282386092](https://github.com/swimmwatch/gpt-voice/actions/runs/31282386092), [Actionlint 31282386166](https://github.com/swimmwatch/gpt-voice/actions/runs/31282386166), and [Local Whisper Fixture Packaging 31282386081](https://github.com/swimmwatch/gpt-voice/actions/runs/31282386081). Windows completed locked source materialization, then failed only while resolving `cl.exe`: the `vcvarsall.bat` PATH had been written to `GITHUB_ENV` for later steps but was not yet active in the current PowerShell process. The follow-up activates the captured PATH before resolving the explicit native tool paths; the workflow contract asserts that ordering. The required Windows native evidence remains pending the replacement candidate.
- Candidate `6d30f331573487ec9829e0a426e609794d840557` launched [Pull Request Checks 31282583435](https://github.com/swimmwatch/gpt-voice/actions/runs/31282583435), [Actionlint 31282583419](https://github.com/swimmwatch/gpt-voice/actions/runs/31282583419), and [Local Whisper Fixture Packaging 31282583415](https://github.com/swimmwatch/gpt-voice/actions/runs/31282583415). Actionlint and fixture packaging passed. The Windows native job executed and passed filesystem-guard, launcher/Job Object, and Whisper.cpp worker-core suites. Quality Gates later reached its 15-minute timeout: the transcript-first cancellation fixture could race before it consumed final audio, then leave its worker alive after the failed assertion. Both package-smoke jobs were consequently skipped, so this SHA cannot be completion evidence. The prepared repair emits a private fixture-ready marker only after transcript-first state is committed, waits for it before sending cancel, and unconditionally shuts down the supervisor and removes its exact temporary marker directory.
- Candidate `5471eec88984648596230c71b3f152aa4c6940d7` launched [Pull Request Checks 31283554578](https://github.com/swimmwatch/gpt-voice/actions/runs/31283554578), [Actionlint 31283554567](https://github.com/swimmwatch/gpt-voice/actions/runs/31283554567), and [Local Whisper Fixture Packaging 31283554556](https://github.com/swimmwatch/gpt-voice/actions/runs/31283554556). Actionlint passed. Quality Gates reached the repaired transcript-first path but timed out because the in-memory scripted worker never emitted the warmed-reuse transcript after `cancelTooLate`; its Linux and Windows conformance jobs therefore cannot provide final evidence, and package-smoke jobs were skipped. The next repair gives that scripted worker an explicit final-audio readiness promise and emits the normal transcript for its warmed reuse, so the test proves both terminal ordering and worker residency without a microtask race or a pending promise.
- Candidate `a7d5e4743356c2797a09e131226d4fb1be7e1b09` launched [Pull Request Checks 31284992110](https://github.com/swimmwatch/gpt-voice/actions/runs/31284992110), [Actionlint 31284992122](https://github.com/swimmwatch/gpt-voice/actions/runs/31284992122), and [Local Whisper Fixture Packaging 31284992120](https://github.com/swimmwatch/gpt-voice/actions/runs/31284992120). The repaired supervisor suite completed all 46 tests. Quality Gates then exposed 11 pre-existing non-Packet-04 documentation, dependency-injection, translation/About UI, readiness, and qualification assertion failures, so package-smoke jobs were skipped. Linux native quality executed formatter, clang-tidy, sanitizer, launcher, filesystem-guard, authority, and supervisor stages successfully, then failed only because Whisper.cpp core still required the absent cached `cmake-3.31.8` despite the job's explicit system CMake/CTest/Ninja configuration. The next repair makes Whisper.cpp Linux quality select the same explicit prepared tool paths as the workflow, with focused resolver and workflow-contract tests; its GCC/Clang ASan/UBSan core run passed locally with bounded build/test/file-tool parallelism.
- Candidate `931bb82fe2052b671b9e4a5c68fd13334469c93c` launched [Pull Request Checks 31285498333](https://github.com/swimmwatch/gpt-voice/actions/runs/31285498333), [Actionlint 31285498340](https://github.com/swimmwatch/gpt-voice/actions/runs/31285498340), and [Local Whisper Fixture Packaging 31285498339](https://github.com/swimmwatch/gpt-voice/actions/runs/31285498339). Actionlint and fixture packaging passed. Linux executed formatting, all clang-tidy checks, sanitizers, launcher, filesystem-guard, supervisor, authority, loader limits, and the repaired Whisper.cpp core suite; CPU-pack staging then still selected the absent cached CMake. Windows executed and passed MSVC filesystem-guard, launcher/Job Object, and Whisper.cpp worker-core suites plus supervisor conformance, then failed its CPU static contract because both CPU/CUDA verifiers rejected the workflow's `--contract-only` mode before validation. Quality Gates still has the 11 pre-existing failures and package-smoke jobs were skipped. The prepared repair applies hosted Linux tools to CPU/CUDA pack builds and makes the Windows static modes validate reviewed profiles/sources without cached MSVC; local CPU-pack build/verify/audit, both static modes, workflow/native-source/parallelism tests, format, lint, typecheck, and type tests pass.

- Candidate `cecf73b7a75ac10006c99d297761e628a5181c3e` launched [Pull Request Checks 31285941383](https://github.com/swimmwatch/gpt-voice/actions/runs/31285941383). Actionlint and all executed Windows native C++ suites passed: filesystem-guard, launcher/Job Object, Whisper.cpp worker-core, supervisor, authority source contract, CPU/CUDA static contracts, and AMD contract. Linux C++ formatting, clang-tidy, sanitizer, launcher, supervisor, authority, loader, and Whisper.cpp core passed. The Linux CPU-pack audit failed only because GitHub-hosted user-namespace mapping rejected `/proc/self/uid_map`; the Windows Node/native filesystem adapter failed only because its native CRLF response left `OK\r` in the TypeScript status field. Quality Gates retained 11 unrelated failures, so package-smoke jobs were skipped; Packet 04 remains unchecked.
- Pending candidate repair: the transport removes exactly one terminal `\r` before field parsing, with a CRLF regression test. The CPU-pack audit keeps initial `unshare --user --map-root-user --net`; only on the precise GitHub-hosted mapping failure it retries through passwordless `/usr/bin/sudo` with `unshare --net`, preserving the restricted audit environment. The fallback has a deterministic command-contract test. Local passes after the repair: native-source tests, filesystem transport suite, CPU-pack audit, launcher and filesystem-guard sanitizer-native suites, formatting, lint, typecheck, type tests, and diff check. Candidate commit/push and exact-SHA CI remain required.

- Candidate `1f15276ed00fd7b968de06e0cd1befe98694d419` launched [Pull Request Checks 31286568056](https://github.com/swimmwatch/gpt-voice/actions/runs/31286568056). Linux native quality passed completely, including the CPU-pack network-isolation audit with the hosted fallback. Windows executed all MSVC filesystem-guard, launcher/Job Object, Whisper.cpp worker-core, supervisor, authority, CPU/CUDA, and AMD steps successfully. Its CRLF transport regression passed, then the final filesystem compatibility test found reopened artifact directories could not reconstruct the transient logical file-mode map, producing `ARTIFACT_UNPROVABLE`. Quality Gates retained the unrelated unit-test failure, so both package-smoke jobs were skipped.
- Pending candidate repair: Windows `LIST` now continues to fail closed without expectations, but an exact expected-entry map supplies the trusted logical mode when an artifact directory is reopened. The existing Windows promotion/reopen/delete integration test is the regression. Linux filesystem transport and sanitizer-native suites, filesystem formatting, and clang-tidy pass locally. Commit, push, and exact-SHA automated Windows execution remain required.

- Candidate `65d8a20ca555a16ceb4aeebc60476d0ded9d7aff` launched [Pull Request Checks 31286938779](https://github.com/swimmwatch/gpt-voice/actions/runs/31286938779). Both mandatory native jobs executed and passed. Linux completed formatting, clang-tidy, sanitized C++ unit/integration tests, launcher/process-tree tests, worker-core, CPU-pack network-isolation audit, Node/native filesystem compatibility, and fixture verification. Windows completed MSVC filesystem-guard, launcher/Job Object, worker-core, supervisor, model-authority, CPU/CUDA/AMD contracts, and Node/native filesystem compatibility; the reopened-artifact regression passed. Actionlint and fixture packaging passed.
- Completion remains blocked outside Packet 04: Quality Gates failed its existing unrelated unit-test set, which skipped both Package Smoke (Fedora Linux) and Package Smoke (Windows). Do not check Packet 04 or start Packet 05 until this gate is resolved and a fresh exact-SHA run completes every required job.
- Authorized Quality Gates remediation is prepared. It makes `MainInteractionLock` require its operation-state dependency, aligns implementation-readiness checks with plan revision 28 and localized catalog notices, and removes brittle documentation, UI, translation-handler, and Windows CUDA-target test assumptions. Local Quality Gates passed formatting, lint, typecheck, test typecheck, all 1,902 unit tests (1 skipped), dependency audit, and the production build; remote candidate evidence remains required.
- Candidate `0755615ab5a9e30ab95c8b0064cb5a29d1346ff2` passed Actionlint, fixture packaging, Quality Gates, and Windows native Quality. Its Windows package-smoke job failed before packaging because disabled Local Whisper resource preparation invoked native builds without the MSVC developer environment. The follow-up centralizes the proven `vcvarsall.bat` setup in a local composite action, invokes it in both Windows native and package-smoke jobs before native preparation, and extends the workflow contract test to prove the ordering. Focused workflow test, formatting, lint, typecheck, test typecheck, and all 1,902 unit tests pass locally; fresh remote evidence is required.
- Candidate `980d5fbdd75f98ea6eadfe18ca5ecd831fcc1529` passed Actionlint, fixture packaging, Quality Gates, Linux native quality, and both package-smoke jobs. Its Windows native job executed but the parent-death launcher fixture could observe both PIDs exited just before Windows released the temporary fixture directory, yielding `EPERM` during cleanup; the later Windows native steps were skipped. The follow-up retries only Windows `EPERM` fixture-directory cleanup in bounded 50 ms intervals (100 attempts), keeps Linux cleanup immediate, and uses that cleanup in every launcher fixture path. Local launcher native tests, the full unit suite (1,901 pass, 1 intentional skip), formatting, lint, typecheck, type tests, and diff check passed; fresh exact-SHA Windows execution remains required.
- Candidate `b3e79d2a35ab3af9834e315c01bc95a556a8de2b` passed its complete first remote gate: [Pull Request Checks 31298497683](https://github.com/swimmwatch/gpt-voice/actions/runs/31298497683) completed Quality Gates, Linux native quality, Windows native quality, Fedora package smoke, and Windows package smoke successfully; [fixture packaging 31298497679](https://github.com/swimmwatch/gpt-voice/actions/runs/31298497679) and [Actionlint 31298497712](https://github.com/swimmwatch/gpt-voice/actions/runs/31298497712) also passed. The Windows native job executed every required MSVC and launcher/Job Object stage, including the repaired cleanup fixture. Packet 04 is checked; this completion-record commit requires the final exact-SHA gate before starting Packet 05.
- Packet 04 completion record `6eccae3dcc2f766a724e72773abcceec6d9262ab` passed its final exact-SHA gate: [Pull Request Checks 31298858658](https://github.com/swimmwatch/gpt-voice/actions/runs/31298858658), [fixture packaging 31298858677](https://github.com/swimmwatch/gpt-voice/actions/runs/31298858677), and [Actionlint 31298858666](https://github.com/swimmwatch/gpt-voice/actions/runs/31298858666). Linux and Windows native quality, both package-smoke jobs, and all fixture jobs executed and succeeded.
- Packet 05 candidate has passed its local gate and awaits a non-force candidate commit/push followed by the complete exact-SHA remote gate. Windows manual supported-host testing remains deferred exclusively to Packet 15; the Packet 05 MSVC native job must execute and succeed now.

## Exact Next Packet

- Packet 05 — Common crypto and frame contracts. Candidate commit/push and first exact-SHA remote gate are next; do not begin Packet 06.
